///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for ExtensionsExtensionInfo.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsExtensionInfo {
  id : String
  name : String
  version : String
  path : String
  enabled : Bool
} derive(Eq, Debug)

///|
/// Encode ExtensionsExtensionInfo as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsExtensionInfo::to_json(self : ExtensionsExtensionInfo) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["name"] = Json::string(self.name)
  fields["version"] = Json::string(self.version)
  fields["path"] = Json::string(self.path)
  fields["enabled"] = Json::boolean(self.enabled)
  Json::object(fields)
}

///|
/// Decode ExtensionsExtensionInfo from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ExtensionsExtensionInfo::from_json(
  value : Json,
) -> ExtensionsExtensionInfo raise @protocol.ProtocolSchemaError {
  let method_name = "ExtensionsExtensionInfo"
  let fields = typed_json_fields(method_name, value)
  {
    id: typed_required_string(method_name, fields, "id", "string"),
    name: typed_required_string(method_name, fields, "name", "string"),
    version: typed_required_string(method_name, fields, "version", "string"),
    path: typed_required_string(method_name, fields, "path", "string"),
    enabled: typed_required_bool(method_name, fields, "enabled", "boolean"),
  }
}

///|
/// Generated CDP value object for ExtensionsStorageArea.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsStorageArea {
  value : String
} derive(Eq, Debug)

///|
/// Encode ExtensionsStorageArea as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsStorageArea::to_json(self : ExtensionsStorageArea) -> Json {
  Json::string(self.value)
}

///|
/// Decode ExtensionsStorageArea from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ExtensionsStorageArea::from_json(
  value : Json,
) -> ExtensionsStorageArea raise @protocol.ProtocolSchemaError {
  let method_name = "ExtensionsStorageArea"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for ExtensionsClearStorageItemsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsClearStorageItemsParams {
  id : String
  storage_area : ExtensionsStorageArea
} derive(Eq, Debug)

///|
/// Encode ExtensionsClearStorageItemsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsClearStorageItemsParams::to_json(
  self : ExtensionsClearStorageItemsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["storageArea"] = self.storage_area.to_json()
  Json::object(fields)
}

///|
/// Build a typed Extensions.clearStorageItems command envelope.
///
/// Pass a ExtensionsClearStorageItemsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_clear_storage_items_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn extensions_clear_storage_items_command(
  id~ : Int,
  params : ExtensionsClearStorageItemsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.clearStorageItems",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsGetExtensionsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsGetExtensionsResult {
  extensions : Array[ExtensionsExtensionInfo]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Extensions.getExtensions.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ExtensionsGetExtensionsResult::from_json(
  result : Json,
) -> ExtensionsGetExtensionsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Extensions.getExtensions"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    extensions: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "extensions"),
      "extensions",
      "array<ExtensionInfo>",
    ).map(item => ExtensionsExtensionInfo::from_json(item)),
  }
}

///|
/// Build a typed Extensions.getExtensions command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_get_extensions_command(id=1, session_id=None)
/// ```
pub fn extensions_get_extensions_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.getExtensions",
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsGetStorageItemsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsGetStorageItemsParams {
  id : String
  storage_area : ExtensionsStorageArea
  keys : Array[String]?
} derive(Eq, Debug)

///|
/// Encode ExtensionsGetStorageItemsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsGetStorageItemsParams::to_json(
  self : ExtensionsGetStorageItemsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["storageArea"] = self.storage_area.to_json()
  if self.keys is Some(value) {
    fields["keys"] = Json::array(value.map(item => Json::string(item)))
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for ExtensionsGetStorageItemsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsGetStorageItemsResult {
  data : Json
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Extensions.getStorageItems.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ExtensionsGetStorageItemsResult::from_json(
  result : Json,
) -> ExtensionsGetStorageItemsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Extensions.getStorageItems"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  { data: typed_required_result_json(method_name, fields, "data") }
}

///|
/// Build a typed Extensions.getStorageItems command envelope.
///
/// Pass a ExtensionsGetStorageItemsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_get_storage_items_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn extensions_get_storage_items_command(
  id~ : Int,
  params : ExtensionsGetStorageItemsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.getStorageItems",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsLoadUnpackedParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsLoadUnpackedParams {
  path : String
  enable_in_incognito : Bool?
} derive(Eq, Debug)

///|
/// Encode ExtensionsLoadUnpackedParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsLoadUnpackedParams::to_json(
  self : ExtensionsLoadUnpackedParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["path"] = Json::string(self.path)
  if self.enable_in_incognito is Some(value) {
    fields["enableInIncognito"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for ExtensionsLoadUnpackedResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsLoadUnpackedResult {
  id : String
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Extensions.loadUnpacked.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ExtensionsLoadUnpackedResult::from_json(
  result : Json,
) -> ExtensionsLoadUnpackedResult raise @protocol.ProtocolSchemaError {
  let method_name = "Extensions.loadUnpacked"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  { id: typed_required_result_string(method_name, fields, "id", "string") }
}

///|
/// Build a typed Extensions.loadUnpacked command envelope.
///
/// Pass a ExtensionsLoadUnpackedParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_load_unpacked_command(id=1, params, session_id=None)
/// ```
pub fn extensions_load_unpacked_command(
  id~ : Int,
  params : ExtensionsLoadUnpackedParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.loadUnpacked",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsRemoveStorageItemsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsRemoveStorageItemsParams {
  id : String
  storage_area : ExtensionsStorageArea
  keys : Array[String]
} derive(Eq, Debug)

///|
/// Encode ExtensionsRemoveStorageItemsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsRemoveStorageItemsParams::to_json(
  self : ExtensionsRemoveStorageItemsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["storageArea"] = self.storage_area.to_json()
  fields["keys"] = Json::array(self.keys.map(item => Json::string(item)))
  Json::object(fields)
}

///|
/// Build a typed Extensions.removeStorageItems command envelope.
///
/// Pass a ExtensionsRemoveStorageItemsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_remove_storage_items_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn extensions_remove_storage_items_command(
  id~ : Int,
  params : ExtensionsRemoveStorageItemsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.removeStorageItems",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsSetStorageItemsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsSetStorageItemsParams {
  id : String
  storage_area : ExtensionsStorageArea
  values : Json
} derive(Eq, Debug)

///|
/// Encode ExtensionsSetStorageItemsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsSetStorageItemsParams::to_json(
  self : ExtensionsSetStorageItemsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["storageArea"] = self.storage_area.to_json()
  fields["values"] = self.values
  Json::object(fields)
}

///|
/// Build a typed Extensions.setStorageItems command envelope.
///
/// Pass a ExtensionsSetStorageItemsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_set_storage_items_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn extensions_set_storage_items_command(
  id~ : Int,
  params : ExtensionsSetStorageItemsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.setStorageItems",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsTriggerActionParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsTriggerActionParams {
  id : String
  target_id : String
} derive(Eq, Debug)

///|
/// Encode ExtensionsTriggerActionParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsTriggerActionParams::to_json(
  self : ExtensionsTriggerActionParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["targetId"] = Json::string(self.target_id)
  Json::object(fields)
}

///|
/// Build a typed Extensions.triggerAction command envelope.
///
/// Pass a ExtensionsTriggerActionParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_trigger_action_command(id=1, params, session_id=None)
/// ```
pub fn extensions_trigger_action_command(
  id~ : Int,
  params : ExtensionsTriggerActionParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.triggerAction",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ExtensionsUninstallParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ExtensionsUninstallParams {
  id : String
} derive(Eq, Debug)

///|
/// Encode ExtensionsUninstallParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ExtensionsUninstallParams::to_json(
  self : ExtensionsUninstallParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  Json::object(fields)
}

///|
/// Build a typed Extensions.uninstall command envelope.
///
/// Pass a ExtensionsUninstallParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = extensions_uninstall_command(id=1, params, session_id=None)
/// ```
pub fn extensions_uninstall_command(
  id~ : Int,
  params : ExtensionsUninstallParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Extensions.uninstall",
    params=params.to_json(),
    session_id?,
  )
}
