///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for HeadlessExperimentalScreenshotParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct HeadlessExperimentalScreenshotParams {
  format : String?
  quality : Int?
  optimize_for_speed : Bool?
} derive(Eq, Debug)

///|
/// Encode HeadlessExperimentalScreenshotParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn HeadlessExperimentalScreenshotParams::to_json(
  self : HeadlessExperimentalScreenshotParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.format is Some(value) {
    fields["format"] = Json::string(value)
  }
  if self.quality is Some(value) {
    fields["quality"] = Json::number(value.to_double())
  }
  if self.optimize_for_speed is Some(value) {
    fields["optimizeForSpeed"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Decode HeadlessExperimentalScreenshotParams from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn HeadlessExperimentalScreenshotParams::from_json(
  value : Json,
) -> HeadlessExperimentalScreenshotParams raise @protocol.ProtocolSchemaError {
  let method_name = "HeadlessExperimentalScreenshotParams"
  let fields = typed_json_fields(method_name, value)
  {
    format: typed_optional_string(method_name, fields, "format", "string"),
    quality: typed_optional_int(method_name, fields, "quality", "integer"),
    optimize_for_speed: typed_optional_bool(
      method_name, fields, "optimizeForSpeed", "boolean",
    ),
  }
}

///|
/// Generated CDP value object for HeadlessExperimentalBeginFrameParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct HeadlessExperimentalBeginFrameParams {
  frame_time_ticks : Double?
  interval : Double?
  no_display_updates : Bool?
  screenshot : HeadlessExperimentalScreenshotParams?
} derive(Eq, Debug)

///|
/// Encode HeadlessExperimentalBeginFrameParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn HeadlessExperimentalBeginFrameParams::to_json(
  self : HeadlessExperimentalBeginFrameParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.frame_time_ticks is Some(value) {
    fields["frameTimeTicks"] = Json::number(value)
  }
  if self.interval is Some(value) {
    fields["interval"] = Json::number(value)
  }
  if self.no_display_updates is Some(value) {
    fields["noDisplayUpdates"] = Json::boolean(value)
  }
  if self.screenshot is Some(value) {
    fields["screenshot"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for HeadlessExperimentalBeginFrameResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct HeadlessExperimentalBeginFrameResult {
  has_damage : Bool
  screenshot_data : String?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for HeadlessExperimental.beginFrame.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn HeadlessExperimentalBeginFrameResult::from_json(
  result : Json,
) -> HeadlessExperimentalBeginFrameResult raise @protocol.ProtocolSchemaError {
  let method_name = "HeadlessExperimental.beginFrame"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    has_damage: typed_required_result_bool(
      method_name, fields, "hasDamage", "boolean",
    ),
    screenshot_data: typed_optional_result_string(
      method_name, fields, "screenshotData", "string",
    ),
  }
}

///|
/// Build a typed HeadlessExperimental.beginFrame command envelope.
///
/// Pass a HeadlessExperimentalBeginFrameParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = headless_experimental_begin_frame_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn headless_experimental_begin_frame_command(
  id~ : Int,
  params : HeadlessExperimentalBeginFrameParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="HeadlessExperimental.beginFrame",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed HeadlessExperimental.disable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = headless_experimental_disable_command(id=1, session_id=None)
/// ```
pub fn headless_experimental_disable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="HeadlessExperimental.disable",
    session_id?,
  )
}

///|
/// Build a typed HeadlessExperimental.enable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = headless_experimental_enable_command(id=1, session_id=None)
/// ```
pub fn headless_experimental_enable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="HeadlessExperimental.enable",
    session_id?,
  )
}
