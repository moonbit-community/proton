///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for IOStreamHandle.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOStreamHandle {
  value : String
} derive(Eq, Debug)

///|
/// Encode IOStreamHandle as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn IOStreamHandle::to_json(self : IOStreamHandle) -> Json {
  Json::string(self.value)
}

///|
/// Decode IOStreamHandle from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn IOStreamHandle::from_json(
  value : Json,
) -> IOStreamHandle raise @protocol.ProtocolSchemaError {
  let method_name = "IOStreamHandle"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for IOCloseParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOCloseParams {
  handle : IOStreamHandle
} derive(Eq, Debug)

///|
/// Encode IOCloseParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn IOCloseParams::to_json(self : IOCloseParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["handle"] = self.handle.to_json()
  Json::object(fields)
}

///|
/// Build a typed IO.close command envelope.
///
/// Pass a IOCloseParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = io_close_command(id=1, params, session_id=None)
/// ```
pub fn io_close_command(
  id~ : Int,
  params : IOCloseParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="IO.close",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for IOReadParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOReadParams {
  handle : IOStreamHandle
  offset : Int?
  size : Int?
} derive(Eq, Debug)

///|
/// Encode IOReadParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn IOReadParams::to_json(self : IOReadParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["handle"] = self.handle.to_json()
  if self.offset is Some(value) {
    fields["offset"] = Json::number(value.to_double())
  }
  if self.size is Some(value) {
    fields["size"] = Json::number(value.to_double())
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for IOReadResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOReadResult {
  base64_encoded : Bool?
  data : String
  eof : Bool
} derive(Eq, Debug)

///|
/// Decode and validate the result object for IO.read.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn IOReadResult::from_json(
  result : Json,
) -> IOReadResult raise @protocol.ProtocolSchemaError {
  let method_name = "IO.read"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    base64_encoded: typed_optional_result_bool(
      method_name, fields, "base64Encoded", "boolean",
    ),
    data: typed_required_result_string(method_name, fields, "data", "string"),
    eof: typed_required_result_bool(method_name, fields, "eof", "boolean"),
  }
}

///|
/// Build a typed IO.read command envelope.
///
/// Pass a IOReadParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = io_read_command(id=1, params, session_id=None)
/// ```
pub fn io_read_command(
  id~ : Int,
  params : IOReadParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="IO.read",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for IOResolveBlobParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOResolveBlobParams {
  object_id : RuntimeRemoteObjectId
} derive(Eq, Debug)

///|
/// Encode IOResolveBlobParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn IOResolveBlobParams::to_json(self : IOResolveBlobParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["objectId"] = self.object_id.to_json()
  Json::object(fields)
}

///|
/// Generated CDP value object for IOResolveBlobResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct IOResolveBlobResult {
  uuid : String
} derive(Eq, Debug)

///|
/// Decode and validate the result object for IO.resolveBlob.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn IOResolveBlobResult::from_json(
  result : Json,
) -> IOResolveBlobResult raise @protocol.ProtocolSchemaError {
  let method_name = "IO.resolveBlob"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  { uuid: typed_required_result_string(method_name, fields, "uuid", "string") }
}

///|
/// Build a typed IO.resolveBlob command envelope.
///
/// Pass a IOResolveBlobParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = io_resolve_blob_command(id=1, params, session_id=None)
/// ```
pub fn io_resolve_blob_command(
  id~ : Int,
  params : IOResolveBlobParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="IO.resolveBlob",
    params=params.to_json(),
    session_id?,
  )
}
