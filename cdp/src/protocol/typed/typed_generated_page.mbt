///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for PageAdFrameExplanation.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAdFrameExplanation {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageAdFrameExplanation as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAdFrameExplanation::to_json(self : PageAdFrameExplanation) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageAdFrameExplanation from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAdFrameExplanation::from_json(
  value : Json,
) -> PageAdFrameExplanation raise @protocol.ProtocolSchemaError {
  let method_name = "PageAdFrameExplanation"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageAdFrameStatus.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAdFrameStatus {
  ad_frame_type : PageAdFrameType
  explanations : Array[PageAdFrameExplanation]?
} derive(Eq, Debug)

///|
/// Encode PageAdFrameStatus as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAdFrameStatus::to_json(self : PageAdFrameStatus) -> Json {
  let fields : Map[String, Json] = {}
  fields["adFrameType"] = self.ad_frame_type.to_json()
  if self.explanations is Some(value) {
    fields["explanations"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Decode PageAdFrameStatus from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAdFrameStatus::from_json(
  value : Json,
) -> PageAdFrameStatus raise @protocol.ProtocolSchemaError {
  let method_name = "PageAdFrameStatus"
  let fields = typed_json_fields(method_name, value)
  {
    ad_frame_type: PageAdFrameType::from_json(
      typed_required_json(method_name, fields, "adFrameType"),
    ),
    explanations: match typed_optional_json(fields, "explanations") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "explanations", "array<AdFrameExplanation>",
          ).map(item => PageAdFrameExplanation::from_json(item)),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageAdFrameType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAdFrameType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageAdFrameType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAdFrameType::to_json(self : PageAdFrameType) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageAdFrameType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAdFrameType::from_json(
  value : Json,
) -> PageAdFrameType raise @protocol.ProtocolSchemaError {
  let method_name = "PageAdFrameType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageAdScriptAncestry.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAdScriptAncestry {
  ancestry_chain : Array[PageAdScriptId]
  root_script_filterlist_rule : String?
} derive(Eq, Debug)

///|
/// Encode PageAdScriptAncestry as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAdScriptAncestry::to_json(self : PageAdScriptAncestry) -> Json {
  let fields : Map[String, Json] = {}
  fields["ancestryChain"] = Json::array(
    self.ancestry_chain.map(item => item.to_json()),
  )
  if self.root_script_filterlist_rule is Some(value) {
    fields["rootScriptFilterlistRule"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode PageAdScriptAncestry from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAdScriptAncestry::from_json(
  value : Json,
) -> PageAdScriptAncestry raise @protocol.ProtocolSchemaError {
  let method_name = "PageAdScriptAncestry"
  let fields = typed_json_fields(method_name, value)
  {
    ancestry_chain: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "ancestryChain"),
      "ancestryChain",
      "array<AdScriptId>",
    ).map(item => PageAdScriptId::from_json(item)),
    root_script_filterlist_rule: typed_optional_string(
      method_name, fields, "rootScriptFilterlistRule", "string",
    ),
  }
}

///|
/// Generated CDP value object for PageAdScriptId.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAdScriptId {
  script_id : RuntimeScriptId
  debugger_id : RuntimeUniqueDebuggerId
} derive(Eq, Debug)

///|
/// Encode PageAdScriptId as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAdScriptId::to_json(self : PageAdScriptId) -> Json {
  let fields : Map[String, Json] = {}
  fields["scriptId"] = self.script_id.to_json()
  fields["debuggerId"] = self.debugger_id.to_json()
  Json::object(fields)
}

///|
/// Decode PageAdScriptId from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAdScriptId::from_json(
  value : Json,
) -> PageAdScriptId raise @protocol.ProtocolSchemaError {
  let method_name = "PageAdScriptId"
  let fields = typed_json_fields(method_name, value)
  {
    script_id: RuntimeScriptId::from_json(
      typed_required_json(method_name, fields, "scriptId"),
    ),
    debugger_id: RuntimeUniqueDebuggerId::from_json(
      typed_required_json(method_name, fields, "debuggerId"),
    ),
  }
}

///|
/// Generated CDP value object for PageAppManifestError.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAppManifestError {
  message : String
  critical : Int
  line : Int
  column : Int
} derive(Eq, Debug)

///|
/// Encode PageAppManifestError as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAppManifestError::to_json(self : PageAppManifestError) -> Json {
  let fields : Map[String, Json] = {}
  fields["message"] = Json::string(self.message)
  fields["critical"] = Json::number(self.critical.to_double())
  fields["line"] = Json::number(self.line.to_double())
  fields["column"] = Json::number(self.column.to_double())
  Json::object(fields)
}

///|
/// Decode PageAppManifestError from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAppManifestError::from_json(
  value : Json,
) -> PageAppManifestError raise @protocol.ProtocolSchemaError {
  let method_name = "PageAppManifestError"
  let fields = typed_json_fields(method_name, value)
  {
    message: typed_required_string(method_name, fields, "message", "string"),
    critical: typed_required_int(method_name, fields, "critical", "integer"),
    line: typed_required_int(method_name, fields, "line", "integer"),
    column: typed_required_int(method_name, fields, "column", "integer"),
  }
}

///|
/// Generated CDP value object for PageAppManifestParsedProperties.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAppManifestParsedProperties {
  scope : String
} derive(Eq, Debug)

///|
/// Encode PageAppManifestParsedProperties as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAppManifestParsedProperties::to_json(
  self : PageAppManifestParsedProperties,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["scope"] = Json::string(self.scope)
  Json::object(fields)
}

///|
/// Decode PageAppManifestParsedProperties from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageAppManifestParsedProperties::from_json(
  value : Json,
) -> PageAppManifestParsedProperties raise @protocol.ProtocolSchemaError {
  let method_name = "PageAppManifestParsedProperties"
  let fields = typed_json_fields(method_name, value)
  { scope: typed_required_string(method_name, fields, "scope", "string") }
}

///|
/// Generated CDP value object for PageBackForwardCacheBlockingDetails.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheBlockingDetails {
  url : String?
  function : String?
  line_number : Int
  column_number : Int
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheBlockingDetails as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheBlockingDetails::to_json(
  self : PageBackForwardCacheBlockingDetails,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.url is Some(value) {
    fields["url"] = Json::string(value)
  }
  if self.function is Some(value) {
    fields["function"] = Json::string(value)
  }
  fields["lineNumber"] = Json::number(self.line_number.to_double())
  fields["columnNumber"] = Json::number(self.column_number.to_double())
  Json::object(fields)
}

///|
/// Decode PageBackForwardCacheBlockingDetails from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageBackForwardCacheBlockingDetails::from_json(
  value : Json,
) -> PageBackForwardCacheBlockingDetails raise @protocol.ProtocolSchemaError {
  let method_name = "PageBackForwardCacheBlockingDetails"
  let fields = typed_json_fields(method_name, value)
  {
    url: typed_optional_string(method_name, fields, "url", "string"),
    function: typed_optional_string(method_name, fields, "function", "string"),
    line_number: typed_required_int(
      method_name, fields, "lineNumber", "integer",
    ),
    column_number: typed_required_int(
      method_name, fields, "columnNumber", "integer",
    ),
  }
}

///|
/// Generated CDP value object for PageBackForwardCacheNotRestoredExplanation.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheNotRestoredExplanation {
  type_ : PageBackForwardCacheNotRestoredReasonType
  reason : PageBackForwardCacheNotRestoredReason
  context : String?
  details : Array[PageBackForwardCacheBlockingDetails]?
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheNotRestoredExplanation as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheNotRestoredExplanation::to_json(
  self : PageBackForwardCacheNotRestoredExplanation,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["type"] = self.type_.to_json()
  fields["reason"] = self.reason.to_json()
  if self.context is Some(value) {
    fields["context"] = Json::string(value)
  }
  if self.details is Some(value) {
    fields["details"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Decode PageBackForwardCacheNotRestoredExplanation from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageBackForwardCacheNotRestoredExplanation::from_json(
  value : Json,
) -> PageBackForwardCacheNotRestoredExplanation raise @protocol.ProtocolSchemaError {
  let method_name = "PageBackForwardCacheNotRestoredExplanation"
  let fields = typed_json_fields(method_name, value)
  {
    type_: PageBackForwardCacheNotRestoredReasonType::from_json(
      typed_required_json(method_name, fields, "type"),
    ),
    reason: PageBackForwardCacheNotRestoredReason::from_json(
      typed_required_json(method_name, fields, "reason"),
    ),
    context: typed_optional_string(method_name, fields, "context", "string"),
    details: match typed_optional_json(fields, "details") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "details", "array<BackForwardCacheBlockingDetails>",
          ).map(item => PageBackForwardCacheBlockingDetails::from_json(item)),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageBackForwardCacheNotRestoredExplanationTree.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheNotRestoredExplanationTree {
  url : String
  explanations : Array[PageBackForwardCacheNotRestoredExplanation]
  children : Array[PageBackForwardCacheNotRestoredExplanationTree]
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheNotRestoredExplanationTree as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheNotRestoredExplanationTree::to_json(
  self : PageBackForwardCacheNotRestoredExplanationTree,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["explanations"] = Json::array(
    self.explanations.map(item => item.to_json()),
  )
  fields["children"] = Json::array(self.children.map(item => item.to_json()))
  Json::object(fields)
}

///|
/// Decode PageBackForwardCacheNotRestoredExplanationTree from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageBackForwardCacheNotRestoredExplanationTree::from_json(
  value : Json,
) -> PageBackForwardCacheNotRestoredExplanationTree raise @protocol.ProtocolSchemaError {
  let method_name = "PageBackForwardCacheNotRestoredExplanationTree"
  let fields = typed_json_fields(method_name, value)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    explanations: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "explanations"),
      "explanations",
      "array<BackForwardCacheNotRestoredExplanation>",
    ).map(item => PageBackForwardCacheNotRestoredExplanation::from_json(item)),
    children: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "children"),
      "children",
      "array<BackForwardCacheNotRestoredExplanationTree>",
    ).map(item => {
      PageBackForwardCacheNotRestoredExplanationTree::from_json(item)
    }),
  }
}

///|
/// Generated CDP value object for PageBackForwardCacheNotRestoredReason.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheNotRestoredReason {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheNotRestoredReason as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheNotRestoredReason::to_json(
  self : PageBackForwardCacheNotRestoredReason,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageBackForwardCacheNotRestoredReason from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageBackForwardCacheNotRestoredReason::from_json(
  value : Json,
) -> PageBackForwardCacheNotRestoredReason raise @protocol.ProtocolSchemaError {
  let method_name = "PageBackForwardCacheNotRestoredReason"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageBackForwardCacheNotRestoredReasonType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheNotRestoredReasonType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheNotRestoredReasonType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheNotRestoredReasonType::to_json(
  self : PageBackForwardCacheNotRestoredReasonType,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageBackForwardCacheNotRestoredReasonType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageBackForwardCacheNotRestoredReasonType::from_json(
  value : Json,
) -> PageBackForwardCacheNotRestoredReasonType raise @protocol.ProtocolSchemaError {
  let method_name = "PageBackForwardCacheNotRestoredReasonType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageClientNavigationDisposition.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageClientNavigationDisposition {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageClientNavigationDisposition as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageClientNavigationDisposition::to_json(
  self : PageClientNavigationDisposition,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageClientNavigationDisposition from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageClientNavigationDisposition::from_json(
  value : Json,
) -> PageClientNavigationDisposition raise @protocol.ProtocolSchemaError {
  let method_name = "PageClientNavigationDisposition"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageClientNavigationReason.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageClientNavigationReason {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageClientNavigationReason as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageClientNavigationReason::to_json(
  self : PageClientNavigationReason,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageClientNavigationReason from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageClientNavigationReason::from_json(
  value : Json,
) -> PageClientNavigationReason raise @protocol.ProtocolSchemaError {
  let method_name = "PageClientNavigationReason"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageCompilationCacheParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCompilationCacheParams {
  url : String
  eager : Bool?
} derive(Eq, Debug)

///|
/// Encode PageCompilationCacheParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCompilationCacheParams::to_json(
  self : PageCompilationCacheParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  if self.eager is Some(value) {
    fields["eager"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Decode PageCompilationCacheParams from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageCompilationCacheParams::from_json(
  value : Json,
) -> PageCompilationCacheParams raise @protocol.ProtocolSchemaError {
  let method_name = "PageCompilationCacheParams"
  let fields = typed_json_fields(method_name, value)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    eager: typed_optional_bool(method_name, fields, "eager", "boolean"),
  }
}

///|
/// Generated CDP value object for PageCrossOriginIsolatedContextType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCrossOriginIsolatedContextType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageCrossOriginIsolatedContextType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCrossOriginIsolatedContextType::to_json(
  self : PageCrossOriginIsolatedContextType,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageCrossOriginIsolatedContextType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageCrossOriginIsolatedContextType::from_json(
  value : Json,
) -> PageCrossOriginIsolatedContextType raise @protocol.ProtocolSchemaError {
  let method_name = "PageCrossOriginIsolatedContextType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageDialogType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDialogType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageDialogType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDialogType::to_json(self : PageDialogType) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageDialogType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageDialogType::from_json(
  value : Json,
) -> PageDialogType raise @protocol.ProtocolSchemaError {
  let method_name = "PageDialogType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageFileFilter.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFileFilter {
  name : String?
  accepts : Array[String]?
} derive(Eq, Debug)

///|
/// Encode PageFileFilter as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFileFilter::to_json(self : PageFileFilter) -> Json {
  let fields : Map[String, Json] = {}
  if self.name is Some(value) {
    fields["name"] = Json::string(value)
  }
  if self.accepts is Some(value) {
    fields["accepts"] = Json::array(value.map(item => Json::string(item)))
  }
  Json::object(fields)
}

///|
/// Decode PageFileFilter from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFileFilter::from_json(
  value : Json,
) -> PageFileFilter raise @protocol.ProtocolSchemaError {
  let method_name = "PageFileFilter"
  let fields = typed_json_fields(method_name, value)
  {
    name: typed_optional_string(method_name, fields, "name", "string"),
    accepts: match typed_optional_json(fields, "accepts") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "accepts", "array<string>").map(item => {
              typed_array_string(method_name, "accepts", "array<string>", item)
            },
          ),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageFileHandler.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFileHandler {
  action : String
  name : String
  icons : Array[PageImageResource]?
  accepts : Array[PageFileFilter]?
  launch_type : String
} derive(Eq, Debug)

///|
/// Encode PageFileHandler as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFileHandler::to_json(self : PageFileHandler) -> Json {
  let fields : Map[String, Json] = {}
  fields["action"] = Json::string(self.action)
  fields["name"] = Json::string(self.name)
  if self.icons is Some(value) {
    fields["icons"] = Json::array(value.map(item => item.to_json()))
  }
  if self.accepts is Some(value) {
    fields["accepts"] = Json::array(value.map(item => item.to_json()))
  }
  fields["launchType"] = Json::string(self.launch_type)
  Json::object(fields)
}

///|
/// Decode PageFileHandler from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFileHandler::from_json(
  value : Json,
) -> PageFileHandler raise @protocol.ProtocolSchemaError {
  let method_name = "PageFileHandler"
  let fields = typed_json_fields(method_name, value)
  {
    action: typed_required_string(method_name, fields, "action", "string"),
    name: typed_required_string(method_name, fields, "name", "string"),
    icons: match typed_optional_json(fields, "icons") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "icons", "array<ImageResource>").map(item => {
              PageImageResource::from_json(item)
            },
          ),
        )
      None => None
    },
    accepts: match typed_optional_json(fields, "accepts") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "accepts", "array<FileFilter>").map(item => {
              PageFileFilter::from_json(item)
            },
          ),
        )
      None => None
    },
    launch_type: typed_required_string(
      method_name, fields, "launchType", "string",
    ),
  }
}

///|
/// Generated CDP value object for PageFontFamilies.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFontFamilies {
  standard : String?
  fixed : String?
  serif : String?
  sans_serif : String?
  cursive : String?
  fantasy : String?
  math : String?
} derive(Eq, Debug)

///|
/// Encode PageFontFamilies as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFontFamilies::to_json(self : PageFontFamilies) -> Json {
  let fields : Map[String, Json] = {}
  if self.standard is Some(value) {
    fields["standard"] = Json::string(value)
  }
  if self.fixed is Some(value) {
    fields["fixed"] = Json::string(value)
  }
  if self.serif is Some(value) {
    fields["serif"] = Json::string(value)
  }
  if self.sans_serif is Some(value) {
    fields["sansSerif"] = Json::string(value)
  }
  if self.cursive is Some(value) {
    fields["cursive"] = Json::string(value)
  }
  if self.fantasy is Some(value) {
    fields["fantasy"] = Json::string(value)
  }
  if self.math is Some(value) {
    fields["math"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode PageFontFamilies from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFontFamilies::from_json(
  value : Json,
) -> PageFontFamilies raise @protocol.ProtocolSchemaError {
  let method_name = "PageFontFamilies"
  let fields = typed_json_fields(method_name, value)
  {
    standard: typed_optional_string(method_name, fields, "standard", "string"),
    fixed: typed_optional_string(method_name, fields, "fixed", "string"),
    serif: typed_optional_string(method_name, fields, "serif", "string"),
    sans_serif: typed_optional_string(
      method_name, fields, "sansSerif", "string",
    ),
    cursive: typed_optional_string(method_name, fields, "cursive", "string"),
    fantasy: typed_optional_string(method_name, fields, "fantasy", "string"),
    math: typed_optional_string(method_name, fields, "math", "string"),
  }
}

///|
/// Generated CDP value object for PageFontSizes.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFontSizes {
  standard : Int?
  fixed : Int?
} derive(Eq, Debug)

///|
/// Encode PageFontSizes as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFontSizes::to_json(self : PageFontSizes) -> Json {
  let fields : Map[String, Json] = {}
  if self.standard is Some(value) {
    fields["standard"] = Json::number(value.to_double())
  }
  if self.fixed is Some(value) {
    fields["fixed"] = Json::number(value.to_double())
  }
  Json::object(fields)
}

///|
/// Decode PageFontSizes from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFontSizes::from_json(
  value : Json,
) -> PageFontSizes raise @protocol.ProtocolSchemaError {
  let method_name = "PageFontSizes"
  let fields = typed_json_fields(method_name, value)
  {
    standard: typed_optional_int(method_name, fields, "standard", "integer"),
    fixed: typed_optional_int(method_name, fields, "fixed", "integer"),
  }
}

///|
/// Generated CDP value object for PageFrame.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrame {
  id : PageFrameId
  parent_id : PageFrameId?
  loader_id : NetworkLoaderId
  name : String?
  url : String
  url_fragment : String?
  domain_and_registry : String
  security_origin : String
  security_origin_details : PageSecurityOriginDetails?
  mime_type : String
  unreachable_url : String?
  ad_frame_status : PageAdFrameStatus?
  secure_context_type : PageSecureContextType
  cross_origin_isolated_context_type : PageCrossOriginIsolatedContextType
  gated_apifeatures : Array[PageGatedAPIFeatures]
} derive(Eq, Debug)

///|
/// Encode PageFrame as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrame::to_json(self : PageFrame) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = self.id.to_json()
  if self.parent_id is Some(value) {
    fields["parentId"] = value.to_json()
  }
  fields["loaderId"] = self.loader_id.to_json()
  if self.name is Some(value) {
    fields["name"] = Json::string(value)
  }
  fields["url"] = Json::string(self.url)
  if self.url_fragment is Some(value) {
    fields["urlFragment"] = Json::string(value)
  }
  fields["domainAndRegistry"] = Json::string(self.domain_and_registry)
  fields["securityOrigin"] = Json::string(self.security_origin)
  if self.security_origin_details is Some(value) {
    fields["securityOriginDetails"] = value.to_json()
  }
  fields["mimeType"] = Json::string(self.mime_type)
  if self.unreachable_url is Some(value) {
    fields["unreachableUrl"] = Json::string(value)
  }
  if self.ad_frame_status is Some(value) {
    fields["adFrameStatus"] = value.to_json()
  }
  fields["secureContextType"] = self.secure_context_type.to_json()
  fields["crossOriginIsolatedContextType"] = self.cross_origin_isolated_context_type.to_json()
  fields["gatedAPIFeatures"] = Json::array(
    self.gated_apifeatures.map(item => item.to_json()),
  )
  Json::object(fields)
}

///|
/// Decode PageFrame from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFrame::from_json(
  value : Json,
) -> PageFrame raise @protocol.ProtocolSchemaError {
  let method_name = "PageFrame"
  let fields = typed_json_fields(method_name, value)
  {
    id: PageFrameId::from_json(typed_required_json(method_name, fields, "id")),
    parent_id: match typed_optional_json(fields, "parentId") {
      Some(value) => Some(PageFrameId::from_json(value))
      None => None
    },
    loader_id: NetworkLoaderId::from_json(
      typed_required_json(method_name, fields, "loaderId"),
    ),
    name: typed_optional_string(method_name, fields, "name", "string"),
    url: typed_required_string(method_name, fields, "url", "string"),
    url_fragment: typed_optional_string(
      method_name, fields, "urlFragment", "string",
    ),
    domain_and_registry: typed_required_string(
      method_name, fields, "domainAndRegistry", "string",
    ),
    security_origin: typed_required_string(
      method_name, fields, "securityOrigin", "string",
    ),
    security_origin_details: match
      typed_optional_json(fields, "securityOriginDetails") {
      Some(value) => Some(PageSecurityOriginDetails::from_json(value))
      None => None
    },
    mime_type: typed_required_string(method_name, fields, "mimeType", "string"),
    unreachable_url: typed_optional_string(
      method_name, fields, "unreachableUrl", "string",
    ),
    ad_frame_status: match typed_optional_json(fields, "adFrameStatus") {
      Some(value) => Some(PageAdFrameStatus::from_json(value))
      None => None
    },
    secure_context_type: PageSecureContextType::from_json(
      typed_required_json(method_name, fields, "secureContextType"),
    ),
    cross_origin_isolated_context_type: PageCrossOriginIsolatedContextType::from_json(
      typed_required_json(method_name, fields, "crossOriginIsolatedContextType"),
    ),
    gated_apifeatures: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "gatedAPIFeatures"),
      "gatedAPIFeatures",
      "array<GatedAPIFeatures>",
    ).map(item => PageGatedAPIFeatures::from_json(item)),
  }
}

///|
/// Generated CDP value object for PageFrameId.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameId {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageFrameId as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameId::to_json(self : PageFrameId) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageFrameId from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFrameId::from_json(
  value : Json,
) -> PageFrameId raise @protocol.ProtocolSchemaError {
  let method_name = "PageFrameId"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageFrameResource.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameResource {
  url : String
  type_ : NetworkResourceType
  mime_type : String
  last_modified : NetworkTimeSinceEpoch?
  content_size : Double?
  failed : Bool?
  canceled : Bool?
} derive(Eq, Debug)

///|
/// Encode PageFrameResource as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameResource::to_json(self : PageFrameResource) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["type"] = self.type_.to_json()
  fields["mimeType"] = Json::string(self.mime_type)
  if self.last_modified is Some(value) {
    fields["lastModified"] = value.to_json()
  }
  if self.content_size is Some(value) {
    fields["contentSize"] = Json::number(value)
  }
  if self.failed is Some(value) {
    fields["failed"] = Json::boolean(value)
  }
  if self.canceled is Some(value) {
    fields["canceled"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Decode PageFrameResource from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFrameResource::from_json(
  value : Json,
) -> PageFrameResource raise @protocol.ProtocolSchemaError {
  let method_name = "PageFrameResource"
  let fields = typed_json_fields(method_name, value)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    type_: NetworkResourceType::from_json(
      typed_required_json(method_name, fields, "type"),
    ),
    mime_type: typed_required_string(method_name, fields, "mimeType", "string"),
    last_modified: match typed_optional_json(fields, "lastModified") {
      Some(value) => Some(NetworkTimeSinceEpoch::from_json(value))
      None => None
    },
    content_size: typed_optional_double(
      method_name, fields, "contentSize", "number",
    ),
    failed: typed_optional_bool(method_name, fields, "failed", "boolean"),
    canceled: typed_optional_bool(method_name, fields, "canceled", "boolean"),
  }
}

///|
/// Generated CDP value object for PageFrameResourceTree.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameResourceTree {
  frame : PageFrame
  child_frames : Array[PageFrameResourceTree]?
  resources : Array[PageFrameResource]
} derive(Eq, Debug)

///|
/// Encode PageFrameResourceTree as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameResourceTree::to_json(self : PageFrameResourceTree) -> Json {
  let fields : Map[String, Json] = {}
  fields["frame"] = self.frame.to_json()
  if self.child_frames is Some(value) {
    fields["childFrames"] = Json::array(value.map(item => item.to_json()))
  }
  fields["resources"] = Json::array(self.resources.map(item => item.to_json()))
  Json::object(fields)
}

///|
/// Decode PageFrameResourceTree from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFrameResourceTree::from_json(
  value : Json,
) -> PageFrameResourceTree raise @protocol.ProtocolSchemaError {
  let method_name = "PageFrameResourceTree"
  let fields = typed_json_fields(method_name, value)
  {
    frame: PageFrame::from_json(
      typed_required_json(method_name, fields, "frame"),
    ),
    child_frames: match typed_optional_json(fields, "childFrames") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "childFrames", "array<FrameResourceTree>",
          ).map(item => PageFrameResourceTree::from_json(item)),
        )
      None => None
    },
    resources: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "resources"),
      "resources",
      "array<FrameResource>",
    ).map(item => PageFrameResource::from_json(item)),
  }
}

///|
/// Generated CDP value object for PageFrameTree.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameTree {
  frame : PageFrame
  child_frames : Array[PageFrameTree]?
} derive(Eq, Debug)

///|
/// Encode PageFrameTree as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameTree::to_json(self : PageFrameTree) -> Json {
  let fields : Map[String, Json] = {}
  fields["frame"] = self.frame.to_json()
  if self.child_frames is Some(value) {
    fields["childFrames"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Decode PageFrameTree from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageFrameTree::from_json(
  value : Json,
) -> PageFrameTree raise @protocol.ProtocolSchemaError {
  let method_name = "PageFrameTree"
  let fields = typed_json_fields(method_name, value)
  {
    frame: PageFrame::from_json(
      typed_required_json(method_name, fields, "frame"),
    ),
    child_frames: match typed_optional_json(fields, "childFrames") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "childFrames", "array<FrameTree>",
          ).map(item => PageFrameTree::from_json(item)),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageGatedAPIFeatures.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGatedAPIFeatures {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageGatedAPIFeatures as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGatedAPIFeatures::to_json(self : PageGatedAPIFeatures) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageGatedAPIFeatures from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageGatedAPIFeatures::from_json(
  value : Json,
) -> PageGatedAPIFeatures raise @protocol.ProtocolSchemaError {
  let method_name = "PageGatedAPIFeatures"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageImageResource.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageImageResource {
  url : String
  sizes : String?
  type_ : String?
} derive(Eq, Debug)

///|
/// Encode PageImageResource as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageImageResource::to_json(self : PageImageResource) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  if self.sizes is Some(value) {
    fields["sizes"] = Json::string(value)
  }
  if self.type_ is Some(value) {
    fields["type"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode PageImageResource from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageImageResource::from_json(
  value : Json,
) -> PageImageResource raise @protocol.ProtocolSchemaError {
  let method_name = "PageImageResource"
  let fields = typed_json_fields(method_name, value)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    sizes: typed_optional_string(method_name, fields, "sizes", "string"),
    type_: typed_optional_string(method_name, fields, "type", "string"),
  }
}

///|
/// Generated CDP value object for PageInstallabilityError.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageInstallabilityError {
  error_id : String
  error_arguments : Array[PageInstallabilityErrorArgument]
} derive(Eq, Debug)

///|
/// Encode PageInstallabilityError as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageInstallabilityError::to_json(self : PageInstallabilityError) -> Json {
  let fields : Map[String, Json] = {}
  fields["errorId"] = Json::string(self.error_id)
  fields["errorArguments"] = Json::array(
    self.error_arguments.map(item => item.to_json()),
  )
  Json::object(fields)
}

///|
/// Decode PageInstallabilityError from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageInstallabilityError::from_json(
  value : Json,
) -> PageInstallabilityError raise @protocol.ProtocolSchemaError {
  let method_name = "PageInstallabilityError"
  let fields = typed_json_fields(method_name, value)
  {
    error_id: typed_required_string(method_name, fields, "errorId", "string"),
    error_arguments: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "errorArguments"),
      "errorArguments",
      "array<InstallabilityErrorArgument>",
    ).map(item => PageInstallabilityErrorArgument::from_json(item)),
  }
}

///|
/// Generated CDP value object for PageInstallabilityErrorArgument.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageInstallabilityErrorArgument {
  name : String
  value : String
} derive(Eq, Debug)

///|
/// Encode PageInstallabilityErrorArgument as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageInstallabilityErrorArgument::to_json(
  self : PageInstallabilityErrorArgument,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["name"] = Json::string(self.name)
  fields["value"] = Json::string(self.value)
  Json::object(fields)
}

///|
/// Decode PageInstallabilityErrorArgument from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageInstallabilityErrorArgument::from_json(
  value : Json,
) -> PageInstallabilityErrorArgument raise @protocol.ProtocolSchemaError {
  let method_name = "PageInstallabilityErrorArgument"
  let fields = typed_json_fields(method_name, value)
  {
    name: typed_required_string(method_name, fields, "name", "string"),
    value: typed_required_string(method_name, fields, "value", "string"),
  }
}

///|
/// Generated CDP value object for PageLaunchHandler.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageLaunchHandler {
  client_mode : String
} derive(Eq, Debug)

///|
/// Encode PageLaunchHandler as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageLaunchHandler::to_json(self : PageLaunchHandler) -> Json {
  let fields : Map[String, Json] = {}
  fields["clientMode"] = Json::string(self.client_mode)
  Json::object(fields)
}

///|
/// Decode PageLaunchHandler from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageLaunchHandler::from_json(
  value : Json,
) -> PageLaunchHandler raise @protocol.ProtocolSchemaError {
  let method_name = "PageLaunchHandler"
  let fields = typed_json_fields(method_name, value)
  {
    client_mode: typed_required_string(
      method_name, fields, "clientMode", "string",
    ),
  }
}

///|
/// Generated CDP value object for PageLayoutViewport.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageLayoutViewport {
  page_x : Int
  page_y : Int
  client_width : Int
  client_height : Int
} derive(Eq, Debug)

///|
/// Encode PageLayoutViewport as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageLayoutViewport::to_json(self : PageLayoutViewport) -> Json {
  let fields : Map[String, Json] = {}
  fields["pageX"] = Json::number(self.page_x.to_double())
  fields["pageY"] = Json::number(self.page_y.to_double())
  fields["clientWidth"] = Json::number(self.client_width.to_double())
  fields["clientHeight"] = Json::number(self.client_height.to_double())
  Json::object(fields)
}

///|
/// Decode PageLayoutViewport from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageLayoutViewport::from_json(
  value : Json,
) -> PageLayoutViewport raise @protocol.ProtocolSchemaError {
  let method_name = "PageLayoutViewport"
  let fields = typed_json_fields(method_name, value)
  {
    page_x: typed_required_int(method_name, fields, "pageX", "integer"),
    page_y: typed_required_int(method_name, fields, "pageY", "integer"),
    client_width: typed_required_int(
      method_name, fields, "clientWidth", "integer",
    ),
    client_height: typed_required_int(
      method_name, fields, "clientHeight", "integer",
    ),
  }
}

///|
/// Generated CDP value object for PageNavigationEntry.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigationEntry {
  id : Int
  url : String
  user_typed_url : String
  title : String
  transition_type : PageTransitionType
} derive(Eq, Debug)

///|
/// Encode PageNavigationEntry as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageNavigationEntry::to_json(self : PageNavigationEntry) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::number(self.id.to_double())
  fields["url"] = Json::string(self.url)
  fields["userTypedURL"] = Json::string(self.user_typed_url)
  fields["title"] = Json::string(self.title)
  fields["transitionType"] = self.transition_type.to_json()
  Json::object(fields)
}

///|
/// Decode PageNavigationEntry from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageNavigationEntry::from_json(
  value : Json,
) -> PageNavigationEntry raise @protocol.ProtocolSchemaError {
  let method_name = "PageNavigationEntry"
  let fields = typed_json_fields(method_name, value)
  {
    id: typed_required_int(method_name, fields, "id", "integer"),
    url: typed_required_string(method_name, fields, "url", "string"),
    user_typed_url: typed_required_string(
      method_name, fields, "userTypedURL", "string",
    ),
    title: typed_required_string(method_name, fields, "title", "string"),
    transition_type: PageTransitionType::from_json(
      typed_required_json(method_name, fields, "transitionType"),
    ),
  }
}

///|
/// Generated CDP value object for PageNavigationType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigationType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageNavigationType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageNavigationType::to_json(self : PageNavigationType) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageNavigationType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageNavigationType::from_json(
  value : Json,
) -> PageNavigationType raise @protocol.ProtocolSchemaError {
  let method_name = "PageNavigationType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageOriginTrial.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrial {
  trial_name : String
  status : PageOriginTrialStatus
  tokens_with_status : Array[PageOriginTrialTokenWithStatus]
} derive(Eq, Debug)

///|
/// Encode PageOriginTrial as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrial::to_json(self : PageOriginTrial) -> Json {
  let fields : Map[String, Json] = {}
  fields["trialName"] = Json::string(self.trial_name)
  fields["status"] = self.status.to_json()
  fields["tokensWithStatus"] = Json::array(
    self.tokens_with_status.map(item => item.to_json()),
  )
  Json::object(fields)
}

///|
/// Decode PageOriginTrial from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrial::from_json(
  value : Json,
) -> PageOriginTrial raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrial"
  let fields = typed_json_fields(method_name, value)
  {
    trial_name: typed_required_string(
      method_name, fields, "trialName", "string",
    ),
    status: PageOriginTrialStatus::from_json(
      typed_required_json(method_name, fields, "status"),
    ),
    tokens_with_status: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "tokensWithStatus"),
      "tokensWithStatus",
      "array<OriginTrialTokenWithStatus>",
    ).map(item => PageOriginTrialTokenWithStatus::from_json(item)),
  }
}

///|
/// Generated CDP value object for PageOriginTrialStatus.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrialStatus {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageOriginTrialStatus as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrialStatus::to_json(self : PageOriginTrialStatus) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageOriginTrialStatus from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrialStatus::from_json(
  value : Json,
) -> PageOriginTrialStatus raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrialStatus"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageOriginTrialToken.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrialToken {
  origin : String
  match_sub_domains : Bool
  trial_name : String
  expiry_time : NetworkTimeSinceEpoch
  is_third_party : Bool
  usage_restriction : PageOriginTrialUsageRestriction
} derive(Eq, Debug)

///|
/// Encode PageOriginTrialToken as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrialToken::to_json(self : PageOriginTrialToken) -> Json {
  let fields : Map[String, Json] = {}
  fields["origin"] = Json::string(self.origin)
  fields["matchSubDomains"] = Json::boolean(self.match_sub_domains)
  fields["trialName"] = Json::string(self.trial_name)
  fields["expiryTime"] = self.expiry_time.to_json()
  fields["isThirdParty"] = Json::boolean(self.is_third_party)
  fields["usageRestriction"] = self.usage_restriction.to_json()
  Json::object(fields)
}

///|
/// Decode PageOriginTrialToken from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrialToken::from_json(
  value : Json,
) -> PageOriginTrialToken raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrialToken"
  let fields = typed_json_fields(method_name, value)
  {
    origin: typed_required_string(method_name, fields, "origin", "string"),
    match_sub_domains: typed_required_bool(
      method_name, fields, "matchSubDomains", "boolean",
    ),
    trial_name: typed_required_string(
      method_name, fields, "trialName", "string",
    ),
    expiry_time: NetworkTimeSinceEpoch::from_json(
      typed_required_json(method_name, fields, "expiryTime"),
    ),
    is_third_party: typed_required_bool(
      method_name, fields, "isThirdParty", "boolean",
    ),
    usage_restriction: PageOriginTrialUsageRestriction::from_json(
      typed_required_json(method_name, fields, "usageRestriction"),
    ),
  }
}

///|
/// Generated CDP value object for PageOriginTrialTokenStatus.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrialTokenStatus {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageOriginTrialTokenStatus as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrialTokenStatus::to_json(
  self : PageOriginTrialTokenStatus,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageOriginTrialTokenStatus from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrialTokenStatus::from_json(
  value : Json,
) -> PageOriginTrialTokenStatus raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrialTokenStatus"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageOriginTrialTokenWithStatus.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrialTokenWithStatus {
  raw_token_text : String
  parsed_token : PageOriginTrialToken?
  status : PageOriginTrialTokenStatus
} derive(Eq, Debug)

///|
/// Encode PageOriginTrialTokenWithStatus as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrialTokenWithStatus::to_json(
  self : PageOriginTrialTokenWithStatus,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["rawTokenText"] = Json::string(self.raw_token_text)
  if self.parsed_token is Some(value) {
    fields["parsedToken"] = value.to_json()
  }
  fields["status"] = self.status.to_json()
  Json::object(fields)
}

///|
/// Decode PageOriginTrialTokenWithStatus from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrialTokenWithStatus::from_json(
  value : Json,
) -> PageOriginTrialTokenWithStatus raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrialTokenWithStatus"
  let fields = typed_json_fields(method_name, value)
  {
    raw_token_text: typed_required_string(
      method_name, fields, "rawTokenText", "string",
    ),
    parsed_token: match typed_optional_json(fields, "parsedToken") {
      Some(value) => Some(PageOriginTrialToken::from_json(value))
      None => None
    },
    status: PageOriginTrialTokenStatus::from_json(
      typed_required_json(method_name, fields, "status"),
    ),
  }
}

///|
/// Generated CDP value object for PageOriginTrialUsageRestriction.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageOriginTrialUsageRestriction {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageOriginTrialUsageRestriction as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageOriginTrialUsageRestriction::to_json(
  self : PageOriginTrialUsageRestriction,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageOriginTrialUsageRestriction from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageOriginTrialUsageRestriction::from_json(
  value : Json,
) -> PageOriginTrialUsageRestriction raise @protocol.ProtocolSchemaError {
  let method_name = "PageOriginTrialUsageRestriction"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PagePermissionsPolicyBlockLocator.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePermissionsPolicyBlockLocator {
  frame_id : PageFrameId
  block_reason : PagePermissionsPolicyBlockReason
} derive(Eq, Debug)

///|
/// Encode PagePermissionsPolicyBlockLocator as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PagePermissionsPolicyBlockLocator::to_json(
  self : PagePermissionsPolicyBlockLocator,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["blockReason"] = self.block_reason.to_json()
  Json::object(fields)
}

///|
/// Decode PagePermissionsPolicyBlockLocator from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PagePermissionsPolicyBlockLocator::from_json(
  value : Json,
) -> PagePermissionsPolicyBlockLocator raise @protocol.ProtocolSchemaError {
  let method_name = "PagePermissionsPolicyBlockLocator"
  let fields = typed_json_fields(method_name, value)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    block_reason: PagePermissionsPolicyBlockReason::from_json(
      typed_required_json(method_name, fields, "blockReason"),
    ),
  }
}

///|
/// Generated CDP value object for PagePermissionsPolicyBlockReason.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePermissionsPolicyBlockReason {
  value : String
} derive(Eq, Debug)

///|
/// Encode PagePermissionsPolicyBlockReason as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PagePermissionsPolicyBlockReason::to_json(
  self : PagePermissionsPolicyBlockReason,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PagePermissionsPolicyBlockReason from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PagePermissionsPolicyBlockReason::from_json(
  value : Json,
) -> PagePermissionsPolicyBlockReason raise @protocol.ProtocolSchemaError {
  let method_name = "PagePermissionsPolicyBlockReason"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PagePermissionsPolicyFeature.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePermissionsPolicyFeature {
  value : String
} derive(Eq, Debug)

///|
/// Encode PagePermissionsPolicyFeature as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PagePermissionsPolicyFeature::to_json(
  self : PagePermissionsPolicyFeature,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode PagePermissionsPolicyFeature from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PagePermissionsPolicyFeature::from_json(
  value : Json,
) -> PagePermissionsPolicyFeature raise @protocol.ProtocolSchemaError {
  let method_name = "PagePermissionsPolicyFeature"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PagePermissionsPolicyFeatureState.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePermissionsPolicyFeatureState {
  feature : PagePermissionsPolicyFeature
  allowed : Bool
  locator : PagePermissionsPolicyBlockLocator?
} derive(Eq, Debug)

///|
/// Encode PagePermissionsPolicyFeatureState as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PagePermissionsPolicyFeatureState::to_json(
  self : PagePermissionsPolicyFeatureState,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["feature"] = self.feature.to_json()
  fields["allowed"] = Json::boolean(self.allowed)
  if self.locator is Some(value) {
    fields["locator"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode PagePermissionsPolicyFeatureState from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PagePermissionsPolicyFeatureState::from_json(
  value : Json,
) -> PagePermissionsPolicyFeatureState raise @protocol.ProtocolSchemaError {
  let method_name = "PagePermissionsPolicyFeatureState"
  let fields = typed_json_fields(method_name, value)
  {
    feature: PagePermissionsPolicyFeature::from_json(
      typed_required_json(method_name, fields, "feature"),
    ),
    allowed: typed_required_bool(method_name, fields, "allowed", "boolean"),
    locator: match typed_optional_json(fields, "locator") {
      Some(value) => Some(PagePermissionsPolicyBlockLocator::from_json(value))
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageProtocolHandler.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageProtocolHandler {
  protocol : String
  url : String
} derive(Eq, Debug)

///|
/// Encode PageProtocolHandler as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageProtocolHandler::to_json(self : PageProtocolHandler) -> Json {
  let fields : Map[String, Json] = {}
  fields["protocol"] = Json::string(self.protocol)
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Decode PageProtocolHandler from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageProtocolHandler::from_json(
  value : Json,
) -> PageProtocolHandler raise @protocol.ProtocolSchemaError {
  let method_name = "PageProtocolHandler"
  let fields = typed_json_fields(method_name, value)
  {
    protocol: typed_required_string(method_name, fields, "protocol", "string"),
    url: typed_required_string(method_name, fields, "url", "string"),
  }
}

///|
/// Generated CDP value object for PageReferrerPolicy.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageReferrerPolicy {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageReferrerPolicy as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageReferrerPolicy::to_json(self : PageReferrerPolicy) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageReferrerPolicy from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageReferrerPolicy::from_json(
  value : Json,
) -> PageReferrerPolicy raise @protocol.ProtocolSchemaError {
  let method_name = "PageReferrerPolicy"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageRelatedApplication.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageRelatedApplication {
  id : String?
  url : String
} derive(Eq, Debug)

///|
/// Encode PageRelatedApplication as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageRelatedApplication::to_json(self : PageRelatedApplication) -> Json {
  let fields : Map[String, Json] = {}
  if self.id is Some(value) {
    fields["id"] = Json::string(value)
  }
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Decode PageRelatedApplication from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageRelatedApplication::from_json(
  value : Json,
) -> PageRelatedApplication raise @protocol.ProtocolSchemaError {
  let method_name = "PageRelatedApplication"
  let fields = typed_json_fields(method_name, value)
  {
    id: typed_optional_string(method_name, fields, "id", "string"),
    url: typed_required_string(method_name, fields, "url", "string"),
  }
}

///|
/// Generated CDP value object for PageScopeExtension.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScopeExtension {
  origin : String
  has_origin_wildcard : Bool
} derive(Eq, Debug)

///|
/// Encode PageScopeExtension as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScopeExtension::to_json(self : PageScopeExtension) -> Json {
  let fields : Map[String, Json] = {}
  fields["origin"] = Json::string(self.origin)
  fields["hasOriginWildcard"] = Json::boolean(self.has_origin_wildcard)
  Json::object(fields)
}

///|
/// Decode PageScopeExtension from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageScopeExtension::from_json(
  value : Json,
) -> PageScopeExtension raise @protocol.ProtocolSchemaError {
  let method_name = "PageScopeExtension"
  let fields = typed_json_fields(method_name, value)
  {
    origin: typed_required_string(method_name, fields, "origin", "string"),
    has_origin_wildcard: typed_required_bool(
      method_name, fields, "hasOriginWildcard", "boolean",
    ),
  }
}

///|
/// Generated CDP value object for PageScreencastFrameMetadata.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScreencastFrameMetadata {
  offset_top : Double
  page_scale_factor : Double
  device_width : Double
  device_height : Double
  scroll_offset_x : Double
  scroll_offset_y : Double
  timestamp : NetworkTimeSinceEpoch?
} derive(Eq, Debug)

///|
/// Encode PageScreencastFrameMetadata as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScreencastFrameMetadata::to_json(
  self : PageScreencastFrameMetadata,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["offsetTop"] = Json::number(self.offset_top)
  fields["pageScaleFactor"] = Json::number(self.page_scale_factor)
  fields["deviceWidth"] = Json::number(self.device_width)
  fields["deviceHeight"] = Json::number(self.device_height)
  fields["scrollOffsetX"] = Json::number(self.scroll_offset_x)
  fields["scrollOffsetY"] = Json::number(self.scroll_offset_y)
  if self.timestamp is Some(value) {
    fields["timestamp"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode PageScreencastFrameMetadata from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageScreencastFrameMetadata::from_json(
  value : Json,
) -> PageScreencastFrameMetadata raise @protocol.ProtocolSchemaError {
  let method_name = "PageScreencastFrameMetadata"
  let fields = typed_json_fields(method_name, value)
  {
    offset_top: typed_required_double(
      method_name, fields, "offsetTop", "number",
    ),
    page_scale_factor: typed_required_double(
      method_name, fields, "pageScaleFactor", "number",
    ),
    device_width: typed_required_double(
      method_name, fields, "deviceWidth", "number",
    ),
    device_height: typed_required_double(
      method_name, fields, "deviceHeight", "number",
    ),
    scroll_offset_x: typed_required_double(
      method_name, fields, "scrollOffsetX", "number",
    ),
    scroll_offset_y: typed_required_double(
      method_name, fields, "scrollOffsetY", "number",
    ),
    timestamp: match typed_optional_json(fields, "timestamp") {
      Some(value) => Some(NetworkTimeSinceEpoch::from_json(value))
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageScreenshot.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScreenshot {
  image : PageImageResource
  form_factor : String
  label : String?
} derive(Eq, Debug)

///|
/// Encode PageScreenshot as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScreenshot::to_json(self : PageScreenshot) -> Json {
  let fields : Map[String, Json] = {}
  fields["image"] = self.image.to_json()
  fields["formFactor"] = Json::string(self.form_factor)
  if self.label is Some(value) {
    fields["label"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode PageScreenshot from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageScreenshot::from_json(
  value : Json,
) -> PageScreenshot raise @protocol.ProtocolSchemaError {
  let method_name = "PageScreenshot"
  let fields = typed_json_fields(method_name, value)
  {
    image: PageImageResource::from_json(
      typed_required_json(method_name, fields, "image"),
    ),
    form_factor: typed_required_string(
      method_name, fields, "formFactor", "string",
    ),
    label: typed_optional_string(method_name, fields, "label", "string"),
  }
}

///|
/// Generated CDP value object for PageScriptFontFamilies.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScriptFontFamilies {
  script : String
  font_families : PageFontFamilies
} derive(Eq, Debug)

///|
/// Encode PageScriptFontFamilies as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScriptFontFamilies::to_json(self : PageScriptFontFamilies) -> Json {
  let fields : Map[String, Json] = {}
  fields["script"] = Json::string(self.script)
  fields["fontFamilies"] = self.font_families.to_json()
  Json::object(fields)
}

///|
/// Decode PageScriptFontFamilies from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageScriptFontFamilies::from_json(
  value : Json,
) -> PageScriptFontFamilies raise @protocol.ProtocolSchemaError {
  let method_name = "PageScriptFontFamilies"
  let fields = typed_json_fields(method_name, value)
  {
    script: typed_required_string(method_name, fields, "script", "string"),
    font_families: PageFontFamilies::from_json(
      typed_required_json(method_name, fields, "fontFamilies"),
    ),
  }
}

///|
/// Generated CDP value object for PageScriptIdentifier.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScriptIdentifier {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageScriptIdentifier as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScriptIdentifier::to_json(self : PageScriptIdentifier) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageScriptIdentifier from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageScriptIdentifier::from_json(
  value : Json,
) -> PageScriptIdentifier raise @protocol.ProtocolSchemaError {
  let method_name = "PageScriptIdentifier"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageSecureContextType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSecureContextType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageSecureContextType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSecureContextType::to_json(self : PageSecureContextType) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageSecureContextType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageSecureContextType::from_json(
  value : Json,
) -> PageSecureContextType raise @protocol.ProtocolSchemaError {
  let method_name = "PageSecureContextType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageSecurityOriginDetails.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSecurityOriginDetails {
  is_localhost : Bool
} derive(Eq, Debug)

///|
/// Encode PageSecurityOriginDetails as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSecurityOriginDetails::to_json(
  self : PageSecurityOriginDetails,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["isLocalhost"] = Json::boolean(self.is_localhost)
  Json::object(fields)
}

///|
/// Decode PageSecurityOriginDetails from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageSecurityOriginDetails::from_json(
  value : Json,
) -> PageSecurityOriginDetails raise @protocol.ProtocolSchemaError {
  let method_name = "PageSecurityOriginDetails"
  let fields = typed_json_fields(method_name, value)
  {
    is_localhost: typed_required_bool(
      method_name, fields, "isLocalhost", "boolean",
    ),
  }
}

///|
/// Generated CDP value object for PageShareTarget.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageShareTarget {
  action : String
  method_ : String
  enctype : String
  title : String?
  text : String?
  url : String?
  files : Array[PageFileFilter]?
} derive(Eq, Debug)

///|
/// Encode PageShareTarget as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageShareTarget::to_json(self : PageShareTarget) -> Json {
  let fields : Map[String, Json] = {}
  fields["action"] = Json::string(self.action)
  fields["method"] = Json::string(self.method_)
  fields["enctype"] = Json::string(self.enctype)
  if self.title is Some(value) {
    fields["title"] = Json::string(value)
  }
  if self.text is Some(value) {
    fields["text"] = Json::string(value)
  }
  if self.url is Some(value) {
    fields["url"] = Json::string(value)
  }
  if self.files is Some(value) {
    fields["files"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Decode PageShareTarget from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageShareTarget::from_json(
  value : Json,
) -> PageShareTarget raise @protocol.ProtocolSchemaError {
  let method_name = "PageShareTarget"
  let fields = typed_json_fields(method_name, value)
  {
    action: typed_required_string(method_name, fields, "action", "string"),
    method_: typed_required_string(method_name, fields, "method", "string"),
    enctype: typed_required_string(method_name, fields, "enctype", "string"),
    title: typed_optional_string(method_name, fields, "title", "string"),
    text: typed_optional_string(method_name, fields, "text", "string"),
    url: typed_optional_string(method_name, fields, "url", "string"),
    files: match typed_optional_json(fields, "files") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "files", "array<FileFilter>").map(item => {
              PageFileFilter::from_json(item)
            },
          ),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for PageShortcut.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageShortcut {
  name : String
  url : String
} derive(Eq, Debug)

///|
/// Encode PageShortcut as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageShortcut::to_json(self : PageShortcut) -> Json {
  let fields : Map[String, Json] = {}
  fields["name"] = Json::string(self.name)
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Decode PageShortcut from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageShortcut::from_json(
  value : Json,
) -> PageShortcut raise @protocol.ProtocolSchemaError {
  let method_name = "PageShortcut"
  let fields = typed_json_fields(method_name, value)
  {
    name: typed_required_string(method_name, fields, "name", "string"),
    url: typed_required_string(method_name, fields, "url", "string"),
  }
}

///|
/// Generated CDP value object for PageTransitionType.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageTransitionType {
  value : String
} derive(Eq, Debug)

///|
/// Encode PageTransitionType as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageTransitionType::to_json(self : PageTransitionType) -> Json {
  Json::string(self.value)
}

///|
/// Decode PageTransitionType from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageTransitionType::from_json(
  value : Json,
) -> PageTransitionType raise @protocol.ProtocolSchemaError {
  let method_name = "PageTransitionType"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PageViewport.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageViewport {
  x : Double
  y : Double
  width : Double
  height : Double
  scale : Double
} derive(Eq, Debug)

///|
/// Encode PageViewport as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageViewport::to_json(self : PageViewport) -> Json {
  let fields : Map[String, Json] = {}
  fields["x"] = Json::number(self.x)
  fields["y"] = Json::number(self.y)
  fields["width"] = Json::number(self.width)
  fields["height"] = Json::number(self.height)
  fields["scale"] = Json::number(self.scale)
  Json::object(fields)
}

///|
/// Decode PageViewport from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageViewport::from_json(
  value : Json,
) -> PageViewport raise @protocol.ProtocolSchemaError {
  let method_name = "PageViewport"
  let fields = typed_json_fields(method_name, value)
  {
    x: typed_required_double(method_name, fields, "x", "number"),
    y: typed_required_double(method_name, fields, "y", "number"),
    width: typed_required_double(method_name, fields, "width", "number"),
    height: typed_required_double(method_name, fields, "height", "number"),
    scale: typed_required_double(method_name, fields, "scale", "number"),
  }
}

///|
/// Generated CDP value object for PageVisualViewport.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageVisualViewport {
  offset_x : Double
  offset_y : Double
  page_x : Double
  page_y : Double
  client_width : Double
  client_height : Double
  scale : Double
  zoom : Double?
} derive(Eq, Debug)

///|
/// Encode PageVisualViewport as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageVisualViewport::to_json(self : PageVisualViewport) -> Json {
  let fields : Map[String, Json] = {}
  fields["offsetX"] = Json::number(self.offset_x)
  fields["offsetY"] = Json::number(self.offset_y)
  fields["pageX"] = Json::number(self.page_x)
  fields["pageY"] = Json::number(self.page_y)
  fields["clientWidth"] = Json::number(self.client_width)
  fields["clientHeight"] = Json::number(self.client_height)
  fields["scale"] = Json::number(self.scale)
  if self.zoom is Some(value) {
    fields["zoom"] = Json::number(value)
  }
  Json::object(fields)
}

///|
/// Decode PageVisualViewport from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageVisualViewport::from_json(
  value : Json,
) -> PageVisualViewport raise @protocol.ProtocolSchemaError {
  let method_name = "PageVisualViewport"
  let fields = typed_json_fields(method_name, value)
  {
    offset_x: typed_required_double(method_name, fields, "offsetX", "number"),
    offset_y: typed_required_double(method_name, fields, "offsetY", "number"),
    page_x: typed_required_double(method_name, fields, "pageX", "number"),
    page_y: typed_required_double(method_name, fields, "pageY", "number"),
    client_width: typed_required_double(
      method_name, fields, "clientWidth", "number",
    ),
    client_height: typed_required_double(
      method_name, fields, "clientHeight", "number",
    ),
    scale: typed_required_double(method_name, fields, "scale", "number"),
    zoom: typed_optional_double(method_name, fields, "zoom", "number"),
  }
}

///|
/// Generated CDP value object for PageWebAppManifest.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageWebAppManifest {
  background_color : String?
  description : String?
  dir : String?
  display : String?
  display_overrides : Array[String]?
  file_handlers : Array[PageFileHandler]?
  icons : Array[PageImageResource]?
  id : String?
  lang : String?
  launch_handler : PageLaunchHandler?
  name : String?
  orientation : String?
  prefer_related_applications : Bool?
  protocol_handlers : Array[PageProtocolHandler]?
  related_applications : Array[PageRelatedApplication]?
  scope : String?
  scope_extensions : Array[PageScopeExtension]?
  screenshots : Array[PageScreenshot]?
  share_target : PageShareTarget?
  short_name : String?
  shortcuts : Array[PageShortcut]?
  start_url : String?
  theme_color : String?
} derive(Eq, Debug)

///|
/// Encode PageWebAppManifest as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageWebAppManifest::to_json(self : PageWebAppManifest) -> Json {
  let fields : Map[String, Json] = {}
  if self.background_color is Some(value) {
    fields["backgroundColor"] = Json::string(value)
  }
  if self.description is Some(value) {
    fields["description"] = Json::string(value)
  }
  if self.dir is Some(value) {
    fields["dir"] = Json::string(value)
  }
  if self.display is Some(value) {
    fields["display"] = Json::string(value)
  }
  if self.display_overrides is Some(value) {
    fields["displayOverrides"] = Json::array(
      value.map(item => Json::string(item)),
    )
  }
  if self.file_handlers is Some(value) {
    fields["fileHandlers"] = Json::array(value.map(item => item.to_json()))
  }
  if self.icons is Some(value) {
    fields["icons"] = Json::array(value.map(item => item.to_json()))
  }
  if self.id is Some(value) {
    fields["id"] = Json::string(value)
  }
  if self.lang is Some(value) {
    fields["lang"] = Json::string(value)
  }
  if self.launch_handler is Some(value) {
    fields["launchHandler"] = value.to_json()
  }
  if self.name is Some(value) {
    fields["name"] = Json::string(value)
  }
  if self.orientation is Some(value) {
    fields["orientation"] = Json::string(value)
  }
  if self.prefer_related_applications is Some(value) {
    fields["preferRelatedApplications"] = Json::boolean(value)
  }
  if self.protocol_handlers is Some(value) {
    fields["protocolHandlers"] = Json::array(value.map(item => item.to_json()))
  }
  if self.related_applications is Some(value) {
    fields["relatedApplications"] = Json::array(
      value.map(item => item.to_json()),
    )
  }
  if self.scope is Some(value) {
    fields["scope"] = Json::string(value)
  }
  if self.scope_extensions is Some(value) {
    fields["scopeExtensions"] = Json::array(value.map(item => item.to_json()))
  }
  if self.screenshots is Some(value) {
    fields["screenshots"] = Json::array(value.map(item => item.to_json()))
  }
  if self.share_target is Some(value) {
    fields["shareTarget"] = value.to_json()
  }
  if self.short_name is Some(value) {
    fields["shortName"] = Json::string(value)
  }
  if self.shortcuts is Some(value) {
    fields["shortcuts"] = Json::array(value.map(item => item.to_json()))
  }
  if self.start_url is Some(value) {
    fields["startUrl"] = Json::string(value)
  }
  if self.theme_color is Some(value) {
    fields["themeColor"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode PageWebAppManifest from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PageWebAppManifest::from_json(
  value : Json,
) -> PageWebAppManifest raise @protocol.ProtocolSchemaError {
  let method_name = "PageWebAppManifest"
  let fields = typed_json_fields(method_name, value)
  {
    background_color: typed_optional_string(
      method_name, fields, "backgroundColor", "string",
    ),
    description: typed_optional_string(
      method_name, fields, "description", "string",
    ),
    dir: typed_optional_string(method_name, fields, "dir", "string"),
    display: typed_optional_string(method_name, fields, "display", "string"),
    display_overrides: match typed_optional_json(fields, "displayOverrides") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "displayOverrides", "array<string>",
          ).map(item => {
            typed_array_string(
              method_name, "displayOverrides", "array<string>", item,
            )
          }),
        )
      None => None
    },
    file_handlers: match typed_optional_json(fields, "fileHandlers") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "fileHandlers", "array<FileHandler>",
          ).map(item => PageFileHandler::from_json(item)),
        )
      None => None
    },
    icons: match typed_optional_json(fields, "icons") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "icons", "array<ImageResource>").map(item => {
              PageImageResource::from_json(item)
            },
          ),
        )
      None => None
    },
    id: typed_optional_string(method_name, fields, "id", "string"),
    lang: typed_optional_string(method_name, fields, "lang", "string"),
    launch_handler: match typed_optional_json(fields, "launchHandler") {
      Some(value) => Some(PageLaunchHandler::from_json(value))
      None => None
    },
    name: typed_optional_string(method_name, fields, "name", "string"),
    orientation: typed_optional_string(
      method_name, fields, "orientation", "string",
    ),
    prefer_related_applications: typed_optional_bool(
      method_name, fields, "preferRelatedApplications", "boolean",
    ),
    protocol_handlers: match typed_optional_json(fields, "protocolHandlers") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "protocolHandlers", "array<ProtocolHandler>",
          ).map(item => PageProtocolHandler::from_json(item)),
        )
      None => None
    },
    related_applications: match
      typed_optional_json(fields, "relatedApplications") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "relatedApplications", "array<RelatedApplication>",
          ).map(item => PageRelatedApplication::from_json(item)),
        )
      None => None
    },
    scope: typed_optional_string(method_name, fields, "scope", "string"),
    scope_extensions: match typed_optional_json(fields, "scopeExtensions") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "scopeExtensions", "array<ScopeExtension>",
          ).map(item => PageScopeExtension::from_json(item)),
        )
      None => None
    },
    screenshots: match typed_optional_json(fields, "screenshots") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "screenshots", "array<Screenshot>",
          ).map(item => PageScreenshot::from_json(item)),
        )
      None => None
    },
    share_target: match typed_optional_json(fields, "shareTarget") {
      Some(value) => Some(PageShareTarget::from_json(value))
      None => None
    },
    short_name: typed_optional_string(
      method_name, fields, "shortName", "string",
    ),
    shortcuts: match typed_optional_json(fields, "shortcuts") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "shortcuts", "array<Shortcut>").map(item => {
              PageShortcut::from_json(item)
            },
          ),
        )
      None => None
    },
    start_url: typed_optional_string(method_name, fields, "startUrl", "string"),
    theme_color: typed_optional_string(
      method_name, fields, "themeColor", "string",
    ),
  }
}

///|
/// Generated CDP value object for PageAddCompilationCacheParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAddCompilationCacheParams {
  url : String
  data : String
} derive(Eq, Debug)

///|
/// Encode PageAddCompilationCacheParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAddCompilationCacheParams::to_json(
  self : PageAddCompilationCacheParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["data"] = Json::string(self.data)
  Json::object(fields)
}

///|
/// Build a typed Page.addCompilationCache command envelope.
///
/// Pass a PageAddCompilationCacheParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_add_compilation_cache_command(id=1, params, session_id=None)
/// ```
pub fn page_add_compilation_cache_command(
  id~ : Int,
  params : PageAddCompilationCacheParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.addCompilationCache",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageAddScriptToEvaluateOnLoadParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAddScriptToEvaluateOnLoadParams {
  script_source : String
} derive(Eq, Debug)

///|
/// Encode PageAddScriptToEvaluateOnLoadParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAddScriptToEvaluateOnLoadParams::to_json(
  self : PageAddScriptToEvaluateOnLoadParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["scriptSource"] = Json::string(self.script_source)
  Json::object(fields)
}

///|
/// Generated CDP value object for PageAddScriptToEvaluateOnLoadResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAddScriptToEvaluateOnLoadResult {
  identifier : PageScriptIdentifier
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.addScriptToEvaluateOnLoad.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageAddScriptToEvaluateOnLoadResult::from_json(
  result : Json,
) -> PageAddScriptToEvaluateOnLoadResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.addScriptToEvaluateOnLoad"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    identifier: PageScriptIdentifier::from_json(
      typed_required_result_json(method_name, fields, "identifier"),
    ),
  }
}

///|
/// Build a typed Page.addScriptToEvaluateOnLoad command envelope.
///
/// Pass a PageAddScriptToEvaluateOnLoadParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_add_script_to_evaluate_on_load_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_add_script_to_evaluate_on_load_command(
  id~ : Int,
  params : PageAddScriptToEvaluateOnLoadParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.addScriptToEvaluateOnLoad",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageAddScriptToEvaluateOnNewDocumentParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAddScriptToEvaluateOnNewDocumentParams {
  source : String
  world_name : String?
  include_command_line_api : Bool?
  run_immediately : Bool?
} derive(Eq, Debug)

///|
/// Encode PageAddScriptToEvaluateOnNewDocumentParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageAddScriptToEvaluateOnNewDocumentParams::to_json(
  self : PageAddScriptToEvaluateOnNewDocumentParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["source"] = Json::string(self.source)
  if self.world_name is Some(value) {
    fields["worldName"] = Json::string(value)
  }
  if self.include_command_line_api is Some(value) {
    fields["includeCommandLineAPI"] = Json::boolean(value)
  }
  if self.run_immediately is Some(value) {
    fields["runImmediately"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageAddScriptToEvaluateOnNewDocumentResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageAddScriptToEvaluateOnNewDocumentResult {
  identifier : PageScriptIdentifier
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.addScriptToEvaluateOnNewDocument.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageAddScriptToEvaluateOnNewDocumentResult::from_json(
  result : Json,
) -> PageAddScriptToEvaluateOnNewDocumentResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.addScriptToEvaluateOnNewDocument"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    identifier: PageScriptIdentifier::from_json(
      typed_required_result_json(method_name, fields, "identifier"),
    ),
  }
}

///|
/// Build a typed Page.addScriptToEvaluateOnNewDocument command envelope.
///
/// Pass a PageAddScriptToEvaluateOnNewDocumentParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_add_script_to_evaluate_on_new_document_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_add_script_to_evaluate_on_new_document_command(
  id~ : Int,
  params : PageAddScriptToEvaluateOnNewDocumentParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.addScriptToEvaluateOnNewDocument",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.bringToFront command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_bring_to_front_command(id=1, session_id=None)
/// ```
pub fn page_bring_to_front_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.bringToFront",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageCaptureScreenshotParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCaptureScreenshotParams {
  format : String?
  quality : Int?
  clip : PageViewport?
  from_surface : Bool?
  capture_beyond_viewport : Bool?
  optimize_for_speed : Bool?
} derive(Eq, Debug)

///|
/// Encode PageCaptureScreenshotParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCaptureScreenshotParams::to_json(
  self : PageCaptureScreenshotParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.format is Some(value) {
    fields["format"] = Json::string(value)
  }
  if self.quality is Some(value) {
    fields["quality"] = Json::number(value.to_double())
  }
  if self.clip is Some(value) {
    fields["clip"] = value.to_json()
  }
  if self.from_surface is Some(value) {
    fields["fromSurface"] = Json::boolean(value)
  }
  if self.capture_beyond_viewport is Some(value) {
    fields["captureBeyondViewport"] = Json::boolean(value)
  }
  if self.optimize_for_speed is Some(value) {
    fields["optimizeForSpeed"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageCaptureScreenshotResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCaptureScreenshotResult {
  data : String
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.captureScreenshot.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageCaptureScreenshotResult::from_json(
  result : Json,
) -> PageCaptureScreenshotResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.captureScreenshot"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  { data: typed_required_result_string(method_name, fields, "data", "string") }
}

///|
/// Build a typed Page.captureScreenshot command envelope.
///
/// Pass a PageCaptureScreenshotParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_capture_screenshot_command(id=1, params, session_id=None)
/// ```
pub fn page_capture_screenshot_command(
  id~ : Int,
  params : PageCaptureScreenshotParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.captureScreenshot",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageCaptureSnapshotParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCaptureSnapshotParams {
  format : String?
} derive(Eq, Debug)

///|
/// Encode PageCaptureSnapshotParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCaptureSnapshotParams::to_json(
  self : PageCaptureSnapshotParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.format is Some(value) {
    fields["format"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageCaptureSnapshotResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCaptureSnapshotResult {
  data : String
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.captureSnapshot.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageCaptureSnapshotResult::from_json(
  result : Json,
) -> PageCaptureSnapshotResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.captureSnapshot"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  { data: typed_required_result_string(method_name, fields, "data", "string") }
}

///|
/// Build a typed Page.captureSnapshot command envelope.
///
/// Pass a PageCaptureSnapshotParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_capture_snapshot_command(id=1, params, session_id=None)
/// ```
pub fn page_capture_snapshot_command(
  id~ : Int,
  params : PageCaptureSnapshotParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.captureSnapshot",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.clearCompilationCache command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_clear_compilation_cache_command(id=1, session_id=None)
/// ```
pub fn page_clear_compilation_cache_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.clearCompilationCache",
    session_id?,
  )
}

///|
/// Build a typed Page.clearDeviceMetricsOverride command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_clear_device_metrics_override_command(id=1, session_id=None)
/// ```
pub fn page_clear_device_metrics_override_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.clearDeviceMetricsOverride",
    session_id?,
  )
}

///|
/// Build a typed Page.clearDeviceOrientationOverride command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_clear_device_orientation_override_command(
///   id=1,
///   session_id=None,
/// )
/// ```
pub fn page_clear_device_orientation_override_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.clearDeviceOrientationOverride",
    session_id?,
  )
}

///|
/// Build a typed Page.clearGeolocationOverride command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_clear_geolocation_override_command(id=1, session_id=None)
/// ```
pub fn page_clear_geolocation_override_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.clearGeolocationOverride",
    session_id?,
  )
}

///|
/// Build a typed Page.close command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_close_command(id=1, session_id=None)
/// ```
pub fn page_close_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Page.close", session_id?)
}

///|
/// Build a typed Page.crash command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_crash_command(id=1, session_id=None)
/// ```
pub fn page_crash_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Page.crash", session_id?)
}

///|
/// Generated CDP value object for PageCreateIsolatedWorldParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCreateIsolatedWorldParams {
  frame_id : PageFrameId
  world_name : String?
  grant_univeral_access : Bool?
} derive(Eq, Debug)

///|
/// Encode PageCreateIsolatedWorldParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCreateIsolatedWorldParams::to_json(
  self : PageCreateIsolatedWorldParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  if self.world_name is Some(value) {
    fields["worldName"] = Json::string(value)
  }
  if self.grant_univeral_access is Some(value) {
    fields["grantUniveralAccess"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageCreateIsolatedWorldResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCreateIsolatedWorldResult {
  execution_context_id : RuntimeExecutionContextId
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.createIsolatedWorld.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageCreateIsolatedWorldResult::from_json(
  result : Json,
) -> PageCreateIsolatedWorldResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.createIsolatedWorld"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    execution_context_id: RuntimeExecutionContextId::from_json(
      typed_required_result_json(method_name, fields, "executionContextId"),
    ),
  }
}

///|
/// Build a typed Page.createIsolatedWorld command envelope.
///
/// Pass a PageCreateIsolatedWorldParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_create_isolated_world_command(id=1, params, session_id=None)
/// ```
pub fn page_create_isolated_world_command(
  id~ : Int,
  params : PageCreateIsolatedWorldParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.createIsolatedWorld",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageDeleteCookieParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDeleteCookieParams {
  cookie_name : String
  url : String
} derive(Eq, Debug)

///|
/// Encode PageDeleteCookieParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDeleteCookieParams::to_json(self : PageDeleteCookieParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["cookieName"] = Json::string(self.cookie_name)
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Build a typed Page.deleteCookie command envelope.
///
/// Pass a PageDeleteCookieParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_delete_cookie_command(id=1, params, session_id=None)
/// ```
pub fn page_delete_cookie_command(
  id~ : Int,
  params : PageDeleteCookieParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.deleteCookie",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.disable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_disable_command(id=1, session_id=None)
/// ```
pub fn page_disable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Page.disable", session_id?)
}

///|
/// Generated CDP value object for PageEnableParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageEnableParams {
  enable_file_chooser_opened_event : Bool?
} derive(Eq, Debug)

///|
/// Encode PageEnableParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageEnableParams::to_json(self : PageEnableParams) -> Json {
  let fields : Map[String, Json] = {}
  if self.enable_file_chooser_opened_event is Some(value) {
    fields["enableFileChooserOpenedEvent"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.enable command envelope.
///
/// Pass a PageEnableParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_enable_command(id=1, params, session_id=None)
/// ```
pub fn page_enable_command(
  id~ : Int,
  params : PageEnableParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.enable",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGenerateTestReportParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGenerateTestReportParams {
  message : String
  group : String?
} derive(Eq, Debug)

///|
/// Encode PageGenerateTestReportParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGenerateTestReportParams::to_json(
  self : PageGenerateTestReportParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["message"] = Json::string(self.message)
  if self.group is Some(value) {
    fields["group"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.generateTestReport command envelope.
///
/// Pass a PageGenerateTestReportParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_generate_test_report_command(id=1, params, session_id=None)
/// ```
pub fn page_generate_test_report_command(
  id~ : Int,
  params : PageGenerateTestReportParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.generateTestReport",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetAdScriptAncestryParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAdScriptAncestryParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageGetAdScriptAncestryParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetAdScriptAncestryParams::to_json(
  self : PageGetAdScriptAncestryParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetAdScriptAncestryResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAdScriptAncestryResult {
  ad_script_ancestry : PageAdScriptAncestry?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getAdScriptAncestry.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetAdScriptAncestryResult::from_json(
  result : Json,
) -> PageGetAdScriptAncestryResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getAdScriptAncestry"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    ad_script_ancestry: match
      typed_optional_result_json(fields, "adScriptAncestry") {
      Some(value) => Some(PageAdScriptAncestry::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed Page.getAdScriptAncestry command envelope.
///
/// Pass a PageGetAdScriptAncestryParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_ad_script_ancestry_command(id=1, params, session_id=None)
/// ```
pub fn page_get_ad_script_ancestry_command(
  id~ : Int,
  params : PageGetAdScriptAncestryParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getAdScriptAncestry",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetAnnotatedPageContentParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAnnotatedPageContentParams {
  include_actionable_information : Bool?
} derive(Eq, Debug)

///|
/// Encode PageGetAnnotatedPageContentParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetAnnotatedPageContentParams::to_json(
  self : PageGetAnnotatedPageContentParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.include_actionable_information is Some(value) {
    fields["includeActionableInformation"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetAnnotatedPageContentResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAnnotatedPageContentResult {
  content : String
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getAnnotatedPageContent.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetAnnotatedPageContentResult::from_json(
  result : Json,
) -> PageGetAnnotatedPageContentResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getAnnotatedPageContent"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    content: typed_required_result_string(
      method_name, fields, "content", "string",
    ),
  }
}

///|
/// Build a typed Page.getAnnotatedPageContent command envelope.
///
/// Pass a PageGetAnnotatedPageContentParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_annotated_page_content_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_get_annotated_page_content_command(
  id~ : Int,
  params : PageGetAnnotatedPageContentParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getAnnotatedPageContent",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetAppIdResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAppIdResult {
  app_id : String?
  recommended_id : String?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getAppId.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetAppIdResult::from_json(
  result : Json,
) -> PageGetAppIdResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getAppId"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    app_id: typed_optional_result_string(method_name, fields, "appId", "string"),
    recommended_id: typed_optional_result_string(
      method_name, fields, "recommendedId", "string",
    ),
  }
}

///|
/// Build a typed Page.getAppId command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_app_id_command(id=1, session_id=None)
/// ```
pub fn page_get_app_id_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Page.getAppId", session_id?)
}

///|
/// Generated CDP value object for PageGetAppManifestParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAppManifestParams {
  manifest_id : String?
} derive(Eq, Debug)

///|
/// Encode PageGetAppManifestParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetAppManifestParams::to_json(
  self : PageGetAppManifestParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.manifest_id is Some(value) {
    fields["manifestId"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetAppManifestResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetAppManifestResult {
  url : String
  errors : Array[PageAppManifestError]
  data : String?
  parsed : PageAppManifestParsedProperties?
  manifest : PageWebAppManifest
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getAppManifest.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetAppManifestResult::from_json(
  result : Json,
) -> PageGetAppManifestResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getAppManifest"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    url: typed_required_result_string(method_name, fields, "url", "string"),
    errors: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "errors"),
      "errors",
      "array<AppManifestError>",
    ).map(item => PageAppManifestError::from_json(item)),
    data: typed_optional_result_string(method_name, fields, "data", "string"),
    parsed: match typed_optional_result_json(fields, "parsed") {
      Some(value) => Some(PageAppManifestParsedProperties::from_json(value))
      None => None
    },
    manifest: PageWebAppManifest::from_json(
      typed_required_result_json(method_name, fields, "manifest"),
    ),
  }
}

///|
/// Build a typed Page.getAppManifest command envelope.
///
/// Pass a PageGetAppManifestParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_app_manifest_command(id=1, params, session_id=None)
/// ```
pub fn page_get_app_manifest_command(
  id~ : Int,
  params : PageGetAppManifestParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getAppManifest",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetFrameTreeResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetFrameTreeResult {
  frame_tree : PageFrameTree
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getFrameTree.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetFrameTreeResult::from_json(
  result : Json,
) -> PageGetFrameTreeResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getFrameTree"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    frame_tree: PageFrameTree::from_json(
      typed_required_result_json(method_name, fields, "frameTree"),
    ),
  }
}

///|
/// Build a typed Page.getFrameTree command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_frame_tree_command(id=1, session_id=None)
/// ```
pub fn page_get_frame_tree_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getFrameTree",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetInstallabilityErrorsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetInstallabilityErrorsResult {
  installability_errors : Array[PageInstallabilityError]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getInstallabilityErrors.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetInstallabilityErrorsResult::from_json(
  result : Json,
) -> PageGetInstallabilityErrorsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getInstallabilityErrors"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    installability_errors: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "installabilityErrors"),
      "installabilityErrors",
      "array<InstallabilityError>",
    ).map(item => PageInstallabilityError::from_json(item)),
  }
}

///|
/// Build a typed Page.getInstallabilityErrors command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_installability_errors_command(id=1, session_id=None)
/// ```
pub fn page_get_installability_errors_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getInstallabilityErrors",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetLayoutMetricsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetLayoutMetricsResult {
  layout_viewport : PageLayoutViewport
  visual_viewport : PageVisualViewport
  content_size : DOMRect
  css_layout_viewport : PageLayoutViewport
  css_visual_viewport : PageVisualViewport
  css_content_size : DOMRect
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getLayoutMetrics.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetLayoutMetricsResult::from_json(
  result : Json,
) -> PageGetLayoutMetricsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getLayoutMetrics"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    layout_viewport: PageLayoutViewport::from_json(
      typed_required_result_json(method_name, fields, "layoutViewport"),
    ),
    visual_viewport: PageVisualViewport::from_json(
      typed_required_result_json(method_name, fields, "visualViewport"),
    ),
    content_size: DOMRect::from_json(
      typed_required_result_json(method_name, fields, "contentSize"),
    ),
    css_layout_viewport: PageLayoutViewport::from_json(
      typed_required_result_json(method_name, fields, "cssLayoutViewport"),
    ),
    css_visual_viewport: PageVisualViewport::from_json(
      typed_required_result_json(method_name, fields, "cssVisualViewport"),
    ),
    css_content_size: DOMRect::from_json(
      typed_required_result_json(method_name, fields, "cssContentSize"),
    ),
  }
}

///|
/// Build a typed Page.getLayoutMetrics command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_layout_metrics_command(id=1, session_id=None)
/// ```
pub fn page_get_layout_metrics_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getLayoutMetrics",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetManifestIconsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetManifestIconsResult {
  primary_icon : String?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getManifestIcons.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetManifestIconsResult::from_json(
  result : Json,
) -> PageGetManifestIconsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getManifestIcons"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    primary_icon: typed_optional_result_string(
      method_name, fields, "primaryIcon", "string",
    ),
  }
}

///|
/// Build a typed Page.getManifestIcons command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_manifest_icons_command(id=1, session_id=None)
/// ```
pub fn page_get_manifest_icons_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getManifestIcons",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetNavigationHistoryResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetNavigationHistoryResult {
  current_index : Int
  entries : Array[PageNavigationEntry]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getNavigationHistory.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetNavigationHistoryResult::from_json(
  result : Json,
) -> PageGetNavigationHistoryResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getNavigationHistory"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    current_index: typed_required_result_int(
      method_name, fields, "currentIndex", "integer",
    ),
    entries: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "entries"),
      "entries",
      "array<NavigationEntry>",
    ).map(item => PageNavigationEntry::from_json(item)),
  }
}

///|
/// Build a typed Page.getNavigationHistory command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_navigation_history_command(id=1, session_id=None)
/// ```
pub fn page_get_navigation_history_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getNavigationHistory",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetOriginTrialsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetOriginTrialsParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageGetOriginTrialsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetOriginTrialsParams::to_json(
  self : PageGetOriginTrialsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetOriginTrialsResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetOriginTrialsResult {
  origin_trials : Array[PageOriginTrial]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getOriginTrials.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetOriginTrialsResult::from_json(
  result : Json,
) -> PageGetOriginTrialsResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getOriginTrials"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    origin_trials: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "originTrials"),
      "originTrials",
      "array<OriginTrial>",
    ).map(item => PageOriginTrial::from_json(item)),
  }
}

///|
/// Build a typed Page.getOriginTrials command envelope.
///
/// Pass a PageGetOriginTrialsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_origin_trials_command(id=1, params, session_id=None)
/// ```
pub fn page_get_origin_trials_command(
  id~ : Int,
  params : PageGetOriginTrialsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getOriginTrials",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetPermissionsPolicyStateParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetPermissionsPolicyStateParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageGetPermissionsPolicyStateParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetPermissionsPolicyStateParams::to_json(
  self : PageGetPermissionsPolicyStateParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetPermissionsPolicyStateResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetPermissionsPolicyStateResult {
  states : Array[PagePermissionsPolicyFeatureState]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getPermissionsPolicyState.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetPermissionsPolicyStateResult::from_json(
  result : Json,
) -> PageGetPermissionsPolicyStateResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getPermissionsPolicyState"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    states: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "states"),
      "states",
      "array<PermissionsPolicyFeatureState>",
    ).map(item => PagePermissionsPolicyFeatureState::from_json(item)),
  }
}

///|
/// Build a typed Page.getPermissionsPolicyState command envelope.
///
/// Pass a PageGetPermissionsPolicyStateParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_permissions_policy_state_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_get_permissions_policy_state_command(
  id~ : Int,
  params : PageGetPermissionsPolicyStateParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getPermissionsPolicyState",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetResourceContentParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetResourceContentParams {
  frame_id : PageFrameId
  url : String
} derive(Eq, Debug)

///|
/// Encode PageGetResourceContentParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageGetResourceContentParams::to_json(
  self : PageGetResourceContentParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Generated CDP value object for PageGetResourceContentResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetResourceContentResult {
  content : String
  base64_encoded : Bool
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getResourceContent.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetResourceContentResult::from_json(
  result : Json,
) -> PageGetResourceContentResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getResourceContent"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    content: typed_required_result_string(
      method_name, fields, "content", "string",
    ),
    base64_encoded: typed_required_result_bool(
      method_name, fields, "base64Encoded", "boolean",
    ),
  }
}

///|
/// Build a typed Page.getResourceContent command envelope.
///
/// Pass a PageGetResourceContentParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_resource_content_command(id=1, params, session_id=None)
/// ```
pub fn page_get_resource_content_command(
  id~ : Int,
  params : PageGetResourceContentParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getResourceContent",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageGetResourceTreeResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageGetResourceTreeResult {
  frame_tree : PageFrameResourceTree
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.getResourceTree.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageGetResourceTreeResult::from_json(
  result : Json,
) -> PageGetResourceTreeResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.getResourceTree"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    frame_tree: PageFrameResourceTree::from_json(
      typed_required_result_json(method_name, fields, "frameTree"),
    ),
  }
}

///|
/// Build a typed Page.getResourceTree command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_get_resource_tree_command(id=1, session_id=None)
/// ```
pub fn page_get_resource_tree_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.getResourceTree",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageHandleJavaScriptDialogParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageHandleJavaScriptDialogParams {
  accept : Bool
  prompt_text : String?
} derive(Eq, Debug)

///|
/// Encode PageHandleJavaScriptDialogParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageHandleJavaScriptDialogParams::to_json(
  self : PageHandleJavaScriptDialogParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["accept"] = Json::boolean(self.accept)
  if self.prompt_text is Some(value) {
    fields["promptText"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.handleJavaScriptDialog command envelope.
///
/// Pass a PageHandleJavaScriptDialogParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_handle_java_script_dialog_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_handle_java_script_dialog_command(
  id~ : Int,
  params : PageHandleJavaScriptDialogParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.handleJavaScriptDialog",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageNavigateParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigateParams {
  url : String
  referrer : String?
  transition_type : PageTransitionType?
  frame_id : PageFrameId?
  referrer_policy : PageReferrerPolicy?
} derive(Eq, Debug)

///|
/// Encode PageNavigateParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageNavigateParams::to_json(self : PageNavigateParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  if self.referrer is Some(value) {
    fields["referrer"] = Json::string(value)
  }
  if self.transition_type is Some(value) {
    fields["transitionType"] = value.to_json()
  }
  if self.frame_id is Some(value) {
    fields["frameId"] = value.to_json()
  }
  if self.referrer_policy is Some(value) {
    fields["referrerPolicy"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageNavigateResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigateResult {
  frame_id : PageFrameId
  loader_id : NetworkLoaderId?
  error_text : String?
  is_download : Bool?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.navigate.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageNavigateResult::from_json(
  result : Json,
) -> PageNavigateResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.navigate"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    frame_id: PageFrameId::from_json(
      typed_required_result_json(method_name, fields, "frameId"),
    ),
    loader_id: match typed_optional_result_json(fields, "loaderId") {
      Some(value) => Some(NetworkLoaderId::from_json(value))
      None => None
    },
    error_text: typed_optional_result_string(
      method_name, fields, "errorText", "string",
    ),
    is_download: typed_optional_result_bool(
      method_name, fields, "isDownload", "boolean",
    ),
  }
}

///|
/// Build a typed Page.navigate command envelope.
///
/// Pass a PageNavigateParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_navigate_command(id=1, params, session_id=None)
/// ```
pub fn page_navigate_command(
  id~ : Int,
  params : PageNavigateParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.navigate",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageNavigateToHistoryEntryParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigateToHistoryEntryParams {
  entry_id : Int
} derive(Eq, Debug)

///|
/// Encode PageNavigateToHistoryEntryParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageNavigateToHistoryEntryParams::to_json(
  self : PageNavigateToHistoryEntryParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["entryId"] = Json::number(self.entry_id.to_double())
  Json::object(fields)
}

///|
/// Build a typed Page.navigateToHistoryEntry command envelope.
///
/// Pass a PageNavigateToHistoryEntryParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_navigate_to_history_entry_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_navigate_to_history_entry_command(
  id~ : Int,
  params : PageNavigateToHistoryEntryParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.navigateToHistoryEntry",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PagePrintToPDFParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePrintToPDFParams {
  landscape : Bool?
  display_header_footer : Bool?
  print_background : Bool?
  scale : Double?
  paper_width : Double?
  paper_height : Double?
  margin_top : Double?
  margin_bottom : Double?
  margin_left : Double?
  margin_right : Double?
  page_ranges : String?
  header_template : String?
  footer_template : String?
  prefer_csspage_size : Bool?
  transfer_mode : String?
  generate_tagged_pdf : Bool?
  generate_document_outline : Bool?
} derive(Eq, Debug)

///|
/// Encode PagePrintToPDFParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PagePrintToPDFParams::to_json(self : PagePrintToPDFParams) -> Json {
  let fields : Map[String, Json] = {}
  if self.landscape is Some(value) {
    fields["landscape"] = Json::boolean(value)
  }
  if self.display_header_footer is Some(value) {
    fields["displayHeaderFooter"] = Json::boolean(value)
  }
  if self.print_background is Some(value) {
    fields["printBackground"] = Json::boolean(value)
  }
  if self.scale is Some(value) {
    fields["scale"] = Json::number(value)
  }
  if self.paper_width is Some(value) {
    fields["paperWidth"] = Json::number(value)
  }
  if self.paper_height is Some(value) {
    fields["paperHeight"] = Json::number(value)
  }
  if self.margin_top is Some(value) {
    fields["marginTop"] = Json::number(value)
  }
  if self.margin_bottom is Some(value) {
    fields["marginBottom"] = Json::number(value)
  }
  if self.margin_left is Some(value) {
    fields["marginLeft"] = Json::number(value)
  }
  if self.margin_right is Some(value) {
    fields["marginRight"] = Json::number(value)
  }
  if self.page_ranges is Some(value) {
    fields["pageRanges"] = Json::string(value)
  }
  if self.header_template is Some(value) {
    fields["headerTemplate"] = Json::string(value)
  }
  if self.footer_template is Some(value) {
    fields["footerTemplate"] = Json::string(value)
  }
  if self.prefer_csspage_size is Some(value) {
    fields["preferCSSPageSize"] = Json::boolean(value)
  }
  if self.transfer_mode is Some(value) {
    fields["transferMode"] = Json::string(value)
  }
  if self.generate_tagged_pdf is Some(value) {
    fields["generateTaggedPDF"] = Json::boolean(value)
  }
  if self.generate_document_outline is Some(value) {
    fields["generateDocumentOutline"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PagePrintToPDFResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PagePrintToPDFResult {
  data : String
  stream : IOStreamHandle?
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.printToPDF.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PagePrintToPDFResult::from_json(
  result : Json,
) -> PagePrintToPDFResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.printToPDF"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    data: typed_required_result_string(method_name, fields, "data", "string"),
    stream: match typed_optional_result_json(fields, "stream") {
      Some(value) => Some(IOStreamHandle::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed Page.printToPDF command envelope.
///
/// Pass a PagePrintToPDFParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_print_to_pdf_command(id=1, params, session_id=None)
/// ```
pub fn page_print_to_pdf_command(
  id~ : Int,
  params : PagePrintToPDFParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.printToPDF",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageProduceCompilationCacheParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageProduceCompilationCacheParams {
  scripts : Array[PageCompilationCacheParams]
} derive(Eq, Debug)

///|
/// Encode PageProduceCompilationCacheParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageProduceCompilationCacheParams::to_json(
  self : PageProduceCompilationCacheParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["scripts"] = Json::array(self.scripts.map(item => item.to_json()))
  Json::object(fields)
}

///|
/// Build a typed Page.produceCompilationCache command envelope.
///
/// Pass a PageProduceCompilationCacheParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_produce_compilation_cache_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_produce_compilation_cache_command(
  id~ : Int,
  params : PageProduceCompilationCacheParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.produceCompilationCache",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageReloadParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageReloadParams {
  ignore_cache : Bool?
  script_to_evaluate_on_load : String?
  loader_id : NetworkLoaderId?
} derive(Eq, Debug)

///|
/// Encode PageReloadParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageReloadParams::to_json(self : PageReloadParams) -> Json {
  let fields : Map[String, Json] = {}
  if self.ignore_cache is Some(value) {
    fields["ignoreCache"] = Json::boolean(value)
  }
  if self.script_to_evaluate_on_load is Some(value) {
    fields["scriptToEvaluateOnLoad"] = Json::string(value)
  }
  if self.loader_id is Some(value) {
    fields["loaderId"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Build a typed Page.reload command envelope.
///
/// Pass a PageReloadParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_reload_command(id=1, params, session_id=None)
/// ```
pub fn page_reload_command(
  id~ : Int,
  params : PageReloadParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.reload",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageRemoveScriptToEvaluateOnLoadParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageRemoveScriptToEvaluateOnLoadParams {
  identifier : PageScriptIdentifier
} derive(Eq, Debug)

///|
/// Encode PageRemoveScriptToEvaluateOnLoadParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageRemoveScriptToEvaluateOnLoadParams::to_json(
  self : PageRemoveScriptToEvaluateOnLoadParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["identifier"] = self.identifier.to_json()
  Json::object(fields)
}

///|
/// Build a typed Page.removeScriptToEvaluateOnLoad command envelope.
///
/// Pass a PageRemoveScriptToEvaluateOnLoadParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_remove_script_to_evaluate_on_load_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_remove_script_to_evaluate_on_load_command(
  id~ : Int,
  params : PageRemoveScriptToEvaluateOnLoadParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.removeScriptToEvaluateOnLoad",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageRemoveScriptToEvaluateOnNewDocumentParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageRemoveScriptToEvaluateOnNewDocumentParams {
  identifier : PageScriptIdentifier
} derive(Eq, Debug)

///|
/// Encode PageRemoveScriptToEvaluateOnNewDocumentParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageRemoveScriptToEvaluateOnNewDocumentParams::to_json(
  self : PageRemoveScriptToEvaluateOnNewDocumentParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["identifier"] = self.identifier.to_json()
  Json::object(fields)
}

///|
/// Build a typed Page.removeScriptToEvaluateOnNewDocument command envelope.
///
/// Pass a PageRemoveScriptToEvaluateOnNewDocumentParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_remove_script_to_evaluate_on_new_document_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_remove_script_to_evaluate_on_new_document_command(
  id~ : Int,
  params : PageRemoveScriptToEvaluateOnNewDocumentParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.removeScriptToEvaluateOnNewDocument",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.resetNavigationHistory command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_reset_navigation_history_command(id=1, session_id=None)
/// ```
pub fn page_reset_navigation_history_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.resetNavigationHistory",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageScreencastFrameAckParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScreencastFrameAckParams {
  session_id : Int
} derive(Eq, Debug)

///|
/// Encode PageScreencastFrameAckParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScreencastFrameAckParams::to_json(
  self : PageScreencastFrameAckParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["sessionId"] = Json::number(self.session_id.to_double())
  Json::object(fields)
}

///|
/// Build a typed Page.screencastFrameAck command envelope.
///
/// Pass a PageScreencastFrameAckParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_screencast_frame_ack_command(id=1, params, session_id=None)
/// ```
pub fn page_screencast_frame_ack_command(
  id~ : Int,
  params : PageScreencastFrameAckParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.screencastFrameAck",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSearchInResourceParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSearchInResourceParams {
  frame_id : PageFrameId
  url : String
  query : String
  case_sensitive : Bool?
  is_regex : Bool?
} derive(Eq, Debug)

///|
/// Encode PageSearchInResourceParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSearchInResourceParams::to_json(
  self : PageSearchInResourceParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["url"] = Json::string(self.url)
  fields["query"] = Json::string(self.query)
  if self.case_sensitive is Some(value) {
    fields["caseSensitive"] = Json::boolean(value)
  }
  if self.is_regex is Some(value) {
    fields["isRegex"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PageSearchInResourceResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSearchInResourceResult {
  result : Array[DebuggerSearchMatch]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Page.searchInResource.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PageSearchInResourceResult::from_json(
  result : Json,
) -> PageSearchInResourceResult raise @protocol.ProtocolSchemaError {
  let method_name = "Page.searchInResource"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    result: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "result"),
      "result",
      "array<Debugger.SearchMatch>",
    ).map(item => DebuggerSearchMatch::from_json(item)),
  }
}

///|
/// Build a typed Page.searchInResource command envelope.
///
/// Pass a PageSearchInResourceParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_search_in_resource_command(id=1, params, session_id=None)
/// ```
pub fn page_search_in_resource_command(
  id~ : Int,
  params : PageSearchInResourceParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.searchInResource",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetAdBlockingEnabledParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetAdBlockingEnabledParams {
  enabled : Bool
} derive(Eq, Debug)

///|
/// Encode PageSetAdBlockingEnabledParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetAdBlockingEnabledParams::to_json(
  self : PageSetAdBlockingEnabledParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["enabled"] = Json::boolean(self.enabled)
  Json::object(fields)
}

///|
/// Build a typed Page.setAdBlockingEnabled command envelope.
///
/// Pass a PageSetAdBlockingEnabledParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_ad_blocking_enabled_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_ad_blocking_enabled_command(
  id~ : Int,
  params : PageSetAdBlockingEnabledParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setAdBlockingEnabled",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetBypassCSPParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetBypassCSPParams {
  enabled : Bool
} derive(Eq, Debug)

///|
/// Encode PageSetBypassCSPParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetBypassCSPParams::to_json(self : PageSetBypassCSPParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["enabled"] = Json::boolean(self.enabled)
  Json::object(fields)
}

///|
/// Build a typed Page.setBypassCSP command envelope.
///
/// Pass a PageSetBypassCSPParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_bypass_csp_command(id=1, params, session_id=None)
/// ```
pub fn page_set_bypass_csp_command(
  id~ : Int,
  params : PageSetBypassCSPParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setBypassCSP",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetDeviceMetricsOverrideParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetDeviceMetricsOverrideParams {
  width : Int
  height : Int
  device_scale_factor : Double
  mobile : Bool
  scale : Double?
  screen_width : Int?
  screen_height : Int?
  position_x : Int?
  position_y : Int?
  dont_set_visible_size : Bool?
  screen_orientation : EmulationScreenOrientation?
  viewport : PageViewport?
} derive(Eq, Debug)

///|
/// Encode PageSetDeviceMetricsOverrideParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetDeviceMetricsOverrideParams::to_json(
  self : PageSetDeviceMetricsOverrideParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["width"] = Json::number(self.width.to_double())
  fields["height"] = Json::number(self.height.to_double())
  fields["deviceScaleFactor"] = Json::number(self.device_scale_factor)
  fields["mobile"] = Json::boolean(self.mobile)
  if self.scale is Some(value) {
    fields["scale"] = Json::number(value)
  }
  if self.screen_width is Some(value) {
    fields["screenWidth"] = Json::number(value.to_double())
  }
  if self.screen_height is Some(value) {
    fields["screenHeight"] = Json::number(value.to_double())
  }
  if self.position_x is Some(value) {
    fields["positionX"] = Json::number(value.to_double())
  }
  if self.position_y is Some(value) {
    fields["positionY"] = Json::number(value.to_double())
  }
  if self.dont_set_visible_size is Some(value) {
    fields["dontSetVisibleSize"] = Json::boolean(value)
  }
  if self.screen_orientation is Some(value) {
    fields["screenOrientation"] = value.to_json()
  }
  if self.viewport is Some(value) {
    fields["viewport"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setDeviceMetricsOverride command envelope.
///
/// Pass a PageSetDeviceMetricsOverrideParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_device_metrics_override_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_device_metrics_override_command(
  id~ : Int,
  params : PageSetDeviceMetricsOverrideParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setDeviceMetricsOverride",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetDeviceOrientationOverrideParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetDeviceOrientationOverrideParams {
  alpha : Double
  beta : Double
  gamma : Double
} derive(Eq, Debug)

///|
/// Encode PageSetDeviceOrientationOverrideParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetDeviceOrientationOverrideParams::to_json(
  self : PageSetDeviceOrientationOverrideParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["alpha"] = Json::number(self.alpha)
  fields["beta"] = Json::number(self.beta)
  fields["gamma"] = Json::number(self.gamma)
  Json::object(fields)
}

///|
/// Build a typed Page.setDeviceOrientationOverride command envelope.
///
/// Pass a PageSetDeviceOrientationOverrideParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_device_orientation_override_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_device_orientation_override_command(
  id~ : Int,
  params : PageSetDeviceOrientationOverrideParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setDeviceOrientationOverride",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetDocumentContentParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetDocumentContentParams {
  frame_id : PageFrameId
  html : String
} derive(Eq, Debug)

///|
/// Encode PageSetDocumentContentParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetDocumentContentParams::to_json(
  self : PageSetDocumentContentParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["html"] = Json::string(self.html)
  Json::object(fields)
}

///|
/// Build a typed Page.setDocumentContent command envelope.
///
/// Pass a PageSetDocumentContentParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_document_content_command(id=1, params, session_id=None)
/// ```
pub fn page_set_document_content_command(
  id~ : Int,
  params : PageSetDocumentContentParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setDocumentContent",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetDownloadBehaviorParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetDownloadBehaviorParams {
  behavior : String
  download_path : String?
} derive(Eq, Debug)

///|
/// Encode PageSetDownloadBehaviorParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetDownloadBehaviorParams::to_json(
  self : PageSetDownloadBehaviorParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["behavior"] = Json::string(self.behavior)
  if self.download_path is Some(value) {
    fields["downloadPath"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setDownloadBehavior command envelope.
///
/// Pass a PageSetDownloadBehaviorParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_download_behavior_command(id=1, params, session_id=None)
/// ```
pub fn page_set_download_behavior_command(
  id~ : Int,
  params : PageSetDownloadBehaviorParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setDownloadBehavior",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetFontFamiliesParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetFontFamiliesParams {
  font_families : PageFontFamilies
  for_scripts : Array[PageScriptFontFamilies]?
} derive(Eq, Debug)

///|
/// Encode PageSetFontFamiliesParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetFontFamiliesParams::to_json(
  self : PageSetFontFamiliesParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["fontFamilies"] = self.font_families.to_json()
  if self.for_scripts is Some(value) {
    fields["forScripts"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setFontFamilies command envelope.
///
/// Pass a PageSetFontFamiliesParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_font_families_command(id=1, params, session_id=None)
/// ```
pub fn page_set_font_families_command(
  id~ : Int,
  params : PageSetFontFamiliesParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setFontFamilies",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetFontSizesParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetFontSizesParams {
  font_sizes : PageFontSizes
} derive(Eq, Debug)

///|
/// Encode PageSetFontSizesParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetFontSizesParams::to_json(self : PageSetFontSizesParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["fontSizes"] = self.font_sizes.to_json()
  Json::object(fields)
}

///|
/// Build a typed Page.setFontSizes command envelope.
///
/// Pass a PageSetFontSizesParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_font_sizes_command(id=1, params, session_id=None)
/// ```
pub fn page_set_font_sizes_command(
  id~ : Int,
  params : PageSetFontSizesParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setFontSizes",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetGeolocationOverrideParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetGeolocationOverrideParams {
  latitude : Double?
  longitude : Double?
  accuracy : Double?
} derive(Eq, Debug)

///|
/// Encode PageSetGeolocationOverrideParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetGeolocationOverrideParams::to_json(
  self : PageSetGeolocationOverrideParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.latitude is Some(value) {
    fields["latitude"] = Json::number(value)
  }
  if self.longitude is Some(value) {
    fields["longitude"] = Json::number(value)
  }
  if self.accuracy is Some(value) {
    fields["accuracy"] = Json::number(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setGeolocationOverride command envelope.
///
/// Pass a PageSetGeolocationOverrideParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_geolocation_override_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_geolocation_override_command(
  id~ : Int,
  params : PageSetGeolocationOverrideParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setGeolocationOverride",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetInterceptFileChooserDialogParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetInterceptFileChooserDialogParams {
  enabled : Bool
  cancel : Bool?
} derive(Eq, Debug)

///|
/// Encode PageSetInterceptFileChooserDialogParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetInterceptFileChooserDialogParams::to_json(
  self : PageSetInterceptFileChooserDialogParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["enabled"] = Json::boolean(self.enabled)
  if self.cancel is Some(value) {
    fields["cancel"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setInterceptFileChooserDialog command envelope.
///
/// Pass a PageSetInterceptFileChooserDialogParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_intercept_file_chooser_dialog_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_intercept_file_chooser_dialog_command(
  id~ : Int,
  params : PageSetInterceptFileChooserDialogParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setInterceptFileChooserDialog",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetLifecycleEventsEnabledParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetLifecycleEventsEnabledParams {
  enabled : Bool
} derive(Eq, Debug)

///|
/// Encode PageSetLifecycleEventsEnabledParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetLifecycleEventsEnabledParams::to_json(
  self : PageSetLifecycleEventsEnabledParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["enabled"] = Json::boolean(self.enabled)
  Json::object(fields)
}

///|
/// Build a typed Page.setLifecycleEventsEnabled command envelope.
///
/// Pass a PageSetLifecycleEventsEnabledParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_lifecycle_events_enabled_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_lifecycle_events_enabled_command(
  id~ : Int,
  params : PageSetLifecycleEventsEnabledParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setLifecycleEventsEnabled",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetPrerenderingAllowedParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetPrerenderingAllowedParams {
  is_allowed : Bool
} derive(Eq, Debug)

///|
/// Encode PageSetPrerenderingAllowedParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetPrerenderingAllowedParams::to_json(
  self : PageSetPrerenderingAllowedParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["isAllowed"] = Json::boolean(self.is_allowed)
  Json::object(fields)
}

///|
/// Build a typed Page.setPrerenderingAllowed command envelope.
///
/// Pass a PageSetPrerenderingAllowedParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_prerendering_allowed_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_prerendering_allowed_command(
  id~ : Int,
  params : PageSetPrerenderingAllowedParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setPrerenderingAllowed",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetRPHRegistrationModeParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetRPHRegistrationModeParams {
  mode : String
} derive(Eq, Debug)

///|
/// Encode PageSetRPHRegistrationModeParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetRPHRegistrationModeParams::to_json(
  self : PageSetRPHRegistrationModeParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["mode"] = Json::string(self.mode)
  Json::object(fields)
}

///|
/// Build a typed Page.setRPHRegistrationMode command envelope.
///
/// Pass a PageSetRPHRegistrationModeParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_rphregistration_mode_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_rphregistration_mode_command(
  id~ : Int,
  params : PageSetRPHRegistrationModeParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setRPHRegistrationMode",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetSPCTransactionModeParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetSPCTransactionModeParams {
  mode : String
} derive(Eq, Debug)

///|
/// Encode PageSetSPCTransactionModeParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetSPCTransactionModeParams::to_json(
  self : PageSetSPCTransactionModeParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["mode"] = Json::string(self.mode)
  Json::object(fields)
}

///|
/// Build a typed Page.setSPCTransactionMode command envelope.
///
/// Pass a PageSetSPCTransactionModeParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_spctransaction_mode_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_spctransaction_mode_command(
  id~ : Int,
  params : PageSetSPCTransactionModeParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setSPCTransactionMode",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetTouchEmulationEnabledParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetTouchEmulationEnabledParams {
  enabled : Bool
  configuration : String?
} derive(Eq, Debug)

///|
/// Encode PageSetTouchEmulationEnabledParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetTouchEmulationEnabledParams::to_json(
  self : PageSetTouchEmulationEnabledParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["enabled"] = Json::boolean(self.enabled)
  if self.configuration is Some(value) {
    fields["configuration"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Build a typed Page.setTouchEmulationEnabled command envelope.
///
/// Pass a PageSetTouchEmulationEnabledParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_touch_emulation_enabled_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_touch_emulation_enabled_command(
  id~ : Int,
  params : PageSetTouchEmulationEnabledParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setTouchEmulationEnabled",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageSetWebLifecycleStateParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageSetWebLifecycleStateParams {
  state : String
} derive(Eq, Debug)

///|
/// Encode PageSetWebLifecycleStateParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageSetWebLifecycleStateParams::to_json(
  self : PageSetWebLifecycleStateParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["state"] = Json::string(self.state)
  Json::object(fields)
}

///|
/// Build a typed Page.setWebLifecycleState command envelope.
///
/// Pass a PageSetWebLifecycleStateParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_set_web_lifecycle_state_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_set_web_lifecycle_state_command(
  id~ : Int,
  params : PageSetWebLifecycleStateParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.setWebLifecycleState",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageStartScreencastParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageStartScreencastParams {
  format : String?
  quality : Int?
  max_width : Int?
  max_height : Int?
  every_nth_frame : Int?
} derive(Eq, Debug)

///|
/// Encode PageStartScreencastParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageStartScreencastParams::to_json(
  self : PageStartScreencastParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.format is Some(value) {
    fields["format"] = Json::string(value)
  }
  if self.quality is Some(value) {
    fields["quality"] = Json::number(value.to_double())
  }
  if self.max_width is Some(value) {
    fields["maxWidth"] = Json::number(value.to_double())
  }
  if self.max_height is Some(value) {
    fields["maxHeight"] = Json::number(value.to_double())
  }
  if self.every_nth_frame is Some(value) {
    fields["everyNthFrame"] = Json::number(value.to_double())
  }
  Json::object(fields)
}

///|
/// Build a typed Page.startScreencast command envelope.
///
/// Pass a PageStartScreencastParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_start_screencast_command(id=1, params, session_id=None)
/// ```
pub fn page_start_screencast_command(
  id~ : Int,
  params : PageStartScreencastParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.startScreencast",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.stopLoading command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_stop_loading_command(id=1, session_id=None)
/// ```
pub fn page_stop_loading_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Page.stopLoading", session_id?)
}

///|
/// Build a typed Page.stopScreencast command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_stop_screencast_command(id=1, session_id=None)
/// ```
pub fn page_stop_screencast_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.stopScreencast",
    session_id?,
  )
}

///|
/// Build a typed Page.waitForDebugger command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = page_wait_for_debugger_command(id=1, session_id=None)
/// ```
pub fn page_wait_for_debugger_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Page.waitForDebugger",
    session_id?,
  )
}

///|
/// Generated CDP value object for PageBackForwardCacheNotUsedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageBackForwardCacheNotUsedEventParams {
  loader_id : NetworkLoaderId
  frame_id : PageFrameId
  not_restored_explanations : Array[PageBackForwardCacheNotRestoredExplanation]
  not_restored_explanations_tree : PageBackForwardCacheNotRestoredExplanationTree?
} derive(Eq, Debug)

///|
/// Encode PageBackForwardCacheNotUsedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageBackForwardCacheNotUsedEventParams::to_json(
  self : PageBackForwardCacheNotUsedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["loaderId"] = self.loader_id.to_json()
  fields["frameId"] = self.frame_id.to_json()
  fields["notRestoredExplanations"] = Json::array(
    self.not_restored_explanations.map(item => item.to_json()),
  )
  if self.not_restored_explanations_tree is Some(value) {
    fields["notRestoredExplanationsTree"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode and validate Page.backForwardCacheNotUsed event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageBackForwardCacheNotUsedEventParams::from_json(
  params : Json,
) -> PageBackForwardCacheNotUsedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.backForwardCacheNotUsed"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    loader_id: NetworkLoaderId::from_json(
      typed_required_json(method_name, fields, "loaderId"),
    ),
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    not_restored_explanations: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "notRestoredExplanations"),
      "notRestoredExplanations",
      "array<BackForwardCacheNotRestoredExplanation>",
    ).map(item => PageBackForwardCacheNotRestoredExplanation::from_json(item)),
    not_restored_explanations_tree: match
      typed_optional_json(fields, "notRestoredExplanationsTree") {
      Some(value) =>
        Some(PageBackForwardCacheNotRestoredExplanationTree::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed Page.backForwardCacheNotUsed event envelope.
///
/// Pass a PageBackForwardCacheNotUsedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_back_forward_cache_not_used_event(params, session_id=None)
/// ```
pub fn page_back_forward_cache_not_used_event(
  params : PageBackForwardCacheNotUsedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.backForwardCacheNotUsed",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageCompilationCacheProducedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageCompilationCacheProducedEventParams {
  url : String
  data : String
} derive(Eq, Debug)

///|
/// Encode PageCompilationCacheProducedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageCompilationCacheProducedEventParams::to_json(
  self : PageCompilationCacheProducedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["data"] = Json::string(self.data)
  Json::object(fields)
}

///|
/// Decode and validate Page.compilationCacheProduced event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageCompilationCacheProducedEventParams::from_json(
  params : Json,
) -> PageCompilationCacheProducedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.compilationCacheProduced"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    data: typed_required_string(method_name, fields, "data", "string"),
  }
}

///|
/// Build a typed Page.compilationCacheProduced event envelope.
///
/// Pass a PageCompilationCacheProducedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_compilation_cache_produced_event(params, session_id=None)
/// ```
pub fn page_compilation_cache_produced_event(
  params : PageCompilationCacheProducedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.compilationCacheProduced",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageDocumentOpenedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDocumentOpenedEventParams {
  frame : PageFrame
} derive(Eq, Debug)

///|
/// Encode PageDocumentOpenedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDocumentOpenedEventParams::to_json(
  self : PageDocumentOpenedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frame"] = self.frame.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.documentOpened event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageDocumentOpenedEventParams::from_json(
  params : Json,
) -> PageDocumentOpenedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.documentOpened"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame: PageFrame::from_json(
      typed_required_json(method_name, fields, "frame"),
    ),
  }
}

///|
/// Build a typed Page.documentOpened event envelope.
///
/// Pass a PageDocumentOpenedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_document_opened_event(params, session_id=None)
/// ```
pub fn page_document_opened_event(
  params : PageDocumentOpenedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.documentOpened",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageDomContentEventFiredEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDomContentEventFiredEventParams {
  timestamp : NetworkMonotonicTime
} derive(Eq, Debug)

///|
/// Encode PageDomContentEventFiredEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDomContentEventFiredEventParams::to_json(
  self : PageDomContentEventFiredEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["timestamp"] = self.timestamp.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.domContentEventFired event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageDomContentEventFiredEventParams::from_json(
  params : Json,
) -> PageDomContentEventFiredEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.domContentEventFired"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    timestamp: NetworkMonotonicTime::from_json(
      typed_required_json(method_name, fields, "timestamp"),
    ),
  }
}

///|
/// Build a typed Page.domContentEventFired event envelope.
///
/// Pass a PageDomContentEventFiredEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_dom_content_event_fired_event(params, session_id=None)
/// ```
pub fn page_dom_content_event_fired_event(
  params : PageDomContentEventFiredEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.domContentEventFired",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageDownloadProgressEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDownloadProgressEventParams {
  guid : String
  total_bytes : Double
  received_bytes : Double
  state : String
} derive(Eq, Debug)

///|
/// Encode PageDownloadProgressEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDownloadProgressEventParams::to_json(
  self : PageDownloadProgressEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["guid"] = Json::string(self.guid)
  fields["totalBytes"] = Json::number(self.total_bytes)
  fields["receivedBytes"] = Json::number(self.received_bytes)
  fields["state"] = Json::string(self.state)
  Json::object(fields)
}

///|
/// Decode and validate Page.downloadProgress event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageDownloadProgressEventParams::from_json(
  params : Json,
) -> PageDownloadProgressEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.downloadProgress"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    guid: typed_required_string(method_name, fields, "guid", "string"),
    total_bytes: typed_required_double(
      method_name, fields, "totalBytes", "number",
    ),
    received_bytes: typed_required_double(
      method_name, fields, "receivedBytes", "number",
    ),
    state: typed_required_string(method_name, fields, "state", "string"),
  }
}

///|
/// Build a typed Page.downloadProgress event envelope.
///
/// Pass a PageDownloadProgressEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_download_progress_event(params, session_id=None)
/// ```
pub fn page_download_progress_event(
  params : PageDownloadProgressEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.downloadProgress",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageDownloadWillBeginEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageDownloadWillBeginEventParams {
  frame_id : PageFrameId
  guid : String
  url : String
  suggested_filename : String
} derive(Eq, Debug)

///|
/// Encode PageDownloadWillBeginEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageDownloadWillBeginEventParams::to_json(
  self : PageDownloadWillBeginEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["guid"] = Json::string(self.guid)
  fields["url"] = Json::string(self.url)
  fields["suggestedFilename"] = Json::string(self.suggested_filename)
  Json::object(fields)
}

///|
/// Decode and validate Page.downloadWillBegin event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageDownloadWillBeginEventParams::from_json(
  params : Json,
) -> PageDownloadWillBeginEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.downloadWillBegin"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    guid: typed_required_string(method_name, fields, "guid", "string"),
    url: typed_required_string(method_name, fields, "url", "string"),
    suggested_filename: typed_required_string(
      method_name, fields, "suggestedFilename", "string",
    ),
  }
}

///|
/// Build a typed Page.downloadWillBegin event envelope.
///
/// Pass a PageDownloadWillBeginEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_download_will_begin_event(params, session_id=None)
/// ```
pub fn page_download_will_begin_event(
  params : PageDownloadWillBeginEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.downloadWillBegin",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFileChooserOpenedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFileChooserOpenedEventParams {
  frame_id : PageFrameId
  mode : String
  backend_node_id : DOMBackendNodeId?
} derive(Eq, Debug)

///|
/// Encode PageFileChooserOpenedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFileChooserOpenedEventParams::to_json(
  self : PageFileChooserOpenedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["mode"] = Json::string(self.mode)
  if self.backend_node_id is Some(value) {
    fields["backendNodeId"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode and validate Page.fileChooserOpened event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFileChooserOpenedEventParams::from_json(
  params : Json,
) -> PageFileChooserOpenedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.fileChooserOpened"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    mode: typed_required_string(method_name, fields, "mode", "string"),
    backend_node_id: match typed_optional_json(fields, "backendNodeId") {
      Some(value) => Some(DOMBackendNodeId::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed Page.fileChooserOpened event envelope.
///
/// Pass a PageFileChooserOpenedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_file_chooser_opened_event(params, session_id=None)
/// ```
pub fn page_file_chooser_opened_event(
  params : PageFileChooserOpenedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.fileChooserOpened",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameAttachedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameAttachedEventParams {
  frame_id : PageFrameId
  parent_frame_id : PageFrameId
  stack : RuntimeStackTrace?
} derive(Eq, Debug)

///|
/// Encode PageFrameAttachedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameAttachedEventParams::to_json(
  self : PageFrameAttachedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["parentFrameId"] = self.parent_frame_id.to_json()
  if self.stack is Some(value) {
    fields["stack"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode and validate Page.frameAttached event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameAttachedEventParams::from_json(
  params : Json,
) -> PageFrameAttachedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameAttached"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    parent_frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "parentFrameId"),
    ),
    stack: match typed_optional_json(fields, "stack") {
      Some(value) => Some(RuntimeStackTrace::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed Page.frameAttached event envelope.
///
/// Pass a PageFrameAttachedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_attached_event(params, session_id=None)
/// ```
pub fn page_frame_attached_event(
  params : PageFrameAttachedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameAttached",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameClearedScheduledNavigationEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameClearedScheduledNavigationEventParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageFrameClearedScheduledNavigationEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameClearedScheduledNavigationEventParams::to_json(
  self : PageFrameClearedScheduledNavigationEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameClearedScheduledNavigation event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameClearedScheduledNavigationEventParams::from_json(
  params : Json,
) -> PageFrameClearedScheduledNavigationEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameClearedScheduledNavigation"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
  }
}

///|
/// Build a typed Page.frameClearedScheduledNavigation event envelope.
///
/// Pass a PageFrameClearedScheduledNavigationEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_cleared_scheduled_navigation_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn page_frame_cleared_scheduled_navigation_event(
  params : PageFrameClearedScheduledNavigationEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameClearedScheduledNavigation",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameDetachedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameDetachedEventParams {
  frame_id : PageFrameId
  reason : String
} derive(Eq, Debug)

///|
/// Encode PageFrameDetachedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameDetachedEventParams::to_json(
  self : PageFrameDetachedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["reason"] = Json::string(self.reason)
  Json::object(fields)
}

///|
/// Decode and validate Page.frameDetached event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameDetachedEventParams::from_json(
  params : Json,
) -> PageFrameDetachedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameDetached"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    reason: typed_required_string(method_name, fields, "reason", "string"),
  }
}

///|
/// Build a typed Page.frameDetached event envelope.
///
/// Pass a PageFrameDetachedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_detached_event(params, session_id=None)
/// ```
pub fn page_frame_detached_event(
  params : PageFrameDetachedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameDetached",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameNavigatedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameNavigatedEventParams {
  frame : PageFrame
  type_ : PageNavigationType
} derive(Eq, Debug)

///|
/// Encode PageFrameNavigatedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameNavigatedEventParams::to_json(
  self : PageFrameNavigatedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frame"] = self.frame.to_json()
  fields["type"] = self.type_.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameNavigated event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameNavigatedEventParams::from_json(
  params : Json,
) -> PageFrameNavigatedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameNavigated"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame: PageFrame::from_json(
      typed_required_json(method_name, fields, "frame"),
    ),
    type_: PageNavigationType::from_json(
      typed_required_json(method_name, fields, "type"),
    ),
  }
}

///|
/// Build a typed Page.frameNavigated event envelope.
///
/// Pass a PageFrameNavigatedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_navigated_event(params, session_id=None)
/// ```
pub fn page_frame_navigated_event(
  params : PageFrameNavigatedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameNavigated",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameRequestedNavigationEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameRequestedNavigationEventParams {
  frame_id : PageFrameId
  reason : PageClientNavigationReason
  url : String
  disposition : PageClientNavigationDisposition
} derive(Eq, Debug)

///|
/// Encode PageFrameRequestedNavigationEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameRequestedNavigationEventParams::to_json(
  self : PageFrameRequestedNavigationEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["reason"] = self.reason.to_json()
  fields["url"] = Json::string(self.url)
  fields["disposition"] = self.disposition.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameRequestedNavigation event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameRequestedNavigationEventParams::from_json(
  params : Json,
) -> PageFrameRequestedNavigationEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameRequestedNavigation"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    reason: PageClientNavigationReason::from_json(
      typed_required_json(method_name, fields, "reason"),
    ),
    url: typed_required_string(method_name, fields, "url", "string"),
    disposition: PageClientNavigationDisposition::from_json(
      typed_required_json(method_name, fields, "disposition"),
    ),
  }
}

///|
/// Build a typed Page.frameRequestedNavigation event envelope.
///
/// Pass a PageFrameRequestedNavigationEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_requested_navigation_event(params, session_id=None)
/// ```
pub fn page_frame_requested_navigation_event(
  params : PageFrameRequestedNavigationEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameRequestedNavigation",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.frameResized event envelope.
///
/// The event has no parameters. The builder validates the event name against
/// the bundled protocol schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_resized_event(session_id=None)
/// ```
pub fn page_frame_resized_event(
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(method_name="Page.frameResized", session_id?)
}

///|
/// Generated CDP value object for PageFrameScheduledNavigationEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameScheduledNavigationEventParams {
  frame_id : PageFrameId
  delay : Double
  reason : PageClientNavigationReason
  url : String
} derive(Eq, Debug)

///|
/// Encode PageFrameScheduledNavigationEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameScheduledNavigationEventParams::to_json(
  self : PageFrameScheduledNavigationEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["delay"] = Json::number(self.delay)
  fields["reason"] = self.reason.to_json()
  fields["url"] = Json::string(self.url)
  Json::object(fields)
}

///|
/// Decode and validate Page.frameScheduledNavigation event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameScheduledNavigationEventParams::from_json(
  params : Json,
) -> PageFrameScheduledNavigationEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameScheduledNavigation"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    delay: typed_required_double(method_name, fields, "delay", "number"),
    reason: PageClientNavigationReason::from_json(
      typed_required_json(method_name, fields, "reason"),
    ),
    url: typed_required_string(method_name, fields, "url", "string"),
  }
}

///|
/// Build a typed Page.frameScheduledNavigation event envelope.
///
/// Pass a PageFrameScheduledNavigationEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_scheduled_navigation_event(params, session_id=None)
/// ```
pub fn page_frame_scheduled_navigation_event(
  params : PageFrameScheduledNavigationEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameScheduledNavigation",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameStartedLoadingEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameStartedLoadingEventParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageFrameStartedLoadingEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameStartedLoadingEventParams::to_json(
  self : PageFrameStartedLoadingEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameStartedLoading event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameStartedLoadingEventParams::from_json(
  params : Json,
) -> PageFrameStartedLoadingEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameStartedLoading"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
  }
}

///|
/// Build a typed Page.frameStartedLoading event envelope.
///
/// Pass a PageFrameStartedLoadingEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_started_loading_event(params, session_id=None)
/// ```
pub fn page_frame_started_loading_event(
  params : PageFrameStartedLoadingEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameStartedLoading",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameStartedNavigatingEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameStartedNavigatingEventParams {
  frame_id : PageFrameId
  url : String
  loader_id : NetworkLoaderId
  navigation_type : String
} derive(Eq, Debug)

///|
/// Encode PageFrameStartedNavigatingEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameStartedNavigatingEventParams::to_json(
  self : PageFrameStartedNavigatingEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["url"] = Json::string(self.url)
  fields["loaderId"] = self.loader_id.to_json()
  fields["navigationType"] = Json::string(self.navigation_type)
  Json::object(fields)
}

///|
/// Decode and validate Page.frameStartedNavigating event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameStartedNavigatingEventParams::from_json(
  params : Json,
) -> PageFrameStartedNavigatingEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameStartedNavigating"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    url: typed_required_string(method_name, fields, "url", "string"),
    loader_id: NetworkLoaderId::from_json(
      typed_required_json(method_name, fields, "loaderId"),
    ),
    navigation_type: typed_required_string(
      method_name, fields, "navigationType", "string",
    ),
  }
}

///|
/// Build a typed Page.frameStartedNavigating event envelope.
///
/// Pass a PageFrameStartedNavigatingEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_started_navigating_event(params, session_id=None)
/// ```
pub fn page_frame_started_navigating_event(
  params : PageFrameStartedNavigatingEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameStartedNavigating",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameStoppedLoadingEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameStoppedLoadingEventParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageFrameStoppedLoadingEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameStoppedLoadingEventParams::to_json(
  self : PageFrameStoppedLoadingEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameStoppedLoading event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameStoppedLoadingEventParams::from_json(
  params : Json,
) -> PageFrameStoppedLoadingEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameStoppedLoading"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
  }
}

///|
/// Build a typed Page.frameStoppedLoading event envelope.
///
/// Pass a PageFrameStoppedLoadingEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_stopped_loading_event(params, session_id=None)
/// ```
pub fn page_frame_stopped_loading_event(
  params : PageFrameStoppedLoadingEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameStoppedLoading",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageFrameSubtreeWillBeDetachedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageFrameSubtreeWillBeDetachedEventParams {
  frame_id : PageFrameId
} derive(Eq, Debug)

///|
/// Encode PageFrameSubtreeWillBeDetachedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageFrameSubtreeWillBeDetachedEventParams::to_json(
  self : PageFrameSubtreeWillBeDetachedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.frameSubtreeWillBeDetached event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageFrameSubtreeWillBeDetachedEventParams::from_json(
  params : Json,
) -> PageFrameSubtreeWillBeDetachedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.frameSubtreeWillBeDetached"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
  }
}

///|
/// Build a typed Page.frameSubtreeWillBeDetached event envelope.
///
/// Pass a PageFrameSubtreeWillBeDetachedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_frame_subtree_will_be_detached_event(params, session_id=None)
/// ```
pub fn page_frame_subtree_will_be_detached_event(
  params : PageFrameSubtreeWillBeDetachedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.frameSubtreeWillBeDetached",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Page.interstitialHidden event envelope.
///
/// The event has no parameters. The builder validates the event name against
/// the bundled protocol schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_interstitial_hidden_event(session_id=None)
/// ```
pub fn page_interstitial_hidden_event(
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(method_name="Page.interstitialHidden", session_id?)
}

///|
/// Build a typed Page.interstitialShown event envelope.
///
/// The event has no parameters. The builder validates the event name against
/// the bundled protocol schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_interstitial_shown_event(session_id=None)
/// ```
pub fn page_interstitial_shown_event(
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(method_name="Page.interstitialShown", session_id?)
}

///|
/// Generated CDP value object for PageJavascriptDialogClosedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageJavascriptDialogClosedEventParams {
  frame_id : PageFrameId
  result : Bool
  user_input : String
} derive(Eq, Debug)

///|
/// Encode PageJavascriptDialogClosedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageJavascriptDialogClosedEventParams::to_json(
  self : PageJavascriptDialogClosedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["result"] = Json::boolean(self.result)
  fields["userInput"] = Json::string(self.user_input)
  Json::object(fields)
}

///|
/// Decode and validate Page.javascriptDialogClosed event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageJavascriptDialogClosedEventParams::from_json(
  params : Json,
) -> PageJavascriptDialogClosedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.javascriptDialogClosed"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    result: typed_required_bool(method_name, fields, "result", "boolean"),
    user_input: typed_required_string(
      method_name, fields, "userInput", "string",
    ),
  }
}

///|
/// Build a typed Page.javascriptDialogClosed event envelope.
///
/// Pass a PageJavascriptDialogClosedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_javascript_dialog_closed_event(params, session_id=None)
/// ```
pub fn page_javascript_dialog_closed_event(
  params : PageJavascriptDialogClosedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.javascriptDialogClosed",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageJavascriptDialogOpeningEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageJavascriptDialogOpeningEventParams {
  url : String
  frame_id : PageFrameId
  message : String
  type_ : PageDialogType
  has_browser_handler : Bool
  default_prompt : String?
} derive(Eq, Debug)

///|
/// Encode PageJavascriptDialogOpeningEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageJavascriptDialogOpeningEventParams::to_json(
  self : PageJavascriptDialogOpeningEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["frameId"] = self.frame_id.to_json()
  fields["message"] = Json::string(self.message)
  fields["type"] = self.type_.to_json()
  fields["hasBrowserHandler"] = Json::boolean(self.has_browser_handler)
  if self.default_prompt is Some(value) {
    fields["defaultPrompt"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode and validate Page.javascriptDialogOpening event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageJavascriptDialogOpeningEventParams::from_json(
  params : Json,
) -> PageJavascriptDialogOpeningEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.javascriptDialogOpening"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    message: typed_required_string(method_name, fields, "message", "string"),
    type_: PageDialogType::from_json(
      typed_required_json(method_name, fields, "type"),
    ),
    has_browser_handler: typed_required_bool(
      method_name, fields, "hasBrowserHandler", "boolean",
    ),
    default_prompt: typed_optional_string(
      method_name, fields, "defaultPrompt", "string",
    ),
  }
}

///|
/// Build a typed Page.javascriptDialogOpening event envelope.
///
/// Pass a PageJavascriptDialogOpeningEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_javascript_dialog_opening_event(params, session_id=None)
/// ```
pub fn page_javascript_dialog_opening_event(
  params : PageJavascriptDialogOpeningEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.javascriptDialogOpening",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageLifecycleEventEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageLifecycleEventEventParams {
  frame_id : PageFrameId
  loader_id : NetworkLoaderId
  name : String
  timestamp : NetworkMonotonicTime
} derive(Eq, Debug)

///|
/// Encode PageLifecycleEventEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageLifecycleEventEventParams::to_json(
  self : PageLifecycleEventEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["loaderId"] = self.loader_id.to_json()
  fields["name"] = Json::string(self.name)
  fields["timestamp"] = self.timestamp.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.lifecycleEvent event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageLifecycleEventEventParams::from_json(
  params : Json,
) -> PageLifecycleEventEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.lifecycleEvent"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    loader_id: NetworkLoaderId::from_json(
      typed_required_json(method_name, fields, "loaderId"),
    ),
    name: typed_required_string(method_name, fields, "name", "string"),
    timestamp: NetworkMonotonicTime::from_json(
      typed_required_json(method_name, fields, "timestamp"),
    ),
  }
}

///|
/// Build a typed Page.lifecycleEvent event envelope.
///
/// Pass a PageLifecycleEventEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_lifecycle_event_event(params, session_id=None)
/// ```
pub fn page_lifecycle_event_event(
  params : PageLifecycleEventEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.lifecycleEvent",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageLoadEventFiredEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageLoadEventFiredEventParams {
  timestamp : NetworkMonotonicTime
} derive(Eq, Debug)

///|
/// Encode PageLoadEventFiredEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageLoadEventFiredEventParams::to_json(
  self : PageLoadEventFiredEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["timestamp"] = self.timestamp.to_json()
  Json::object(fields)
}

///|
/// Decode and validate Page.loadEventFired event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageLoadEventFiredEventParams::from_json(
  params : Json,
) -> PageLoadEventFiredEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.loadEventFired"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    timestamp: NetworkMonotonicTime::from_json(
      typed_required_json(method_name, fields, "timestamp"),
    ),
  }
}

///|
/// Build a typed Page.loadEventFired event envelope.
///
/// Pass a PageLoadEventFiredEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_load_event_fired_event(params, session_id=None)
/// ```
pub fn page_load_event_fired_event(
  params : PageLoadEventFiredEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.loadEventFired",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageNavigatedWithinDocumentEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageNavigatedWithinDocumentEventParams {
  frame_id : PageFrameId
  url : String
  navigation_type : String
} derive(Eq, Debug)

///|
/// Encode PageNavigatedWithinDocumentEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageNavigatedWithinDocumentEventParams::to_json(
  self : PageNavigatedWithinDocumentEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["frameId"] = self.frame_id.to_json()
  fields["url"] = Json::string(self.url)
  fields["navigationType"] = Json::string(self.navigation_type)
  Json::object(fields)
}

///|
/// Decode and validate Page.navigatedWithinDocument event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageNavigatedWithinDocumentEventParams::from_json(
  params : Json,
) -> PageNavigatedWithinDocumentEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.navigatedWithinDocument"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    frame_id: PageFrameId::from_json(
      typed_required_json(method_name, fields, "frameId"),
    ),
    url: typed_required_string(method_name, fields, "url", "string"),
    navigation_type: typed_required_string(
      method_name, fields, "navigationType", "string",
    ),
  }
}

///|
/// Build a typed Page.navigatedWithinDocument event envelope.
///
/// Pass a PageNavigatedWithinDocumentEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_navigated_within_document_event(params, session_id=None)
/// ```
pub fn page_navigated_within_document_event(
  params : PageNavigatedWithinDocumentEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.navigatedWithinDocument",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageScreencastFrameEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScreencastFrameEventParams {
  data : String
  metadata : PageScreencastFrameMetadata
  session_id : Int
} derive(Eq, Debug)

///|
/// Encode PageScreencastFrameEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScreencastFrameEventParams::to_json(
  self : PageScreencastFrameEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["data"] = Json::string(self.data)
  fields["metadata"] = self.metadata.to_json()
  fields["sessionId"] = Json::number(self.session_id.to_double())
  Json::object(fields)
}

///|
/// Decode and validate Page.screencastFrame event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageScreencastFrameEventParams::from_json(
  params : Json,
) -> PageScreencastFrameEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.screencastFrame"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    data: typed_required_string(method_name, fields, "data", "string"),
    metadata: PageScreencastFrameMetadata::from_json(
      typed_required_json(method_name, fields, "metadata"),
    ),
    session_id: typed_required_int(method_name, fields, "sessionId", "integer"),
  }
}

///|
/// Build a typed Page.screencastFrame event envelope.
///
/// Pass a PageScreencastFrameEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_screencast_frame_event(params, session_id=None)
/// ```
pub fn page_screencast_frame_event(
  params : PageScreencastFrameEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.screencastFrame",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageScreencastVisibilityChangedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageScreencastVisibilityChangedEventParams {
  visible : Bool
} derive(Eq, Debug)

///|
/// Encode PageScreencastVisibilityChangedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageScreencastVisibilityChangedEventParams::to_json(
  self : PageScreencastVisibilityChangedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["visible"] = Json::boolean(self.visible)
  Json::object(fields)
}

///|
/// Decode and validate Page.screencastVisibilityChanged event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageScreencastVisibilityChangedEventParams::from_json(
  params : Json,
) -> PageScreencastVisibilityChangedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.screencastVisibilityChanged"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  { visible: typed_required_bool(method_name, fields, "visible", "boolean") }
}

///|
/// Build a typed Page.screencastVisibilityChanged event envelope.
///
/// Pass a PageScreencastVisibilityChangedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_screencast_visibility_changed_event(params, session_id=None)
/// ```
pub fn page_screencast_visibility_changed_event(
  params : PageScreencastVisibilityChangedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.screencastVisibilityChanged",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PageWindowOpenEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PageWindowOpenEventParams {
  url : String
  window_name : String
  window_features : Array[String]
  user_gesture : Bool
} derive(Eq, Debug)

///|
/// Encode PageWindowOpenEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PageWindowOpenEventParams::to_json(
  self : PageWindowOpenEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["url"] = Json::string(self.url)
  fields["windowName"] = Json::string(self.window_name)
  fields["windowFeatures"] = Json::array(
    self.window_features.map(item => Json::string(item)),
  )
  fields["userGesture"] = Json::boolean(self.user_gesture)
  Json::object(fields)
}

///|
/// Decode and validate Page.windowOpen event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn PageWindowOpenEventParams::from_json(
  params : Json,
) -> PageWindowOpenEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Page.windowOpen"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    url: typed_required_string(method_name, fields, "url", "string"),
    window_name: typed_required_string(
      method_name, fields, "windowName", "string",
    ),
    window_features: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "windowFeatures"),
      "windowFeatures",
      "array<string>",
    ).map(item => {
      typed_array_string(method_name, "windowFeatures", "array<string>", item)
    }),
    user_gesture: typed_required_bool(
      method_name, fields, "userGesture", "boolean",
    ),
  }
}

///|
/// Build a typed Page.windowOpen event envelope.
///
/// Pass a PageWindowOpenEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = page_window_open_event(params, session_id=None)
/// ```
pub fn page_window_open_event(
  params : PageWindowOpenEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Page.windowOpen",
    params=params.to_json(),
    session_id?,
  )
}
