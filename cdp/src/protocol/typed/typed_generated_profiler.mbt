///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for ProfilerCoverageRange.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerCoverageRange {
  start_offset : Int
  end_offset : Int
  count : Int
} derive(Eq, Debug)

///|
/// Encode ProfilerCoverageRange as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerCoverageRange::to_json(self : ProfilerCoverageRange) -> Json {
  let fields : Map[String, Json] = {}
  fields["startOffset"] = Json::number(self.start_offset.to_double())
  fields["endOffset"] = Json::number(self.end_offset.to_double())
  fields["count"] = Json::number(self.count.to_double())
  Json::object(fields)
}

///|
/// Decode ProfilerCoverageRange from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerCoverageRange::from_json(
  value : Json,
) -> ProfilerCoverageRange raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerCoverageRange"
  let fields = typed_json_fields(method_name, value)
  {
    start_offset: typed_required_int(
      method_name, fields, "startOffset", "integer",
    ),
    end_offset: typed_required_int(method_name, fields, "endOffset", "integer"),
    count: typed_required_int(method_name, fields, "count", "integer"),
  }
}

///|
/// Generated CDP value object for ProfilerFunctionCoverage.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerFunctionCoverage {
  function_name : String
  ranges : Array[ProfilerCoverageRange]
  is_block_coverage : Bool
} derive(Eq, Debug)

///|
/// Encode ProfilerFunctionCoverage as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerFunctionCoverage::to_json(
  self : ProfilerFunctionCoverage,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["functionName"] = Json::string(self.function_name)
  fields["ranges"] = Json::array(self.ranges.map(item => item.to_json()))
  fields["isBlockCoverage"] = Json::boolean(self.is_block_coverage)
  Json::object(fields)
}

///|
/// Decode ProfilerFunctionCoverage from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerFunctionCoverage::from_json(
  value : Json,
) -> ProfilerFunctionCoverage raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerFunctionCoverage"
  let fields = typed_json_fields(method_name, value)
  {
    function_name: typed_required_string(
      method_name, fields, "functionName", "string",
    ),
    ranges: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "ranges"),
      "ranges",
      "array<CoverageRange>",
    ).map(item => ProfilerCoverageRange::from_json(item)),
    is_block_coverage: typed_required_bool(
      method_name, fields, "isBlockCoverage", "boolean",
    ),
  }
}

///|
/// Generated CDP value object for ProfilerPositionTickInfo.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerPositionTickInfo {
  line : Int
  ticks : Int
} derive(Eq, Debug)

///|
/// Encode ProfilerPositionTickInfo as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerPositionTickInfo::to_json(
  self : ProfilerPositionTickInfo,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["line"] = Json::number(self.line.to_double())
  fields["ticks"] = Json::number(self.ticks.to_double())
  Json::object(fields)
}

///|
/// Decode ProfilerPositionTickInfo from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerPositionTickInfo::from_json(
  value : Json,
) -> ProfilerPositionTickInfo raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerPositionTickInfo"
  let fields = typed_json_fields(method_name, value)
  {
    line: typed_required_int(method_name, fields, "line", "integer"),
    ticks: typed_required_int(method_name, fields, "ticks", "integer"),
  }
}

///|
/// Generated CDP value object for ProfilerProfile.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerProfile {
  nodes : Array[ProfilerProfileNode]
  start_time : Double
  end_time : Double
  samples : Array[Int]?
  time_deltas : Array[Int]?
} derive(Eq, Debug)

///|
/// Encode ProfilerProfile as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerProfile::to_json(self : ProfilerProfile) -> Json {
  let fields : Map[String, Json] = {}
  fields["nodes"] = Json::array(self.nodes.map(item => item.to_json()))
  fields["startTime"] = Json::number(self.start_time)
  fields["endTime"] = Json::number(self.end_time)
  if self.samples is Some(value) {
    fields["samples"] = Json::array(
      value.map(item => Json::number(item.to_double())),
    )
  }
  if self.time_deltas is Some(value) {
    fields["timeDeltas"] = Json::array(
      value.map(item => Json::number(item.to_double())),
    )
  }
  Json::object(fields)
}

///|
/// Decode ProfilerProfile from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerProfile::from_json(
  value : Json,
) -> ProfilerProfile raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerProfile"
  let fields = typed_json_fields(method_name, value)
  {
    nodes: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "nodes"),
      "nodes",
      "array<ProfileNode>",
    ).map(item => ProfilerProfileNode::from_json(item)),
    start_time: typed_required_double(
      method_name, fields, "startTime", "number",
    ),
    end_time: typed_required_double(method_name, fields, "endTime", "number"),
    samples: match typed_optional_json(fields, "samples") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "samples", "array<integer>").map(item => {
              typed_array_int(method_name, "samples", "array<integer>", item)
            },
          ),
        )
      None => None
    },
    time_deltas: match typed_optional_json(fields, "timeDeltas") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "timeDeltas", "array<integer>").map(item => {
              typed_array_int(method_name, "timeDeltas", "array<integer>", item)
            },
          ),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for ProfilerProfileNode.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerProfileNode {
  id : Int
  call_frame : RuntimeCallFrame
  hit_count : Int?
  children : Array[Int]?
  deopt_reason : String?
  position_ticks : Array[ProfilerPositionTickInfo]?
} derive(Eq, Debug)

///|
/// Encode ProfilerProfileNode as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerProfileNode::to_json(self : ProfilerProfileNode) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::number(self.id.to_double())
  fields["callFrame"] = self.call_frame.to_json()
  if self.hit_count is Some(value) {
    fields["hitCount"] = Json::number(value.to_double())
  }
  if self.children is Some(value) {
    fields["children"] = Json::array(
      value.map(item => Json::number(item.to_double())),
    )
  }
  if self.deopt_reason is Some(value) {
    fields["deoptReason"] = Json::string(value)
  }
  if self.position_ticks is Some(value) {
    fields["positionTicks"] = Json::array(value.map(item => item.to_json()))
  }
  Json::object(fields)
}

///|
/// Decode ProfilerProfileNode from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerProfileNode::from_json(
  value : Json,
) -> ProfilerProfileNode raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerProfileNode"
  let fields = typed_json_fields(method_name, value)
  {
    id: typed_required_int(method_name, fields, "id", "integer"),
    call_frame: RuntimeCallFrame::from_json(
      typed_required_json(method_name, fields, "callFrame"),
    ),
    hit_count: typed_optional_int(method_name, fields, "hitCount", "integer"),
    children: match typed_optional_json(fields, "children") {
      Some(value) =>
        Some(
          typed_param_array(method_name, value, "children", "array<integer>").map(item => {
              typed_array_int(method_name, "children", "array<integer>", item)
            },
          ),
        )
      None => None
    },
    deopt_reason: typed_optional_string(
      method_name, fields, "deoptReason", "string",
    ),
    position_ticks: match typed_optional_json(fields, "positionTicks") {
      Some(value) =>
        Some(
          typed_param_array(
            method_name, value, "positionTicks", "array<PositionTickInfo>",
          ).map(item => ProfilerPositionTickInfo::from_json(item)),
        )
      None => None
    },
  }
}

///|
/// Generated CDP value object for ProfilerScriptCoverage.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerScriptCoverage {
  script_id : RuntimeScriptId
  url : String
  functions : Array[ProfilerFunctionCoverage]
} derive(Eq, Debug)

///|
/// Encode ProfilerScriptCoverage as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerScriptCoverage::to_json(self : ProfilerScriptCoverage) -> Json {
  let fields : Map[String, Json] = {}
  fields["scriptId"] = self.script_id.to_json()
  fields["url"] = Json::string(self.url)
  fields["functions"] = Json::array(self.functions.map(item => item.to_json()))
  Json::object(fields)
}

///|
/// Decode ProfilerScriptCoverage from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn ProfilerScriptCoverage::from_json(
  value : Json,
) -> ProfilerScriptCoverage raise @protocol.ProtocolSchemaError {
  let method_name = "ProfilerScriptCoverage"
  let fields = typed_json_fields(method_name, value)
  {
    script_id: RuntimeScriptId::from_json(
      typed_required_json(method_name, fields, "scriptId"),
    ),
    url: typed_required_string(method_name, fields, "url", "string"),
    functions: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "functions"),
      "functions",
      "array<FunctionCoverage>",
    ).map(item => ProfilerFunctionCoverage::from_json(item)),
  }
}

///|
/// Build a typed Profiler.disable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_disable_command(id=1, session_id=None)
/// ```
pub fn profiler_disable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Profiler.disable", session_id?)
}

///|
/// Build a typed Profiler.enable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_enable_command(id=1, session_id=None)
/// ```
pub fn profiler_enable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Profiler.enable", session_id?)
}

///|
/// Generated CDP value object for ProfilerGetBestEffortCoverageResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerGetBestEffortCoverageResult {
  result : Array[ProfilerScriptCoverage]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Profiler.getBestEffortCoverage.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ProfilerGetBestEffortCoverageResult::from_json(
  result : Json,
) -> ProfilerGetBestEffortCoverageResult raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.getBestEffortCoverage"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    result: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "result"),
      "result",
      "array<ScriptCoverage>",
    ).map(item => ProfilerScriptCoverage::from_json(item)),
  }
}

///|
/// Build a typed Profiler.getBestEffortCoverage command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_get_best_effort_coverage_command(id=1, session_id=None)
/// ```
pub fn profiler_get_best_effort_coverage_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Profiler.getBestEffortCoverage",
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerSetSamplingIntervalParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerSetSamplingIntervalParams {
  interval : Int
} derive(Eq, Debug)

///|
/// Encode ProfilerSetSamplingIntervalParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerSetSamplingIntervalParams::to_json(
  self : ProfilerSetSamplingIntervalParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["interval"] = Json::number(self.interval.to_double())
  Json::object(fields)
}

///|
/// Build a typed Profiler.setSamplingInterval command envelope.
///
/// Pass a ProfilerSetSamplingIntervalParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_set_sampling_interval_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn profiler_set_sampling_interval_command(
  id~ : Int,
  params : ProfilerSetSamplingIntervalParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Profiler.setSamplingInterval",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Build a typed Profiler.start command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_start_command(id=1, session_id=None)
/// ```
pub fn profiler_start_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Profiler.start", session_id?)
}

///|
/// Generated CDP value object for ProfilerStartPreciseCoverageParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerStartPreciseCoverageParams {
  call_count : Bool?
  detailed : Bool?
  allow_triggered_updates : Bool?
} derive(Eq, Debug)

///|
/// Encode ProfilerStartPreciseCoverageParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerStartPreciseCoverageParams::to_json(
  self : ProfilerStartPreciseCoverageParams,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.call_count is Some(value) {
    fields["callCount"] = Json::boolean(value)
  }
  if self.detailed is Some(value) {
    fields["detailed"] = Json::boolean(value)
  }
  if self.allow_triggered_updates is Some(value) {
    fields["allowTriggeredUpdates"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for ProfilerStartPreciseCoverageResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerStartPreciseCoverageResult {
  timestamp : Double
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Profiler.startPreciseCoverage.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ProfilerStartPreciseCoverageResult::from_json(
  result : Json,
) -> ProfilerStartPreciseCoverageResult raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.startPreciseCoverage"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    timestamp: typed_required_result_double(
      method_name, fields, "timestamp", "number",
    ),
  }
}

///|
/// Build a typed Profiler.startPreciseCoverage command envelope.
///
/// Pass a ProfilerStartPreciseCoverageParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_start_precise_coverage_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn profiler_start_precise_coverage_command(
  id~ : Int,
  params : ProfilerStartPreciseCoverageParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Profiler.startPreciseCoverage",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerStopResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerStopResult {
  profile : ProfilerProfile
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Profiler.stop.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ProfilerStopResult::from_json(
  result : Json,
) -> ProfilerStopResult raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.stop"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    profile: ProfilerProfile::from_json(
      typed_required_result_json(method_name, fields, "profile"),
    ),
  }
}

///|
/// Build a typed Profiler.stop command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_stop_command(id=1, session_id=None)
/// ```
pub fn profiler_stop_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(id~, method_name="Profiler.stop", session_id?)
}

///|
/// Build a typed Profiler.stopPreciseCoverage command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_stop_precise_coverage_command(id=1, session_id=None)
/// ```
pub fn profiler_stop_precise_coverage_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Profiler.stopPreciseCoverage",
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerTakePreciseCoverageResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerTakePreciseCoverageResult {
  result : Array[ProfilerScriptCoverage]
  timestamp : Double
} derive(Eq, Debug)

///|
/// Decode and validate the result object for Profiler.takePreciseCoverage.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn ProfilerTakePreciseCoverageResult::from_json(
  result : Json,
) -> ProfilerTakePreciseCoverageResult raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.takePreciseCoverage"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    result: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "result"),
      "result",
      "array<ScriptCoverage>",
    ).map(item => ProfilerScriptCoverage::from_json(item)),
    timestamp: typed_required_result_double(
      method_name, fields, "timestamp", "number",
    ),
  }
}

///|
/// Build a typed Profiler.takePreciseCoverage command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = profiler_take_precise_coverage_command(id=1, session_id=None)
/// ```
pub fn profiler_take_precise_coverage_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Profiler.takePreciseCoverage",
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerConsoleProfileFinishedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerConsoleProfileFinishedEventParams {
  id : String
  location : DebuggerLocation
  profile : ProfilerProfile
  title : String?
} derive(Eq, Debug)

///|
/// Encode ProfilerConsoleProfileFinishedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerConsoleProfileFinishedEventParams::to_json(
  self : ProfilerConsoleProfileFinishedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["location"] = self.location.to_json()
  fields["profile"] = self.profile.to_json()
  if self.title is Some(value) {
    fields["title"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode and validate Profiler.consoleProfileFinished event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn ProfilerConsoleProfileFinishedEventParams::from_json(
  params : Json,
) -> ProfilerConsoleProfileFinishedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.consoleProfileFinished"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    id: typed_required_string(method_name, fields, "id", "string"),
    location: DebuggerLocation::from_json(
      typed_required_json(method_name, fields, "location"),
    ),
    profile: ProfilerProfile::from_json(
      typed_required_json(method_name, fields, "profile"),
    ),
    title: typed_optional_string(method_name, fields, "title", "string"),
  }
}

///|
/// Build a typed Profiler.consoleProfileFinished event envelope.
///
/// Pass a ProfilerConsoleProfileFinishedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = profiler_console_profile_finished_event(params, session_id=None)
/// ```
pub fn profiler_console_profile_finished_event(
  params : ProfilerConsoleProfileFinishedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Profiler.consoleProfileFinished",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerConsoleProfileStartedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerConsoleProfileStartedEventParams {
  id : String
  location : DebuggerLocation
  title : String?
} derive(Eq, Debug)

///|
/// Encode ProfilerConsoleProfileStartedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerConsoleProfileStartedEventParams::to_json(
  self : ProfilerConsoleProfileStartedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["id"] = Json::string(self.id)
  fields["location"] = self.location.to_json()
  if self.title is Some(value) {
    fields["title"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Decode and validate Profiler.consoleProfileStarted event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn ProfilerConsoleProfileStartedEventParams::from_json(
  params : Json,
) -> ProfilerConsoleProfileStartedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.consoleProfileStarted"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    id: typed_required_string(method_name, fields, "id", "string"),
    location: DebuggerLocation::from_json(
      typed_required_json(method_name, fields, "location"),
    ),
    title: typed_optional_string(method_name, fields, "title", "string"),
  }
}

///|
/// Build a typed Profiler.consoleProfileStarted event envelope.
///
/// Pass a ProfilerConsoleProfileStartedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = profiler_console_profile_started_event(params, session_id=None)
/// ```
pub fn profiler_console_profile_started_event(
  params : ProfilerConsoleProfileStartedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Profiler.consoleProfileStarted",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for ProfilerPreciseCoverageDeltaUpdateEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct ProfilerPreciseCoverageDeltaUpdateEventParams {
  timestamp : Double
  occasion : String
  result : Array[ProfilerScriptCoverage]
} derive(Eq, Debug)

///|
/// Encode ProfilerPreciseCoverageDeltaUpdateEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn ProfilerPreciseCoverageDeltaUpdateEventParams::to_json(
  self : ProfilerPreciseCoverageDeltaUpdateEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["timestamp"] = Json::number(self.timestamp)
  fields["occasion"] = Json::string(self.occasion)
  fields["result"] = Json::array(self.result.map(item => item.to_json()))
  Json::object(fields)
}

///|
/// Decode and validate Profiler.preciseCoverageDeltaUpdate event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn ProfilerPreciseCoverageDeltaUpdateEventParams::from_json(
  params : Json,
) -> ProfilerPreciseCoverageDeltaUpdateEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Profiler.preciseCoverageDeltaUpdate"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    timestamp: typed_required_double(method_name, fields, "timestamp", "number"),
    occasion: typed_required_string(method_name, fields, "occasion", "string"),
    result: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "result"),
      "result",
      "array<ScriptCoverage>",
    ).map(item => ProfilerScriptCoverage::from_json(item)),
  }
}

///|
/// Build a typed Profiler.preciseCoverageDeltaUpdate event envelope.
///
/// Pass a ProfilerPreciseCoverageDeltaUpdateEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = profiler_precise_coverage_delta_update_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn profiler_precise_coverage_delta_update_event(
  params : ProfilerPreciseCoverageDeltaUpdateEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Profiler.preciseCoverageDeltaUpdate",
    params=params.to_json(),
    session_id?,
  )
}
