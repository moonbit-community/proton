///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for PWADisplayMode.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWADisplayMode {
  value : String
} derive(Eq, Debug)

///|
/// Encode PWADisplayMode as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWADisplayMode::to_json(self : PWADisplayMode) -> Json {
  Json::string(self.value)
}

///|
/// Decode PWADisplayMode from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PWADisplayMode::from_json(
  value : Json,
) -> PWADisplayMode raise @protocol.ProtocolSchemaError {
  let method_name = "PWADisplayMode"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for PWAFileHandler.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAFileHandler {
  action : String
  accepts : Array[PWAFileHandlerAccept]
  display_name : String
} derive(Eq, Debug)

///|
/// Encode PWAFileHandler as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAFileHandler::to_json(self : PWAFileHandler) -> Json {
  let fields : Map[String, Json] = {}
  fields["action"] = Json::string(self.action)
  fields["accepts"] = Json::array(self.accepts.map(item => item.to_json()))
  fields["displayName"] = Json::string(self.display_name)
  Json::object(fields)
}

///|
/// Decode PWAFileHandler from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PWAFileHandler::from_json(
  value : Json,
) -> PWAFileHandler raise @protocol.ProtocolSchemaError {
  let method_name = "PWAFileHandler"
  let fields = typed_json_fields(method_name, value)
  {
    action: typed_required_string(method_name, fields, "action", "string"),
    accepts: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "accepts"),
      "accepts",
      "array<FileHandlerAccept>",
    ).map(item => PWAFileHandlerAccept::from_json(item)),
    display_name: typed_required_string(
      method_name, fields, "displayName", "string",
    ),
  }
}

///|
/// Generated CDP value object for PWAFileHandlerAccept.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAFileHandlerAccept {
  media_type : String
  file_extensions : Array[String]
} derive(Eq, Debug)

///|
/// Encode PWAFileHandlerAccept as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAFileHandlerAccept::to_json(self : PWAFileHandlerAccept) -> Json {
  let fields : Map[String, Json] = {}
  fields["mediaType"] = Json::string(self.media_type)
  fields["fileExtensions"] = Json::array(
    self.file_extensions.map(item => Json::string(item)),
  )
  Json::object(fields)
}

///|
/// Decode PWAFileHandlerAccept from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn PWAFileHandlerAccept::from_json(
  value : Json,
) -> PWAFileHandlerAccept raise @protocol.ProtocolSchemaError {
  let method_name = "PWAFileHandlerAccept"
  let fields = typed_json_fields(method_name, value)
  {
    media_type: typed_required_string(
      method_name, fields, "mediaType", "string",
    ),
    file_extensions: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "fileExtensions"),
      "fileExtensions",
      "array<string>",
    ).map(item => {
      typed_array_string(method_name, "fileExtensions", "array<string>", item)
    }),
  }
}

///|
/// Generated CDP value object for PWAChangeAppUserSettingsParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAChangeAppUserSettingsParams {
  manifest_id : String
  link_capturing : Bool?
  display_mode : PWADisplayMode?
} derive(Eq, Debug)

///|
/// Encode PWAChangeAppUserSettingsParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAChangeAppUserSettingsParams::to_json(
  self : PWAChangeAppUserSettingsParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  if self.link_capturing is Some(value) {
    fields["linkCapturing"] = Json::boolean(value)
  }
  if self.display_mode is Some(value) {
    fields["displayMode"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Build a typed PWA.changeAppUserSettings command envelope.
///
/// Pass a PWAChangeAppUserSettingsParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_change_app_user_settings_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn pwa_change_app_user_settings_command(
  id~ : Int,
  params : PWAChangeAppUserSettingsParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.changeAppUserSettings",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWAGetOsAppStateParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAGetOsAppStateParams {
  manifest_id : String
} derive(Eq, Debug)

///|
/// Encode PWAGetOsAppStateParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAGetOsAppStateParams::to_json(self : PWAGetOsAppStateParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  Json::object(fields)
}

///|
/// Generated CDP value object for PWAGetOsAppStateResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAGetOsAppStateResult {
  badge_count : Int
  file_handlers : Array[PWAFileHandler]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for PWA.getOsAppState.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PWAGetOsAppStateResult::from_json(
  result : Json,
) -> PWAGetOsAppStateResult raise @protocol.ProtocolSchemaError {
  let method_name = "PWA.getOsAppState"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    badge_count: typed_required_result_int(
      method_name, fields, "badgeCount", "integer",
    ),
    file_handlers: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "fileHandlers"),
      "fileHandlers",
      "array<FileHandler>",
    ).map(item => PWAFileHandler::from_json(item)),
  }
}

///|
/// Build a typed PWA.getOsAppState command envelope.
///
/// Pass a PWAGetOsAppStateParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_get_os_app_state_command(id=1, params, session_id=None)
/// ```
pub fn pwa_get_os_app_state_command(
  id~ : Int,
  params : PWAGetOsAppStateParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.getOsAppState",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWAInstallParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAInstallParams {
  manifest_id : String
  install_url_or_bundle_url : String?
} derive(Eq, Debug)

///|
/// Encode PWAInstallParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAInstallParams::to_json(self : PWAInstallParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  if self.install_url_or_bundle_url is Some(value) {
    fields["installUrlOrBundleUrl"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Build a typed PWA.install command envelope.
///
/// Pass a PWAInstallParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_install_command(id=1, params, session_id=None)
/// ```
pub fn pwa_install_command(
  id~ : Int,
  params : PWAInstallParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.install",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWALaunchParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWALaunchParams {
  manifest_id : String
  url : String?
} derive(Eq, Debug)

///|
/// Encode PWALaunchParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWALaunchParams::to_json(self : PWALaunchParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  if self.url is Some(value) {
    fields["url"] = Json::string(value)
  }
  Json::object(fields)
}

///|
/// Generated CDP value object for PWALaunchResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWALaunchResult {
  target_id : TargetTargetID
} derive(Eq, Debug)

///|
/// Decode and validate the result object for PWA.launch.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PWALaunchResult::from_json(
  result : Json,
) -> PWALaunchResult raise @protocol.ProtocolSchemaError {
  let method_name = "PWA.launch"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    target_id: TargetTargetID::from_json(
      typed_required_result_json(method_name, fields, "targetId"),
    ),
  }
}

///|
/// Build a typed PWA.launch command envelope.
///
/// Pass a PWALaunchParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_launch_command(id=1, params, session_id=None)
/// ```
pub fn pwa_launch_command(
  id~ : Int,
  params : PWALaunchParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.launch",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWALaunchFilesInAppParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWALaunchFilesInAppParams {
  manifest_id : String
  files : Array[String]
} derive(Eq, Debug)

///|
/// Encode PWALaunchFilesInAppParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWALaunchFilesInAppParams::to_json(
  self : PWALaunchFilesInAppParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  fields["files"] = Json::array(self.files.map(item => Json::string(item)))
  Json::object(fields)
}

///|
/// Generated CDP value object for PWALaunchFilesInAppResult.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWALaunchFilesInAppResult {
  target_ids : Array[TargetTargetID]
} derive(Eq, Debug)

///|
/// Decode and validate the result object for PWA.launchFilesInApp.
///
/// Pass the `result` object from a successful CDP response. The decoder checks
/// required return fields and known primitive types against the bundled
/// protocol schema.
pub fn PWALaunchFilesInAppResult::from_json(
  result : Json,
) -> PWALaunchFilesInAppResult raise @protocol.ProtocolSchemaError {
  let method_name = "PWA.launchFilesInApp"
  let response : @protocol.CdpResponseMessage = {
    id: 0,
    result: Some(result),
    error: None,
    session_id: None,
  }
  ignore(@protocol.cdp_schema_response(method_name~, response))
  let fields = typed_result_fields(method_name, result)
  {
    target_ids: typed_result_array(
      method_name,
      typed_required_result_json(method_name, fields, "targetIds"),
      "targetIds",
      "array<Target.TargetID>",
    ).map(item => TargetTargetID::from_json(item)),
  }
}

///|
/// Build a typed PWA.launchFilesInApp command envelope.
///
/// Pass a PWALaunchFilesInAppParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_launch_files_in_app_command(id=1, params, session_id=None)
/// ```
pub fn pwa_launch_files_in_app_command(
  id~ : Int,
  params : PWALaunchFilesInAppParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.launchFilesInApp",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWAOpenCurrentPageInAppParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAOpenCurrentPageInAppParams {
  manifest_id : String
} derive(Eq, Debug)

///|
/// Encode PWAOpenCurrentPageInAppParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAOpenCurrentPageInAppParams::to_json(
  self : PWAOpenCurrentPageInAppParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  Json::object(fields)
}

///|
/// Build a typed PWA.openCurrentPageInApp command envelope.
///
/// Pass a PWAOpenCurrentPageInAppParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_open_current_page_in_app_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn pwa_open_current_page_in_app_command(
  id~ : Int,
  params : PWAOpenCurrentPageInAppParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.openCurrentPageInApp",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for PWAUninstallParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct PWAUninstallParams {
  manifest_id : String
} derive(Eq, Debug)

///|
/// Encode PWAUninstallParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn PWAUninstallParams::to_json(self : PWAUninstallParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["manifestId"] = Json::string(self.manifest_id)
  Json::object(fields)
}

///|
/// Build a typed PWA.uninstall command envelope.
///
/// Pass a PWAUninstallParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = pwa_uninstall_command(id=1, params, session_id=None)
/// ```
pub fn pwa_uninstall_command(
  id~ : Int,
  params : PWAUninstallParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="PWA.uninstall",
    params=params.to_json(),
    session_id?,
  )
}
