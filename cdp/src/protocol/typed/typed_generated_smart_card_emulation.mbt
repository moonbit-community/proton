///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for SmartCardEmulationConnectionState.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationConnectionState {
  value : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationConnectionState as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationConnectionState::to_json(
  self : SmartCardEmulationConnectionState,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode SmartCardEmulationConnectionState from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationConnectionState::from_json(
  value : Json,
) -> SmartCardEmulationConnectionState raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationConnectionState"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for SmartCardEmulationDisposition.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationDisposition {
  value : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationDisposition as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationDisposition::to_json(
  self : SmartCardEmulationDisposition,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode SmartCardEmulationDisposition from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationDisposition::from_json(
  value : Json,
) -> SmartCardEmulationDisposition raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationDisposition"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for SmartCardEmulationProtocol.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationProtocol {
  value : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationProtocol as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationProtocol::to_json(
  self : SmartCardEmulationProtocol,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode SmartCardEmulationProtocol from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationProtocol::from_json(
  value : Json,
) -> SmartCardEmulationProtocol raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationProtocol"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for SmartCardEmulationProtocolSet.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationProtocolSet {
  t0 : Bool?
  t1 : Bool?
  raw : Bool?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationProtocolSet as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationProtocolSet::to_json(
  self : SmartCardEmulationProtocolSet,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.t0 is Some(value) {
    fields["t0"] = Json::boolean(value)
  }
  if self.t1 is Some(value) {
    fields["t1"] = Json::boolean(value)
  }
  if self.raw is Some(value) {
    fields["raw"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Decode SmartCardEmulationProtocolSet from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationProtocolSet::from_json(
  value : Json,
) -> SmartCardEmulationProtocolSet raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationProtocolSet"
  let fields = typed_json_fields(method_name, value)
  {
    t0: typed_optional_bool(method_name, fields, "t0", "boolean"),
    t1: typed_optional_bool(method_name, fields, "t1", "boolean"),
    raw: typed_optional_bool(method_name, fields, "raw", "boolean"),
  }
}

///|
/// Generated CDP value object for SmartCardEmulationReaderStateFlags.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReaderStateFlags {
  unaware : Bool?
  ignore : Bool?
  changed : Bool?
  unknown : Bool?
  unavailable : Bool?
  empty : Bool?
  present : Bool?
  exclusive : Bool?
  inuse : Bool?
  mute : Bool?
  unpowered : Bool?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReaderStateFlags as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReaderStateFlags::to_json(
  self : SmartCardEmulationReaderStateFlags,
) -> Json {
  let fields : Map[String, Json] = {}
  if self.unaware is Some(value) {
    fields["unaware"] = Json::boolean(value)
  }
  if self.ignore is Some(value) {
    fields["ignore"] = Json::boolean(value)
  }
  if self.changed is Some(value) {
    fields["changed"] = Json::boolean(value)
  }
  if self.unknown is Some(value) {
    fields["unknown"] = Json::boolean(value)
  }
  if self.unavailable is Some(value) {
    fields["unavailable"] = Json::boolean(value)
  }
  if self.empty is Some(value) {
    fields["empty"] = Json::boolean(value)
  }
  if self.present is Some(value) {
    fields["present"] = Json::boolean(value)
  }
  if self.exclusive is Some(value) {
    fields["exclusive"] = Json::boolean(value)
  }
  if self.inuse is Some(value) {
    fields["inuse"] = Json::boolean(value)
  }
  if self.mute is Some(value) {
    fields["mute"] = Json::boolean(value)
  }
  if self.unpowered is Some(value) {
    fields["unpowered"] = Json::boolean(value)
  }
  Json::object(fields)
}

///|
/// Decode SmartCardEmulationReaderStateFlags from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationReaderStateFlags::from_json(
  value : Json,
) -> SmartCardEmulationReaderStateFlags raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationReaderStateFlags"
  let fields = typed_json_fields(method_name, value)
  {
    unaware: typed_optional_bool(method_name, fields, "unaware", "boolean"),
    ignore: typed_optional_bool(method_name, fields, "ignore", "boolean"),
    changed: typed_optional_bool(method_name, fields, "changed", "boolean"),
    unknown: typed_optional_bool(method_name, fields, "unknown", "boolean"),
    unavailable: typed_optional_bool(
      method_name, fields, "unavailable", "boolean",
    ),
    empty: typed_optional_bool(method_name, fields, "empty", "boolean"),
    present: typed_optional_bool(method_name, fields, "present", "boolean"),
    exclusive: typed_optional_bool(method_name, fields, "exclusive", "boolean"),
    inuse: typed_optional_bool(method_name, fields, "inuse", "boolean"),
    mute: typed_optional_bool(method_name, fields, "mute", "boolean"),
    unpowered: typed_optional_bool(method_name, fields, "unpowered", "boolean"),
  }
}

///|
/// Generated CDP value object for SmartCardEmulationReaderStateIn.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReaderStateIn {
  reader : String
  current_state : SmartCardEmulationReaderStateFlags
  current_insertion_count : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReaderStateIn as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReaderStateIn::to_json(
  self : SmartCardEmulationReaderStateIn,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["reader"] = Json::string(self.reader)
  fields["currentState"] = self.current_state.to_json()
  fields["currentInsertionCount"] = Json::number(
    self.current_insertion_count.to_double(),
  )
  Json::object(fields)
}

///|
/// Decode SmartCardEmulationReaderStateIn from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationReaderStateIn::from_json(
  value : Json,
) -> SmartCardEmulationReaderStateIn raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationReaderStateIn"
  let fields = typed_json_fields(method_name, value)
  {
    reader: typed_required_string(method_name, fields, "reader", "string"),
    current_state: SmartCardEmulationReaderStateFlags::from_json(
      typed_required_json(method_name, fields, "currentState"),
    ),
    current_insertion_count: typed_required_int(
      method_name, fields, "currentInsertionCount", "integer",
    ),
  }
}

///|
/// Generated CDP value object for SmartCardEmulationReaderStateOut.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReaderStateOut {
  reader : String
  event_state : SmartCardEmulationReaderStateFlags
  event_count : Int
  atr : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReaderStateOut as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReaderStateOut::to_json(
  self : SmartCardEmulationReaderStateOut,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["reader"] = Json::string(self.reader)
  fields["eventState"] = self.event_state.to_json()
  fields["eventCount"] = Json::number(self.event_count.to_double())
  fields["atr"] = Json::string(self.atr)
  Json::object(fields)
}

///|
/// Decode SmartCardEmulationReaderStateOut from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationReaderStateOut::from_json(
  value : Json,
) -> SmartCardEmulationReaderStateOut raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationReaderStateOut"
  let fields = typed_json_fields(method_name, value)
  {
    reader: typed_required_string(method_name, fields, "reader", "string"),
    event_state: SmartCardEmulationReaderStateFlags::from_json(
      typed_required_json(method_name, fields, "eventState"),
    ),
    event_count: typed_required_int(
      method_name, fields, "eventCount", "integer",
    ),
    atr: typed_required_string(method_name, fields, "atr", "string"),
  }
}

///|
/// Generated CDP value object for SmartCardEmulationResultCode.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationResultCode {
  value : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationResultCode as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationResultCode::to_json(
  self : SmartCardEmulationResultCode,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode SmartCardEmulationResultCode from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationResultCode::from_json(
  value : Json,
) -> SmartCardEmulationResultCode raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationResultCode"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Generated CDP value object for SmartCardEmulationShareMode.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationShareMode {
  value : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationShareMode as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationShareMode::to_json(
  self : SmartCardEmulationShareMode,
) -> Json {
  Json::string(self.value)
}

///|
/// Decode SmartCardEmulationShareMode from CDP JSON.
///
/// This helper is generated from a protocol domain type and is used by command
/// result and event parameter decoders.
pub fn SmartCardEmulationShareMode::from_json(
  value : Json,
) -> SmartCardEmulationShareMode raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulationShareMode"
  match value {
    String(inner) => { value: inner }
    _ => raise InvalidParameterType(method_name, "value", "string")
  }
}

///|
/// Build a typed SmartCardEmulation.disable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_disable_command(id=1, session_id=None)
/// ```
pub fn smart_card_emulation_disable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.disable",
    session_id?,
  )
}

///|
/// Build a typed SmartCardEmulation.enable command envelope.
///
/// The command has no parameters. The builder validates the method against
/// the bundled protocol schema and returns a `CdpCommandMessage` that can be
/// sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_enable_command(id=1, session_id=None)
/// ```
pub fn smart_card_emulation_enable_command(
  id~ : Int,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.enable",
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportBeginTransactionResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportBeginTransactionResultParams {
  request_id : String
  handle : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportBeginTransactionResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportBeginTransactionResultParams::to_json(
  self : SmartCardEmulationReportBeginTransactionResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportBeginTransactionResult command envelope.
///
/// Pass a SmartCardEmulationReportBeginTransactionResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_begin_transaction_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_begin_transaction_result_command(
  id~ : Int,
  params : SmartCardEmulationReportBeginTransactionResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportBeginTransactionResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportConnectResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportConnectResultParams {
  request_id : String
  handle : Int
  active_protocol : SmartCardEmulationProtocol?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportConnectResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportConnectResultParams::to_json(
  self : SmartCardEmulationReportConnectResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  if self.active_protocol is Some(value) {
    fields["activeProtocol"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportConnectResult command envelope.
///
/// Pass a SmartCardEmulationReportConnectResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_connect_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_connect_result_command(
  id~ : Int,
  params : SmartCardEmulationReportConnectResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportConnectResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportDataResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportDataResultParams {
  request_id : String
  data : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportDataResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportDataResultParams::to_json(
  self : SmartCardEmulationReportDataResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["data"] = Json::string(self.data)
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportDataResult command envelope.
///
/// Pass a SmartCardEmulationReportDataResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_data_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_data_result_command(
  id~ : Int,
  params : SmartCardEmulationReportDataResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportDataResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportErrorParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportErrorParams {
  request_id : String
  result_code : SmartCardEmulationResultCode
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportErrorParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportErrorParams::to_json(
  self : SmartCardEmulationReportErrorParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["resultCode"] = self.result_code.to_json()
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportError command envelope.
///
/// Pass a SmartCardEmulationReportErrorParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_error_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_error_command(
  id~ : Int,
  params : SmartCardEmulationReportErrorParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportError",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportEstablishContextResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportEstablishContextResultParams {
  request_id : String
  context_id : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportEstablishContextResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportEstablishContextResultParams::to_json(
  self : SmartCardEmulationReportEstablishContextResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportEstablishContextResult command envelope.
///
/// Pass a SmartCardEmulationReportEstablishContextResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_establish_context_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_establish_context_result_command(
  id~ : Int,
  params : SmartCardEmulationReportEstablishContextResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportEstablishContextResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportGetStatusChangeResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportGetStatusChangeResultParams {
  request_id : String
  reader_states : Array[SmartCardEmulationReaderStateOut]
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportGetStatusChangeResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportGetStatusChangeResultParams::to_json(
  self : SmartCardEmulationReportGetStatusChangeResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["readerStates"] = Json::array(
    self.reader_states.map(item => item.to_json()),
  )
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportGetStatusChangeResult command envelope.
///
/// Pass a SmartCardEmulationReportGetStatusChangeResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_get_status_change_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_get_status_change_result_command(
  id~ : Int,
  params : SmartCardEmulationReportGetStatusChangeResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportGetStatusChangeResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportListReadersResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportListReadersResultParams {
  request_id : String
  readers : Array[String]
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportListReadersResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportListReadersResultParams::to_json(
  self : SmartCardEmulationReportListReadersResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["readers"] = Json::array(self.readers.map(item => Json::string(item)))
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportListReadersResult command envelope.
///
/// Pass a SmartCardEmulationReportListReadersResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_list_readers_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_list_readers_result_command(
  id~ : Int,
  params : SmartCardEmulationReportListReadersResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportListReadersResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportPlainResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportPlainResultParams {
  request_id : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportPlainResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportPlainResultParams::to_json(
  self : SmartCardEmulationReportPlainResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportPlainResult command envelope.
///
/// Pass a SmartCardEmulationReportPlainResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_plain_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_plain_result_command(
  id~ : Int,
  params : SmartCardEmulationReportPlainResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportPlainResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportReleaseContextResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportReleaseContextResultParams {
  request_id : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportReleaseContextResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportReleaseContextResultParams::to_json(
  self : SmartCardEmulationReportReleaseContextResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportReleaseContextResult command envelope.
///
/// Pass a SmartCardEmulationReportReleaseContextResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_release_context_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_release_context_result_command(
  id~ : Int,
  params : SmartCardEmulationReportReleaseContextResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportReleaseContextResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReportStatusResultParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReportStatusResultParams {
  request_id : String
  reader_name : String
  state : SmartCardEmulationConnectionState
  atr : String
  protocol : SmartCardEmulationProtocol?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReportStatusResultParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReportStatusResultParams::to_json(
  self : SmartCardEmulationReportStatusResultParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["readerName"] = Json::string(self.reader_name)
  fields["state"] = self.state.to_json()
  fields["atr"] = Json::string(self.atr)
  if self.protocol is Some(value) {
    fields["protocol"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Build a typed SmartCardEmulation.reportStatusResult command envelope.
///
/// Pass a SmartCardEmulationReportStatusResultParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = smart_card_emulation_report_status_result_command(
///   id=1,
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_report_status_result_command(
  id~ : Int,
  params : SmartCardEmulationReportStatusResultParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="SmartCardEmulation.reportStatusResult",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationBeginTransactionRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationBeginTransactionRequestedEventParams {
  request_id : String
  handle : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationBeginTransactionRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationBeginTransactionRequestedEventParams::to_json(
  self : SmartCardEmulationBeginTransactionRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.beginTransactionRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationBeginTransactionRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationBeginTransactionRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.beginTransactionRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.beginTransactionRequested event envelope.
///
/// Pass a SmartCardEmulationBeginTransactionRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_begin_transaction_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_begin_transaction_requested_event(
  params : SmartCardEmulationBeginTransactionRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.beginTransactionRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationCancelRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationCancelRequestedEventParams {
  request_id : String
  context_id : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationCancelRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationCancelRequestedEventParams::to_json(
  self : SmartCardEmulationCancelRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.cancelRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationCancelRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationCancelRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.cancelRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    context_id: typed_required_int(method_name, fields, "contextId", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.cancelRequested event envelope.
///
/// Pass a SmartCardEmulationCancelRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_cancel_requested_event(params, session_id=None)
/// ```
pub fn smart_card_emulation_cancel_requested_event(
  params : SmartCardEmulationCancelRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.cancelRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationConnectRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationConnectRequestedEventParams {
  request_id : String
  context_id : Int
  reader : String
  share_mode : SmartCardEmulationShareMode
  preferred_protocols : SmartCardEmulationProtocolSet
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationConnectRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationConnectRequestedEventParams::to_json(
  self : SmartCardEmulationConnectRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  fields["reader"] = Json::string(self.reader)
  fields["shareMode"] = self.share_mode.to_json()
  fields["preferredProtocols"] = self.preferred_protocols.to_json()
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.connectRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationConnectRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationConnectRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.connectRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    context_id: typed_required_int(method_name, fields, "contextId", "integer"),
    reader: typed_required_string(method_name, fields, "reader", "string"),
    share_mode: SmartCardEmulationShareMode::from_json(
      typed_required_json(method_name, fields, "shareMode"),
    ),
    preferred_protocols: SmartCardEmulationProtocolSet::from_json(
      typed_required_json(method_name, fields, "preferredProtocols"),
    ),
  }
}

///|
/// Build a typed SmartCardEmulation.connectRequested event envelope.
///
/// Pass a SmartCardEmulationConnectRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_connect_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_connect_requested_event(
  params : SmartCardEmulationConnectRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.connectRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationControlRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationControlRequestedEventParams {
  request_id : String
  handle : Int
  control_code : Int
  data : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationControlRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationControlRequestedEventParams::to_json(
  self : SmartCardEmulationControlRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["controlCode"] = Json::number(self.control_code.to_double())
  fields["data"] = Json::string(self.data)
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.controlRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationControlRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationControlRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.controlRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    control_code: typed_required_int(
      method_name, fields, "controlCode", "integer",
    ),
    data: typed_required_string(method_name, fields, "data", "string"),
  }
}

///|
/// Build a typed SmartCardEmulation.controlRequested event envelope.
///
/// Pass a SmartCardEmulationControlRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_control_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_control_requested_event(
  params : SmartCardEmulationControlRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.controlRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationDisconnectRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationDisconnectRequestedEventParams {
  request_id : String
  handle : Int
  disposition : SmartCardEmulationDisposition
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationDisconnectRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationDisconnectRequestedEventParams::to_json(
  self : SmartCardEmulationDisconnectRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["disposition"] = self.disposition.to_json()
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.disconnectRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationDisconnectRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationDisconnectRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.disconnectRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    disposition: SmartCardEmulationDisposition::from_json(
      typed_required_json(method_name, fields, "disposition"),
    ),
  }
}

///|
/// Build a typed SmartCardEmulation.disconnectRequested event envelope.
///
/// Pass a SmartCardEmulationDisconnectRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_disconnect_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_disconnect_requested_event(
  params : SmartCardEmulationDisconnectRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.disconnectRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationEndTransactionRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationEndTransactionRequestedEventParams {
  request_id : String
  handle : Int
  disposition : SmartCardEmulationDisposition
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationEndTransactionRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationEndTransactionRequestedEventParams::to_json(
  self : SmartCardEmulationEndTransactionRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["disposition"] = self.disposition.to_json()
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.endTransactionRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationEndTransactionRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationEndTransactionRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.endTransactionRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    disposition: SmartCardEmulationDisposition::from_json(
      typed_required_json(method_name, fields, "disposition"),
    ),
  }
}

///|
/// Build a typed SmartCardEmulation.endTransactionRequested event envelope.
///
/// Pass a SmartCardEmulationEndTransactionRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_end_transaction_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_end_transaction_requested_event(
  params : SmartCardEmulationEndTransactionRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.endTransactionRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationEstablishContextRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationEstablishContextRequestedEventParams {
  request_id : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationEstablishContextRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationEstablishContextRequestedEventParams::to_json(
  self : SmartCardEmulationEstablishContextRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.establishContextRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationEstablishContextRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationEstablishContextRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.establishContextRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
  }
}

///|
/// Build a typed SmartCardEmulation.establishContextRequested event envelope.
///
/// Pass a SmartCardEmulationEstablishContextRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_establish_context_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_establish_context_requested_event(
  params : SmartCardEmulationEstablishContextRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.establishContextRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationGetAttribRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationGetAttribRequestedEventParams {
  request_id : String
  handle : Int
  attrib_id : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationGetAttribRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationGetAttribRequestedEventParams::to_json(
  self : SmartCardEmulationGetAttribRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["attribId"] = Json::number(self.attrib_id.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.getAttribRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationGetAttribRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationGetAttribRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.getAttribRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    attrib_id: typed_required_int(method_name, fields, "attribId", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.getAttribRequested event envelope.
///
/// Pass a SmartCardEmulationGetAttribRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_get_attrib_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_get_attrib_requested_event(
  params : SmartCardEmulationGetAttribRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.getAttribRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationGetStatusChangeRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationGetStatusChangeRequestedEventParams {
  request_id : String
  context_id : Int
  reader_states : Array[SmartCardEmulationReaderStateIn]
  timeout : Int?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationGetStatusChangeRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationGetStatusChangeRequestedEventParams::to_json(
  self : SmartCardEmulationGetStatusChangeRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  fields["readerStates"] = Json::array(
    self.reader_states.map(item => item.to_json()),
  )
  if self.timeout is Some(value) {
    fields["timeout"] = Json::number(value.to_double())
  }
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.getStatusChangeRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationGetStatusChangeRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationGetStatusChangeRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.getStatusChangeRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    context_id: typed_required_int(method_name, fields, "contextId", "integer"),
    reader_states: typed_param_array(
      method_name,
      typed_required_json(method_name, fields, "readerStates"),
      "readerStates",
      "array<ReaderStateIn>",
    ).map(item => SmartCardEmulationReaderStateIn::from_json(item)),
    timeout: typed_optional_int(method_name, fields, "timeout", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.getStatusChangeRequested event envelope.
///
/// Pass a SmartCardEmulationGetStatusChangeRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_get_status_change_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_get_status_change_requested_event(
  params : SmartCardEmulationGetStatusChangeRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.getStatusChangeRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationListReadersRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationListReadersRequestedEventParams {
  request_id : String
  context_id : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationListReadersRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationListReadersRequestedEventParams::to_json(
  self : SmartCardEmulationListReadersRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.listReadersRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationListReadersRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationListReadersRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.listReadersRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    context_id: typed_required_int(method_name, fields, "contextId", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.listReadersRequested event envelope.
///
/// Pass a SmartCardEmulationListReadersRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_list_readers_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_list_readers_requested_event(
  params : SmartCardEmulationListReadersRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.listReadersRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationReleaseContextRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationReleaseContextRequestedEventParams {
  request_id : String
  context_id : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationReleaseContextRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationReleaseContextRequestedEventParams::to_json(
  self : SmartCardEmulationReleaseContextRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["contextId"] = Json::number(self.context_id.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.releaseContextRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationReleaseContextRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationReleaseContextRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.releaseContextRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    context_id: typed_required_int(method_name, fields, "contextId", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.releaseContextRequested event envelope.
///
/// Pass a SmartCardEmulationReleaseContextRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_release_context_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_release_context_requested_event(
  params : SmartCardEmulationReleaseContextRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.releaseContextRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationSetAttribRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationSetAttribRequestedEventParams {
  request_id : String
  handle : Int
  attrib_id : Int
  data : String
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationSetAttribRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationSetAttribRequestedEventParams::to_json(
  self : SmartCardEmulationSetAttribRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["attribId"] = Json::number(self.attrib_id.to_double())
  fields["data"] = Json::string(self.data)
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.setAttribRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationSetAttribRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationSetAttribRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.setAttribRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    attrib_id: typed_required_int(method_name, fields, "attribId", "integer"),
    data: typed_required_string(method_name, fields, "data", "string"),
  }
}

///|
/// Build a typed SmartCardEmulation.setAttribRequested event envelope.
///
/// Pass a SmartCardEmulationSetAttribRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_set_attrib_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_set_attrib_requested_event(
  params : SmartCardEmulationSetAttribRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.setAttribRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationStatusRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationStatusRequestedEventParams {
  request_id : String
  handle : Int
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationStatusRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationStatusRequestedEventParams::to_json(
  self : SmartCardEmulationStatusRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.statusRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationStatusRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationStatusRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.statusRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
  }
}

///|
/// Build a typed SmartCardEmulation.statusRequested event envelope.
///
/// Pass a SmartCardEmulationStatusRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_status_requested_event(params, session_id=None)
/// ```
pub fn smart_card_emulation_status_requested_event(
  params : SmartCardEmulationStatusRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.statusRequested",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for SmartCardEmulationTransmitRequestedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct SmartCardEmulationTransmitRequestedEventParams {
  request_id : String
  handle : Int
  data : String
  protocol : SmartCardEmulationProtocol?
} derive(Eq, Debug)

///|
/// Encode SmartCardEmulationTransmitRequestedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn SmartCardEmulationTransmitRequestedEventParams::to_json(
  self : SmartCardEmulationTransmitRequestedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["requestId"] = Json::string(self.request_id)
  fields["handle"] = Json::number(self.handle.to_double())
  fields["data"] = Json::string(self.data)
  if self.protocol is Some(value) {
    fields["protocol"] = value.to_json()
  }
  Json::object(fields)
}

///|
/// Decode and validate SmartCardEmulation.transmitRequested event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn SmartCardEmulationTransmitRequestedEventParams::from_json(
  params : Json,
) -> SmartCardEmulationTransmitRequestedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "SmartCardEmulation.transmitRequested"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    request_id: typed_required_string(
      method_name, fields, "requestId", "string",
    ),
    handle: typed_required_int(method_name, fields, "handle", "integer"),
    data: typed_required_string(method_name, fields, "data", "string"),
    protocol: match typed_optional_json(fields, "protocol") {
      Some(value) => Some(SmartCardEmulationProtocol::from_json(value))
      None => None
    },
  }
}

///|
/// Build a typed SmartCardEmulation.transmitRequested event envelope.
///
/// Pass a SmartCardEmulationTransmitRequestedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = smart_card_emulation_transmit_requested_event(
///   params,
///   session_id=None,
/// )
/// ```
pub fn smart_card_emulation_transmit_requested_event(
  params : SmartCardEmulationTransmitRequestedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="SmartCardEmulation.transmitRequested",
    params=params.to_json(),
    session_id?,
  )
}
