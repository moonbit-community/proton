///|
// Generated by tools/gen_protocol_manifest.mjs. Do not edit by hand.

///|
/// Generated CDP value object for TetheringBindParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct TetheringBindParams {
  port : Int
} derive(Eq, Debug)

///|
/// Encode TetheringBindParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn TetheringBindParams::to_json(self : TetheringBindParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["port"] = Json::number(self.port.to_double())
  Json::object(fields)
}

///|
/// Build a typed Tethering.bind command envelope.
///
/// Pass a TetheringBindParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = tethering_bind_command(id=1, params, session_id=None)
/// ```
pub fn tethering_bind_command(
  id~ : Int,
  params : TetheringBindParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Tethering.bind",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for TetheringUnbindParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct TetheringUnbindParams {
  port : Int
} derive(Eq, Debug)

///|
/// Encode TetheringUnbindParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn TetheringUnbindParams::to_json(self : TetheringUnbindParams) -> Json {
  let fields : Map[String, Json] = {}
  fields["port"] = Json::number(self.port.to_double())
  Json::object(fields)
}

///|
/// Build a typed Tethering.unbind command envelope.
///
/// Pass a TetheringUnbindParams value for command parameters. The builder validates
/// the generated JSON against the bundled protocol schema and returns a
/// `CdpCommandMessage` that can be sent with `CdpClient::send_cdp_message`.
///
/// # Example
/// ```mbt nocheck
/// let command = tethering_unbind_command(id=1, params, session_id=None)
/// ```
pub fn tethering_unbind_command(
  id~ : Int,
  params : TetheringUnbindParams,
  session_id? : String,
) -> @protocol.CdpCommandMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_command(
    id~,
    method_name="Tethering.unbind",
    params=params.to_json(),
    session_id?,
  )
}

///|
/// Generated CDP value object for TetheringAcceptedEventParams.
///
/// Fields follow the Chrome DevTools Protocol schema. Optional schema fields
/// are represented with `?`; nested generated types are used when the protocol
/// gives a referenced type name.
pub(all) struct TetheringAcceptedEventParams {
  port : Int
  connection_id : String
} derive(Eq, Debug)

///|
/// Encode TetheringAcceptedEventParams as CDP JSON.
///
/// Use this when passing generated parameter objects to lower-level command
/// builders or when inspecting the exact wire payload sent to Chrome.
pub fn TetheringAcceptedEventParams::to_json(
  self : TetheringAcceptedEventParams,
) -> Json {
  let fields : Map[String, Json] = {}
  fields["port"] = Json::number(self.port.to_double())
  fields["connectionId"] = Json::string(self.connection_id)
  Json::object(fields)
}

///|
/// Decode and validate Tethering.accepted event parameters.
///
/// The input must be the event `params` object from a CDP event. Required
/// fields and known primitive types are checked against the bundled protocol
/// schema before the generated value is returned.
pub fn TetheringAcceptedEventParams::from_json(
  params : Json,
) -> TetheringAcceptedEventParams raise @protocol.ProtocolSchemaError {
  let method_name = "Tethering.accepted"
  ignore(@protocol.cdp_schema_event(method_name~, params~))
  let fields = typed_json_fields(method_name, params)
  {
    port: typed_required_int(method_name, fields, "port", "integer"),
    connection_id: typed_required_string(
      method_name, fields, "connectionId", "string",
    ),
  }
}

///|
/// Build a typed Tethering.accepted event envelope.
///
/// Pass a TetheringAcceptedEventParams value decoded from or intended for the event params.
/// The builder validates the generated JSON against the bundled protocol
/// schema and returns a `CdpEventMessage`.
///
/// # Example
/// ```mbt nocheck
/// let event = tethering_accepted_event(params, session_id=None)
/// ```
pub fn tethering_accepted_event(
  params : TetheringAcceptedEventParams,
  session_id? : String,
) -> @protocol.CdpEventMessage raise @protocol.ProtocolSchemaError {
  @protocol.cdp_schema_event(
    method_name="Tethering.accepted",
    params=params.to_json(),
    session_id?,
  )
}
