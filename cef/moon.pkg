import {
  "justjavac/ffi" @ffi,
}

supported_targets = "native"

options(
  "native-stub": [ "cef_native.c" ],
  targets: {
    "binding.mbt": [ "native" ],
    "capabilities.mbt": [ "native" ],
    "cef_wbtest.mbt": [ "native" ],
    "config.mbt": [ "native" ],
    "runtime.mbt": [ "native" ],
  },
)
