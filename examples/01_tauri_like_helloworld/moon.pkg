import {
  "justjavac/proton",
}

supported_targets = "native"

dev_build(rule: "embed", input: "index.html", output: "index.mbt")

formatter(ignore: [ "index.mbt" ])

pkgtype(kind: "executable")

options(
  targets: { "main.mbt": [ "native" ] },
)
