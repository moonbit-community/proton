import {
  "moonbitlang/async",
  "justjavac/proton",
}

supported_targets = "native"

dev_build(rule: "embed", input: "hello.html", output: "hello.mbt")

options(
  formatter: { "ignore": [ "hello.mbt" ] },
  "is-main": true,
  targets: { "main.mbt": [ "native" ] },
)
