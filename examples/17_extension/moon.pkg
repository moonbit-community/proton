import {
  "moonbit-community/proton",
}

supported_targets = "native"

dev_build(rule: "embed", input: "extension.html", output: "extension.mbt")

formatter(ignore: [ "extension.mbt" ])

pkgtype(kind: "executable")

options(
  targets: { "main.mbt": [ "native" ] },
)
