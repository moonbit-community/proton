// Generated by `moon tool embed --text`, do not edit.

///|
let resource : String =
  #|<html>
  #|    <head>
  #|        <style>
  #|            :root {
  #|                color-scheme: light dark;
  #|                --bg-splash: rgba(244, 189, 122, 0.55);
  #|                --bg-start: #f4efe3;
  #|                --bg-end: #dce9f0;
  #|                --ink: #14313a;
  #|                --muted: #607781;
  #|                --shell: rgba(255, 255, 255, 0.9);
  #|                --shell-line: rgba(20, 49, 58, 0.08);
  #|                --panel: #edf5f7;
  #|                --panel-line: rgba(20, 49, 58, 0.12);
  #|                --field: rgba(255, 255, 255, 0.94);
  #|                --surface: #ffffff;
  #|                --code-chip: #deeff4;
  #|                --button: #14313a;
  #|                --button-alt: #0a7a84;
  #|                --button-ghost: #ffffff;
  #|                --button-ghost-text: #14313a;
  #|                --button-warn: #9f5036;
  #|                --notice-ok: #ddf1ea;
  #|                --notice-error: #f7dfdb;
  #|                --shadow: rgba(20, 49, 58, 0.15);
  #|            }
  #|
  #|            @media (prefers-color-scheme: dark) {
  #|                :root {
  #|                    --bg-splash: rgba(52, 211, 153, 0.3);
  #|                    --bg-start: #0e171f;
  #|                    --bg-end: #151c27;
  #|                    --ink: #edf3f8;
  #|                    --muted: #9eb1bc;
  #|                    --shell: rgba(16, 24, 31, 0.9);
  #|                    --shell-line: rgba(237, 243, 248, 0.08);
  #|                    --panel: #111b24;
  #|                    --panel-line: rgba(237, 243, 248, 0.1);
  #|                    --field: #0f1820;
  #|                    --surface: #0c141b;
  #|                    --code-chip: #12303a;
  #|                    --button: #34d399;
  #|                    --button-alt: #22c1b4;
  #|                    --button-ghost: #16212b;
  #|                    --button-ghost-text: #edf3f8;
  #|                    --button-warn: #f87171;
  #|                    --notice-ok: #0f2a29;
  #|                    --notice-error: #351918;
  #|                    --shadow: rgba(0, 0, 0, 0.42);
  #|                }
  #|            }
  #|
  #|            body {
  #|                margin: 0;
  #|                min-height: 100vh;
  #|                background:
  #|                    radial-gradient(circle at top left, var(--bg-splash), transparent 32%),
  #|                    linear-gradient(145deg, var(--bg-start) 0%, var(--bg-end) 100%);
  #|                color: var(--ink);
  #|                font-family: "Avenir Next", "Segoe UI", sans-serif;
  #|            }
  #|            .shell {
  #|                width: min(68rem, calc(100vw - 2rem));
  #|                margin: 1rem auto;
  #|                padding: 1.25rem;
  #|                border-radius: 28px;
  #|                background: var(--shell);
  #|                border: 1px solid var(--shell-line);
  #|                box-shadow: 0 26px 90px var(--shadow);
  #|            }
  #|            h1,
  #|            h2,
  #|            p {
  #|                margin: 0;
  #|            }
  #|            .hero,
  #|            .panel,
  #|            .stack,
  #|            .form {
  #|                display: grid;
  #|                gap: 0.85rem;
  #|            }
  #|            .hero {
  #|                margin-bottom: 1rem;
  #|            }
  #|            .hero p {
  #|                color: var(--muted);
  #|                max-width: 50rem;
  #|            }
  #|            code {
  #|                padding: 0.16rem 0.42rem;
  #|                border-radius: 999px;
  #|                background: var(--code-chip);
  #|            }
  #|            .layout {
  #|                display: grid;
  #|                grid-template-columns: minmax(0, 1.2fr) minmax(18rem, 0.9fr);
  #|                gap: 1rem;
  #|            }
  #|            .panel {
  #|                padding: 1rem;
  #|                border-radius: 22px;
  #|                background: var(--panel);
  #|                border: 1px solid var(--panel-line);
  #|            }
  #|            label {
  #|                display: grid;
  #|                gap: 0.35rem;
  #|                font-size: 0.95rem;
  #|                font-weight: 600;
  #|            }
  #|            input,
  #|            textarea,
  #|            button {
  #|                font: inherit;
  #|            }
  #|            input,
  #|            textarea {
  #|                width: 100%;
  #|                box-sizing: border-box;
  #|                border: 1px solid var(--panel-line);
  #|                border-radius: 14px;
  #|                background: var(--field);
  #|                color: var(--ink);
  #|                padding: 0.82rem 0.95rem;
  #|            }
  #|            textarea {
  #|                min-height: 14rem;
  #|                resize: vertical;
  #|                line-height: 1.48;
  #|            }
  #|            .actions {
  #|                display: grid;
  #|                grid-template-columns: repeat(2, minmax(0, 1fr));
  #|                gap: 0.75rem;
  #|            }
  #|            button {
  #|                border: 0;
  #|                border-radius: 999px;
  #|                padding: 0.82rem 1rem;
  #|                background: var(--button);
  #|                color: white;
  #|                font-weight: 600;
  #|                cursor: pointer;
  #|            }
  #|            button.alt {
  #|                background: var(--button-alt);
  #|            }
  #|            button.ghost {
  #|                background: var(--button-ghost);
  #|                color: var(--button-ghost-text);
  #|                border: 1px solid var(--panel-line);
  #|            }
  #|            button.warn {
  #|                background: var(--button-warn);
  #|            }
  #|            button:disabled {
  #|                opacity: 0.65;
  #|                cursor: progress;
  #|            }
  #|            .notice,
  #|            .meta {
  #|                border-radius: 16px;
  #|                padding: 0.82rem 0.95rem;
  #|                background: var(--surface);
  #|                border: 1px solid var(--panel-line);
  #|            }
  #|            .notice.error {
  #|                background: var(--notice-error);
  #|            }
  #|            .notice.ok {
  #|                background: var(--notice-ok);
  #|            }
  #|            .muted {
  #|                color: var(--muted);
  #|            }
  #|            .list {
  #|                display: grid;
  #|                gap: 0.55rem;
  #|                margin: 0;
  #|                padding: 0;
  #|                list-style: none;
  #|                max-height: 18rem;
  #|                overflow: auto;
  #|            }
  #|            .list li {
  #|                border-radius: 14px;
  #|                background: var(--surface);
  #|                border: 1px solid var(--panel-line);
  #|                padding: 0.72rem 0.82rem;
  #|                font-family: "SF Mono", "Menlo", monospace;
  #|                font-size: 0.88rem;
  #|            }
  #|            .meta strong {
  #|                display: block;
  #|                margin-bottom: 0.28rem;
  #|                color: var(--muted);
  #|                font-size: 0.78rem;
  #|                text-transform: uppercase;
  #|            }
  #|            @media (max-width: 860px) {
  #|                .layout,
  #|                .actions {
  #|                    grid-template-columns: 1fr;
  #|                }
  #|            }
  #|        </style>
  #|    </head>
  #|    <body>
  #|        <div class="shell">
  #|            <div class="hero">
  #|                <h1>__MoonBit__ App Runtime + Dialog + Clipboard + Files</h1>
  #|                <p>
  #|                    This example uses <code>window.__MoonBit__.dialog</code> and
  #|                    <code>window.__MoonBit__.clipboard</code>, then writes selected
  #|                    save destinations through <code>ext:fs/write_file</code>.
  #|                </p>
  #|            </div>
  #|            <div class="layout">
  #|                <div class="stack">
  #|                    <div class="panel">
  #|                        <h2>Desktop Controls</h2>
  #|                        <div class="form">
  #|                            <label>
  #|                                Dialog title
  #|                                <input id="title" value="__MoonBit__ Desktop Runtime" />
  #|                            </label>
  #|                            <label>
  #|                                Message / clipboard text
  #|                                <textarea id="message">__MoonBit__ runtime bridge is talking to native dialogs, files, and the system clipboard.</textarea>
  #|                            </label>
  #|                            <label>
  #|                                Suggested file path
  #|                                <input id="filePath" value="moonbit-runtime-demo.txt" />
  #|                            </label>
  #|                            <div class="actions">
  #|                                <button id="messageBtn">Show Message</button>
  #|                                <button id="confirmBtn" class="alt">Confirm Action</button>
  #|                                <button id="openBtn" class="ghost">Open File Dialog</button>
  #|                                <button id="saveBtn" class="ghost">Save File Dialog</button>
  #|                                <button id="writeClipboardBtn">Write Clipboard</button>
  #|                                <button id="readClipboardBtn" class="warn">Read Clipboard</button>
  #|                            </div>
  #|                        </div>
  #|                    </div>
  #|                </div>
  #|                <div class="stack">
  #|                    <div class="panel">
  #|                        <h2>Status</h2>
  #|                        <div id="notice" class="notice ok">Runtime is ready.</div>
  #|                        <div id="selection" class="meta">
  #|                            <strong>Last Selection</strong>
  #|                            <span>-</span>
  #|                        </div>
  #|                        <div id="clipboardState" class="meta">
  #|                            <strong>Clipboard</strong>
  #|                            <span>-</span>
  #|                        </div>
  #|                    </div>
  #|                    <div class="panel">
  #|                        <h2>Extension Activity</h2>
  #|                        <p class="muted">
  #|                            Events arrive via <code>__MoonBit__.events.on(...)</code>.
  #|                        </p>
  #|                        <ul id="events" class="list">
  #|                            <li class="muted">No native events yet.</li>
  #|                        </ul>
  #|                    </div>
  #|                </div>
  #|            </div>
  #|        </div>
  #|        <script>
  #|            const app = window.__MoonBit__;
  #|            const dialog = app.dialog;
  #|            const clipboard = app.clipboard;
  #|            const titleInput = document.getElementById("title");
  #|            const messageInput = document.getElementById("message");
  #|            const filePathInput = document.getElementById("filePath");
  #|            const notice = document.getElementById("notice");
  #|            const selection = document.getElementById("selection").querySelector("span");
  #|            const clipboardState = document.getElementById("clipboardState").querySelector("span");
  #|            const events = document.getElementById("events");
  #|            const buttons = Array.from(document.querySelectorAll("button"));
  #|
  #|            function setBusy(busy) {
  #|                buttons.forEach((button) => {
  #|                    button.disabled = busy;
  #|                });
  #|            }
  #|
  #|            function setNotice(message, kind) {
  #|                notice.textContent = message;
  #|                notice.className = "notice " + (kind || "ok");
  #|            }
  #|
  #|            function renderEvent(event) {
  #|                const item = document.createElement("li");
  #|                const payload = event.payload || {};
  #|                const parts = [event.extension + "." + event.name];
  #|                if (payload.path) {
  #|                    parts.push(payload.path);
  #|                }
  #|                if (payload.level) {
  #|                    parts.push(payload.level);
  #|                }
  #|                if (payload.text_length != null) {
  #|                    parts.push(payload.text_length + " chars");
  #|                }
  #|                item.textContent = parts.join(" | ");
  #|                if (events.firstElementChild && events.firstElementChild.className === "muted") {
  #|                    events.innerHTML = "";
  #|                }
  #|                events.prepend(item);
  #|                while (events.children.length > 10) {
  #|                    events.removeChild(events.lastElementChild);
  #|                }
  #|            }
  #|
  #|            app.events.on("dialog.activity", renderEvent);
  #|            app.events.on("clipboard.activity", renderEvent);
  #|            app.events.on("fs.activity", renderEvent);
  #|
  #|            async function fsOp(name, payload) {
  #|                return app.core.invokeOp("ext:fs/" + name, payload);
  #|            }
  #|
  #|            document.getElementById("messageBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    await dialog.message({
  #|                        title: titleInput.value,
  #|                        message: messageInput.value,
  #|                        level: "info",
  #|                    });
  #|                    setNotice("Displayed a native message dialog.");
  #|                } catch (error) {
  #|                    setNotice("Message dialog failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|
  #|            document.getElementById("confirmBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    const reply = await dialog.confirm({
  #|                        title: titleInput.value,
  #|                        message: messageInput.value,
  #|                        level: "warning",
  #|                    });
  #|                    setNotice(
  #|                        reply.confirmed ? "Confirmed through the native dialog." : "The native dialog was cancelled.",
  #|                        reply.confirmed ? "ok" : "error",
  #|                    );
  #|                } catch (error) {
  #|                    setNotice("Confirm dialog failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|
  #|            document.getElementById("openBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    const reply = await dialog.openFile({
  #|                        title: "Pick a file",
  #|                        path: filePathInput.value,
  #|                    });
  #|                    selection.textContent = reply.path || "(cancelled)";
  #|                    if (reply.path) {
  #|                        filePathInput.value = reply.path;
  #|                    }
  #|                    setNotice(reply.path ? "Selected a file path." : "Open file dialog cancelled.");
  #|                } catch (error) {
  #|                    setNotice("Open file dialog failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|
  #|            document.getElementById("saveBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    const reply = await dialog.saveFile({
  #|                        title: "Choose a save destination",
  #|                        path: filePathInput.value,
  #|                    });
  #|                    selection.textContent = reply.path || "(cancelled)";
  #|                    if (reply.path) {
  #|                        filePathInput.value = reply.path;
  #|                        await fsOp("write_file", {
  #|                            path: reply.path,
  #|                            content: messageInput.value,
  #|                        });
  #|                    }
  #|                    setNotice(reply.path ? "Saved the message text to the selected file." : "Save file dialog cancelled.");
  #|                } catch (error) {
  #|                    setNotice("Save file dialog failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|
  #|            document.getElementById("writeClipboardBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    await clipboard.writeText({ text: messageInput.value });
  #|                    clipboardState.textContent = "wrote " + messageInput.value.length + " chars";
  #|                    setNotice("Wrote the current message into the system clipboard.");
  #|                } catch (error) {
  #|                    setNotice("Clipboard write failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|
  #|            document.getElementById("readClipboardBtn").addEventListener("click", async () => {
  #|                setBusy(true);
  #|                try {
  #|                    const reply = await clipboard.readText({});
  #|                    messageInput.value = reply.text || "";
  #|                    clipboardState.textContent = reply.text ? reply.text : "(empty)";
  #|                    setNotice(reply.text ? "Loaded text from the clipboard." : "Clipboard is empty.");
  #|                } catch (error) {
  #|                    setNotice("Clipboard read failed: " + String(error), "error");
  #|                }
  #|                setBusy(false);
  #|            });
  #|        </script>
  #|    </body>
  #|</html>
  #|
