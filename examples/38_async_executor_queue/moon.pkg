import {
  "moonbitlang/core/json",
  "moonbitlang/async" @async,
  "justjavac/ffi",
  "justjavac/lepus" @webview,
}

supported_targets = "native"

options(
  "is-main": true,
  "native-stub": [ "async_executor_native.c" ],
  "pre-build": [
    {
      "command": ":embed -i $input -o $output",
      "input": "app.html",
      "output": "app.mbt",
    },
  ],
  targets: { "main.mbt": [ "native" ] },
)
