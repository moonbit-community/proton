# Sync And Async Extensions

Shows sync and async command extensions installed through the same
`.extension(...)` API.

- `window.__MoonBit__.math.double(...)`
- `window.__MoonBit__.add.slowAdd(...)`

Run:

```sh
moon -C examples run 39_sync_async_extensions --target native
```
