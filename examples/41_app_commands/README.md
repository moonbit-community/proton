# App Commands

Proton command-extension bridge example and native E2E fixture.

`window.__MoonBit__.core.invokeOp(...)` calls registered MoonBit command handlers.

Run:

```sh
moon -C examples run 41_app_commands --target native
```
