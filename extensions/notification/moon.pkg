import {
  "moonbitlang/core/json",
  "justjavac/proton",
  "justjavac/proton/extension" @proton_extension,
  "justjavac/proton/native" @proton_native,
  "justjavac/proton_contract",
  "justjavac/proton_ext/notification/contract" @notification_contract,
}

import {
  "moonbitlang/core/debug",
} for "wbtest"

supported_targets = "native"

formatter(ignore: [ "extension.g.mbt" ])

options(
  targets: {
    "extension.g.mbt": [ "native" ],
    "extension.mbt": [ "native" ],
    "extension_wbtest.mbt": [ "native" ],
    "notification_native.mbt": [ "native" ],
  },
)
