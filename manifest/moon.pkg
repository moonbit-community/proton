import {
  "moonbitlang/core/debug" @debug,
  "moonbitlang/core/json",
}

supported_targets = "native"

options(
  targets: {
    "app_manifest.mbt": [ "native" ],
    "json_helpers.mbt": [ "native" ],
    "manifest_entry.mbt": [ "native" ],
    "manifest_extensions.mbt": [ "native" ],
    "manifest_window.mbt": [ "native" ],
  },
)
