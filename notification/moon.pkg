import {
  "justjavac/ffi" @ffi,
}

import {
  "moonbitlang/core/debug" @debug,
} for "wbtest"

supported_targets = "native"

options(
  "native-stub": [ "notification_native.c" ],
  targets: {
    "notification.mbt": [ "native" ],
    "notification_wbtest.mbt": [ "native" ],
  },
)
