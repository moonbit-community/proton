# proton/catalog

`moonbit-community/proton/catalog` discovers extension metadata without importing
extension code.

It scans explicit search roots for `proton.ext.json` and loads descriptors.

Catalog metadata is for tooling. Applications still link extensions explicitly
in MoonBit code.
