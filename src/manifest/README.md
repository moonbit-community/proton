# proton/manifest

`justjavac/proton/manifest` defines the runtime manifest types shared by
bootstrap, runtime, and the root facade.

It is a data package. Parsing belongs in `bootstrap`; lifecycle belongs in
`runtime`.
