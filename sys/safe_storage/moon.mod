name = "moonbit-community/proton_safe_storage"

version = "0.2.8"

readme = "README.md"

repository = "https://github.com/moonbit-community/proton/tree/main/sys/safe_storage"

license = "Apache-2.0"

keywords = [ "desktop", "safe-storage", "native", "windows", "macos" ]

description = "Native OS-backed safe storage helpers for MoonBit."

preferred_target = "native"

supported_targets = "+native"

source = "."
