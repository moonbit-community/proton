import {
  "moonbit-community/proton_ffi" @ffi,
  "moonbitlang/core/encoding/utf8",
  "moonbitlang/core/json",
}

supported_targets = "native"

options(
  link: { "native": { "stub-cc-flags": "${build.LINUX_STUB_FLAGS}" } },
  "native-stub": [ "tray_native.c" ],
)
